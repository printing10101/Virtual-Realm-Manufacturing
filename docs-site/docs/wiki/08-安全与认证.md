# 08. 安全与认证

## 8.1 统一鉴权 `app/auth/`

### 8.1.1 三令牌体系

| 令牌类型 | 来源 | 用途 | 验证器 |
|---------|------|------|--------|
| **LNN Flat Token** | 启动时生成（`.lnn_token` / `LNN_TOKEN` 环境变量） | 内部节点鉴权 + API 入口 | 文件读取 + 对比 |
| **JWT** | 用户登录后签发 | 用户态 API | HS256 + 过期校验 |
| **Agent Token** | 智能体签发 | 智能体间通信 | 独立密钥 + 短 TTL |

### 8.1.2 `unified_auth.py` 行为
`UnifiedAuthMiddleware` 是纯 ASGI 中间件：
1. 检查 `X-Request-ID`，缺失则生成
2. 公开路径短路：`/api/health`、`/api/v1/auth/login`、`/api/docs`、`/api/redoc`、`/api/openapi.json`、`/metrics`、SSE 任务流（可选）
3. 解析 Authorization Header：
   - `Bearer <jwt>` → 走 JWT 验证
   - `LNN <token>` / `Flat <token>` → 走 LNN 验证
   - `Agent <token>` → 走 Agent 验证
4. 写入 `request.state.user` / `request.state.agent`
5. 失败时返回 401/403，统一响应体

### 8.1.3 公开路径注册
```python
PUBLIC_PATHS = {
    "/api/health", "/api/v1/auth/login", "/api/v1/auth/register",
    "/api/docs", "/api/redoc", "/api/openapi.json",
    "/metrics", "/api/v1/jobs/*/stream",  # SSE 通常需要单独签发
}
```

## 8.2 RBAC 权限 `app/auth/permissions.py`

### 8.2.1 权限码
```python
PERMISSIONS = {
    "system:config",       # 系统配置
    "rule:edit",           # 编辑规则
    "rule:view",           # 查看规则
    "toolpath:edit",       # 编辑刀轨
    "toolpath:view",       # 查看刀轨
    "process:plan",        # 工艺规划
    "user:manage",         # 用户管理
    "audit:view",          # 审计日志查看
    "plugin:install",      # 安装插件
    "project:delete",      # 删除工程
    ...
}
```

### 8.2.2 角色
| 角色 | 默认权限 |
|------|----------|
| `admin` | 全部 |
| `engineer` | rule/edit, toolpath/edit, process/plan, project/* |
| `operator` | toolpath/view, project/view |
| `auditor` | audit/view |

### 8.2.3 检查函数
```python
require_permission(perm: str) -> Dependency
user_has_permission(user, perm) -> bool
```

## 8.3 JWT 工具 `app/auth/security.py`

- `create_access_token(data, expires_delta=None)` — 签发
- `verify_token(token)` — 验证
- `get_password_hash(password)` — bcrypt
- `verify_password(plain, hashed)` — 校验
- `TokenPayload` — Pydantic 解析

## 8.4 CORS 安全门 `app/middleware/cors_config.py`

### 8.4.1 强制规则
| 规则 | 描述 |
|------|------|
| ❌ | `allow_origins=["*"]` + `allow_credentials=True` 同时存在 |
| ✅ | 显式白名单 + 显式 allowed_methods/headers |
| ✅ | 缺失配置时使用安全的 dev 默认（localhost） |

### 8.4.2 启动校验
```python
def enforce_startup_security():
    if not validate_cors_config(app.state.cors_config):
        logger.critical("CORS configuration failed security check")
        sys.exit(1)
```

## 8.5 安全头中间件 `app/auth/security_headers_asgi.py`

纯 ASGI，不缓冲请求体：
- `Content-Security-Policy`
- `Strict-Transport-Security` (生产)
- `X-Frame-Options: DENY`
- `X-Content-Type-Options: nosniff`
- `Referrer-Policy: strict-origin-when-cross-origin`
- `Permissions-Policy`

## 8.6 限流 `app/middleware/rate_limiter.py`

- 基于 `slowapi.Limiter`
- 默认：`100/minute` per IP
- 端点覆盖：
  ```python
  @router.post("/login")
  @limiter.limit("5/minute")
  def login(): ...
  ```
- 异常：429 Too Many Requests

## 8.7 审计日志 `app/audit/audit_log.py`

### 8.7.1 核心结构
```python
class UserDecision(Enum):
    ACCEPT = "ACCEPT"
    MODIFY = "MODIFY"
    REJECT = "REJECT"
    SKIP = "SKIP"

class AIModule(Enum):
    LNN = "LNN"
    LLM = "LLM"
    RAG = "RAG"
    ROUTER = "Router"
    FUSION = "Fusion"

class OperationStatus(Enum):
    SUCCESS = "SUCCESS"
    FAILURE = "FAILURE"
    TIMEOUT = "TIMEOUT"
```

### 8.7.2 记录方式
- 落盘：`logs/audit/audit_log.jsonl`（JSON Lines）
- 同时入 PostgreSQL 供前端查询
- 前端：`src/composables/useAuditLog.ts`

## 8.8 空闲自动休眠 `app/sidecar/sidecar_lifecycle.py`

- `IdleAutoShutdownMiddleware` — BaseHTTPMiddleware
- 跟踪请求间隔，超过 30 min 触发：
  - 写入 sidecar 状态文件
  - 注册 atexit 处理器
  - 触发 `GracefulShutdownHandler.shutdown()`

## 8.9 数据主权 `app/api/v1/user_sovereignty.py`

- 用户级数据隔离
- 跨设备/跨实例数据可携带
- 端到端可追踪

## 8.10 令牌管理 `app/config.py:TokenConfig`

| 字段 | 含义 |
|------|------|
| `token` | 当前生效的 LNN Flat Token |
| `banned_tokens` | 撤销列表（写入 `.lnn_banned_tokens.json`） |
| `metadata_path` | 元数据（创建时间、来源等） |
| `history_path` | 轮换历史 |

### 8.10.1 优先级
1. `LNN_TOKEN` 环境变量（生产）
2. `.lnn_token` 文件
3. 启动时自动生成 UUID

### 8.10.2 轮换
- 命令：`POST /api/v1/auth/rotate-token`（需 admin）
- 旧 Token 写入 `banned_tokens`（保留 7 天）

## 8.11 审计与可观测性

| 维度 | 工具 |
|------|------|
| 应用日志 | `app/core/logging_config.py`（结构化 JSON） |
| 审计日志 | `app/audit/audit_log.py`（决策追踪） |
| 指标 | `/metrics`（Prometheus 文本） |
| 健康检查 | `/api/health`（深度）/ `/api/health/ping`（轻量） |
| 心跳 | `/api/v1/heartbeat` |
| 环形日志 | `/api/v1/logs/*` |
| 任务进度 | `/api/v1/jobs/*/stream`（SSE） |
