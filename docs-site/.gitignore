# docs-site 局部忽略规则
# 根 .gitignore 已全局忽略 node_modules/，本文件仅作显式声明与子项目策略说明。

# ============================================================
# 运行时依赖（pnpm install 生成）
# ============================================================
node_modules/

# ============================================================
# 构建产物（vitepress build 生成）
# ============================================================
dist/
.vitepress/dist/
.vitepress/cache/

# ============================================================
# 包管理器锁文件提交策略（P2-E7 修复）
# ------------------------------------------------------------
# 本子项目使用 pnpm 管理依赖，pnpm-lock.yaml 应当提交到仓库，
# 以保证文档站构建的可复现性（与根 .gitignore 注释一致）。
#
# 若 lockfile 因 pnpm 版本差异频繁变更导致噪声，可在根目录
# .gitignore 中显式添加 `docs-site/pnpm-lock.yaml` 并改为
# "按需安装、不锁版本" 策略。当前默认策略：提交 lockfile。
# ============================================================

# ============================================================
# IDE / 系统文件
# ============================================================
.vscode/
.idea/
.DS_Store
Thumbs.db
