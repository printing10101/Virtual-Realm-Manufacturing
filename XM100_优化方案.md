# XM-100 五轴加工能力优化方案

## 一、问题概述

当前系统拥有生产级的五轴后处理器 `xmachine.py`（428行），但 G 代码生成器 `gcode_generator.py` 仅支持三轴加工，导致五轴加工链路完全断开。经检查确认共 16 项差距，按优先级分为 P0/P1/P2 三级。

## 二、优化目标

打通 XM-100 五轴加工的完整链路：从 G 代码生成 → 后处理 → 材料/刀具/参数匹配 → 碰撞检测 → 文件传输 → 测试验证，实现可交付的五轴加工能力。

## 三、优化方案（按优先级排序）

---

### P0：基础链路打通（6 项）

#### P0-1：CONTROLLER_MAP 集成 xmachine_xm100

**现状**：`gcode_generator.py` 的 `CONTROLLER_MAP` 只有 fanuc_0i/siemens_840d/heidenhain_tnc 三个控制器，未挂载 `xmachine_xm100`。

**方案**：
- 在 `gcode_generator.py` 顶部导入 `XMachineXM100PostProcessor`
- 在 `CONTROLLER_MAP` 中添加 `"xmachine_xm100": XMachineXM100PostProcessor`
- 修改 `generate()` 方法，当 controller_type 为 `xmachine_xm100` 时，自动启用五轴运动指令生成（A/C 轴）

**涉及文件**：
- `python/app/process_planning/gcode_generator.py`

**验证标准**：`generator.generate(plan, controller_type="xmachine_xm100")` 能输出含 RTCP(G43.4)、TWP(G43.5)、A/C 轴指令的完整 G 代码。

---

#### P0-2：_generate_feature_code() 添加五轴运动指令

**现状**：`_generate_feature_code()` 只生成 X/Y/Z 三轴直线插补，无 A/C 轴旋转指令。

**方案**：
- 在 `_generate_feature_code()` 中增加五轴分支：当后处理器为 `XMachineXM100PostProcessor` 时，生成 A/C 轴联动指令
- 添加五轴特有 G 代码：G43.4（RTCP）、G43.5（TWP）、G68.2（坐标系旋转）
- 添加 A/C 轴角度计算逻辑：根据刀轴矢量计算 A 轴和 C 轴角度
- 添加五轴安全校验：奇异点检测、工作空间约束检查

**涉及文件**：
- `python/app/process_planning/gcode_generator.py`

**验证标准**：生成的 G 代码包含 A/C 轴角度值、RTCP/TWP 指令，且角度在 XM-100 工作范围内（A: ±120°, C: 360°连续）。

---

#### P0-3：材料库添加黄铜/铜/塑料

**现状**：`materials.json` 只有 4 种材料（45#钢/铝合金6061/不锈钢304/40Cr），缺少 XM-100 最常用的黄铜、铜、塑料。

**方案**：
在 `materials.json` 中添加以下材料：
1. **黄铜 H59**：密度 8.5 g/cm³，硬度 100 HB，切削性能 excellent
2. **紫铜 T2**：密度 8.9 g/cm³，硬度 80 HB，切削性能 good
3. **ABS 塑料**：密度 1.05 g/cm³，硬度 N/A，切削性能 excellent
4. **POM（赛钢）**：密度 1.41 g/cm³，硬度 N/A，切削性能 excellent
5. **尼龙 PA6**：密度 1.14 g/cm³，硬度 N/A，切削性能 good

**涉及文件**：
- `python/app/data/materials.json`
- `python/app/database/data/materials.json`（同步更新）

**验证标准**：材料库查询接口能返回新增材料，且切削参数库有对应参数。

---

#### P0-4：刀具库添加球头铣刀和锥度刀

**现状**：`tools.json` 只有麻花钻/立铣刀/面铣刀/中心钻，无球头铣刀和锥度刀。

**方案**：
在 `tools.json` 中添加以下刀具：
1. **球头铣刀 R0.5**：直径 1mm，硬质合金，五轴精加工标配
2. **球头铣刀 R1**：直径 2mm，硬质合金
3. **球头铣刀 R2**：直径 4mm，硬质合金
4. **球头铣刀 R3**：直径 6mm，硬质合金
5. **球头铣刀 R5**：直径 10mm，硬质合金
6. **锥度球头刀 R0.5×6°**：锥度 6°，颈长 10mm，五轴深腔加工
7. **锥度球头刀 R1×6°**：锥度 6°，颈长 15mm
8. **锥度球头刀 R1×10°**：锥度 10°，颈长 20mm

**涉及文件**：
- `python/app/data/tools.json`
- `python/app/database/data/tools.json`（同步更新）

**验证标准**：刀具库查询接口能返回新增刀具，且切削参数库有对应参数。

---

#### P0-5：切削参数库扩展

**现状**：`cutting_parameters.json` 只有 4 组参数，远不够覆盖新增材料和刀具。

**方案**：
添加以下切削参数组合：
1. 黄铜 + 硬质合金立铣刀：切削速度 150-300 m/min，进给 0.05-0.15 mm/齿
2. 紫铜 + 硬质合金立铣刀：切削速度 100-200 m/min，进给 0.03-0.1 mm/齿
3. ABS + 硬质合金立铣刀：切削速度 300-600 m/min，进给 0.1-0.3 mm/齿
4. POM + 硬质合金立铣刀：切削速度 200-500 m/min，进给 0.08-0.2 mm/齿
5. 铝合金 + 球头铣刀（五轴精加工）：切削速度 300-500 m/min，进给 0.02-0.08 mm/齿
6. 黄铜 + 球头铣刀：切削速度 120-250 m/min，进给 0.02-0.06 mm/齿
7. 不锈钢 + 球头铣刀：切削速度 40-80 m/min，进给 0.02-0.05 mm/齿
8. 45#钢 + 锥度球头刀：切削速度 80-150 m/min，进给 0.03-0.08 mm/齿

**涉及文件**：
- `python/app/data/cutting_parameters.json`
- `python/app/database/data/cutting_parameters.json`（同步更新）

**验证标准**：参数库能根据材料+刀具组合返回推荐参数。

---

#### P0-6：碰撞检测添加五轴旋转轴支持

**现状**：`collision_detector.py` 只实现三轴 AABB 碰撞检测，五轴旋转运动下刀柄碰撞/干涉完全没检测。

**方案**：
- 在 `CollisionDetector` 中添加 `check_5axis_collision()` 方法
- 实现 OBB（有向包围盒）碰撞检测，支持旋转轴运动
- 添加刀柄/刀杆与工件的干涉检测
- 添加旋转轴运动范围限制检查（A: ±120°, C: 360°）
- 添加工作台与刀具的碰撞检测

**涉及文件**：
- `python/app/simulation/collision_detector.py`

**验证标准**：五轴运动场景下能正确检测刀柄碰撞和旋转干涉。

---

### P1：加工质量提升（6 项）

#### P1-1：添加五轴刀路规划算法

**现状**：无五轴刀路规划算法，缺少 opencamlib 等库。

**方案**：
- 创建 `python/app/toolpath/five_axis_planner.py`
- 实现三种五轴刀路策略：
  1. **前倾角法（Lead-Angle）**：刀轴沿进给方向前倾，避免球头刀零速点
  2. **侧倾角法（Tilt-Angle）**：刀轴侧倾，用于侧壁精加工
  3. **插补法（Interpolation）**：刀轴矢量平滑插补，用于自由曲面精加工
- 实现奇异点检测与规避算法
- 实现刀轴矢量平滑过渡算法

**涉及文件**：
- `python/app/toolpath/five_axis_planner.py`（新建）
- `python/app/toolpath/__init__.py`（更新导出）

**验证标准**：能对 STL 模型生成五轴刀路，刀轴矢量连续无突变。

---

#### P1-2：PINN 参数范围扩展到 20000rpm

**现状**：`pinn.py` 参数范围上限 10000rpm，XM-100 主轴最高 20000rpm。

**方案**：
- 修改 `pinn.py` 中 `speed` 参数范围：`(500.0, 10000.0)` → `(500.0, 20000.0)`
- 同步修改归一化逻辑中的 `speed_max` 值
- 更新训练数据生成器中的速度采样范围

**涉及文件**：
- `python/app/simulation/cutting_force/pinn.py`

**验证标准**：PINN 模型能接受 20000rpm 输入并输出合理预测值。

---

#### P1-3：工艺规则添加多轴加工策略

**现状**：`process_rules.json` 只有 4 条规则，无多轴加工策略。

**方案**：
添加以下工艺规则：
1. **五轴粗加工策略**：使用立铣刀，侧铣方式，切深 ≤ 1D，切宽 ≤ 0.6D
2. **五轴半精加工策略**：使用球头刀，前倾角 5-15°，留余量 0.1-0.3mm
3. **五轴精加工策略**：使用球头刀，行距 ≤ 0.3R，表面粗糙度 Ra ≤ 0.8μm
4. **深腔加工策略**：使用锥度球头刀，避免刀柄干涉，分层加工
5. **薄壁加工策略**：对称加工，减小切深，提高主轴转速
6. **奇异点规避规则**：A 轴角度避开 0°±5° 奇异区域

**涉及文件**：
- `python/app/data/process_rules.json`
- `python/app/database/data/process_rules.json`（同步更新）

**验证标准**：工艺规划能根据特征类型自动选择多轴加工策略。

---

#### P1-4：修复 JEPA 导入路径

**现状**：存在两份 JEPA 代码（`app/ai/ijepa_3d/` 和 `research/multimodal_jepa/ijepa_3d/`），生产代码从 research 导入但路径不稳定。

**方案**：
- 统一 JEPA 代码到 `app/ai/ijepa_3d/`
- 删除 `research/multimodal_jepa/ijepa_3d/` 中的重复代码，改为从 `app.ai.ijepa_3d` 导入
- 修复所有 `from app.ai.ijepa_3d` 的导入路径
- 确保 `research_bridge.py` 正确桥接

**涉及文件**：
- `python/app/ai/ijepa_3d/`（保留并完善）
- `research/multimodal_jepa/ijepa_3d/`（清理重复代码）
- `python/app/research_bridge.py`（修复导入路径）

**验证标准**：所有 JEPA 导入路径统一，无重复代码。

---

#### P1-5：添加 Xmaker 文件传输集成

**现状**：无 Xmaker 文件传输集成代码。

**方案**：
- 创建 `python/app/integrations/xmaker_integration.py`
- 实现以下功能：
  1. G 代码文件传输到 XM-100 机床
  2. 加工状态实时监控
  3. 加工结果回传
  4. 机床文件管理（上传/下载/删除）
- 创建 `python/app/integrations/__init__.py` 导出集成模块

**涉及文件**：
- `python/app/integrations/xmaker_integration.py`（新建）
- `python/app/integrations/__init__.py`（更新导出）

**验证标准**：能通过 API 将 G 代码文件传输到 XM-100 机床。

---

#### P1-6：添加 XM-100 相关测试

**现状**：tests 目录下无 XM-100 相关测试。

**方案**：
创建以下测试文件：
1. `tests/test_xmachine_postprocessor.py`：测试五轴后处理器
2. `tests/test_five_axis_gcode.py`：测试五轴 G 代码生成
3. `tests/test_five_axis_collision.py`：测试五轴碰撞检测
4. `tests/test_material_tool_matching.py`：测试材料/刀具匹配
5. `tests/test_xmaker_integration.py`：测试 Xmaker 文件传输

**涉及文件**：
- `python/tests/test_xmachine_postprocessor.py`（新建）
- `python/tests/test_five_axis_gcode.py`（新建）
- `python/tests/test_five_axis_collision.py`（新建）
- `python/tests/test_material_tool_matching.py`（新建）
- `python/tests/test_xmaker_integration.py`（新建）

**验证标准**：所有测试通过，覆盖率 ≥ 80%。

---

### P2：壁垒建立（4 项）

#### P2-1：JEPA 变体集成到生产流水线

**现状**：3 个 JEPA 变体（multimodal_jepa、video_jepa、point_mae）均在 research 目录，未集成到生产流水线。

**方案**：
- 在 `app/ai/` 下创建统一的 JEPA 推理接口
- 实现 JEPA 特征提取 → 知识图谱注入 → 工艺推荐的完整链路
- 添加 JEPA 模型版本管理和热更新机制

**涉及文件**：
- `python/app/ai/jepa_service.py`（新建）
- `python/app/ai/__init__.py`（更新导出）

**验证标准**：能通过 API 调用 JEPA 模型进行特征提取和工艺推荐。

---

#### P2-2：CadQuery 自动化特征提取

**现状**：`cadquery_gen.py` 存在，但自动化特征提取功能不完整。

**方案**：
- 完善 `cadquery_gen.py` 中的特征识别算法
- 支持识别：孔/槽/台/圆角/倒角/自由曲面
- 实现 STEP/IGES 文件自动导入和特征提取
- 输出结构化特征数据供工艺规划使用

**涉及文件**：
- `python/app/cad/cadquery_gen.py`（完善）
- `python/app/cad/feature_recognizer.py`（新建）

**验证标准**：能自动识别 STEP 文件中的加工特征并输出特征列表。

---

#### P2-3：API 添加 XM-100 端点

**现状**：API 目录下无 XM-100 相关路由。

**方案**：
- 创建 `python/app/api/v1/xm100.py`
- 实现以下端点：
  1. `POST /api/v1/xm100/gcode`：生成 XM-100 G 代码
  2. `POST /api/v1/xm100/upload`：上传 G 代码到机床
  3. `GET /api/v1/xm100/status`：获取机床状态
  4. `GET /api/v1/xm100/materials`：获取可用材料列表
  5. `GET /api/v1/xm100/tools`：获取可用刀具列表
- 在 `python/app/api/v1/__init__.py` 中注册路由

**涉及文件**：
- `python/app/api/v1/xm100.py`（新建）
- `python/app/api/v1/__init__.py`（更新路由注册）

**验证标准**：API 端点可正常访问，返回正确数据。

---

#### P2-4：知识图谱与材料/刀具库深度互联

**现状**：知识图谱与材料/刀具库未深度集成。

**方案**：
- 在知识图谱中创建材料节点、刀具节点、工艺参数节点
- 建立材料-刀具-参数的关联关系
- 实现基于图谱的工艺推荐：输入材料 → 推荐刀具 → 推荐参数
- 支持图谱查询 API

**涉及文件**：
- `python/app/knowledge_graph/material_tool_graph.py`（新建）
- `python/app/knowledge_graph/__init__.py`（更新导出）

**验证标准**：能通过知识图谱查询材料对应的推荐刀具和参数。

---

## 四、实施计划

### 阶段一：基础链路打通（P0，预计 3-5 天）
1. P0-1：CONTROLLER_MAP 集成（0.5 天）
2. P0-2：五轴运动指令生成（1 天）
3. P0-3：材料库扩展（0.5 天）
4. P0-4：刀具库扩展（0.5 天）
5. P0-5：切削参数扩展（0.5 天）
6. P0-6：五轴碰撞检测（1 天）

### 阶段二：加工质量提升（P1，预计 5-7 天）
1. P1-1：五轴刀路规划（2 天）
2. P1-2：PINN 参数扩展（0.5 天）
3. P1-3：工艺规则扩展（1 天）
4. P1-4：JEPA 路径修复（0.5 天）
5. P1-5：Xmaker 集成（1 天）
6. P1-6：测试编写（1 天）

### 阶段三：壁垒建立（P2，预计 7-10 天）
1. P2-1：JEPA 集成（2 天）
2. P2-2：CadQuery 特征提取（2 天）
3. P2-3：API 端点（1.5 天）
4. P2-4：知识图谱互联（2 天）

---

## 五、风险评估

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 五轴刀路算法复杂度高 | 开发周期延长 | 先实现基础前倾角法，后续迭代 |
| 碰撞检测性能问题 | 实时性不足 | 使用空间划分加速（八叉树） |
| JEPA 代码整合影响研究环境 | 研究代码不可用 | 保留 research 目录，改为导入桥接 |
| Xmaker 机床通信协议不明确 | 集成困难 | 先实现文件传输，后续补充协议 |

---

## 六、验收标准

1. **功能验收**：能从 CAD 模型生成 XM-100 五轴 G 代码，并传输到机床
2. **性能验收**：G 代码生成时间 < 10 秒（简单零件），碰撞检测时间 < 1 秒
3. **质量验收**：生成的 G 代码能通过仿真验证，无碰撞/过切
4. **测试验收**：所有单元测试通过，覆盖率 ≥ 80%
