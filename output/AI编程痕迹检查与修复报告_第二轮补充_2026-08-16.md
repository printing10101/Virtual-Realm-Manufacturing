# AI 编程痕迹检查与修复 — 第二轮补充（2026-08-15/16）

在第一轮（ruff 静态清理 119→0 + 4 类 bug 修复）基础上，本轮补齐目标中未深入覆盖的检查项。

## 本轮修复（2 项）

### 1. 重复代码消除：AGENT_ENDPOINT_PERMISSIONS 双份维护
- **文件**：`app/agent/middleware.py` + `app/auth/permissions.py`
- **问题**：两份**内容完全一致**的端点到权限级别映射表（9 条）+ `WRITE_SCOPES = {"W","B","T"}`。
  `agent/middleware.py` 本地重定义（其 WRITE_SCOPES 定义后从未被使用=死代码），
  `auth/middleware.py` / `auth/unified_auth.py` 从 permissions 导入。权限变更时只改一处会静默漂移。
- **修复**：`agent/middleware.py` 删除本地定义，改为从 `app.auth.permissions` 导入
  （`AGENT_ENDPOINT_PERMISSIONS` / `WRITE_SCOPES` 单一来源）。
  验证：`same object: True`（两模块共享同一 dict 对象）；94 passed 无回归。

### 2. mypy 真实错误修复：agent/middleware.py 3 处
- **问题**（类型标注与运行时行为不符，属目标中"mypy 真实错误"）：
  - L77：`log_path: str | None` 参数被赋 `Path` 对象（类型不匹配）
  - L78：`Path(log_path)` 参数应为 `str | PathLike` 而非 `str | None`
  - L98：`self._stream = None` 无标注，被赋 `TextIOWrapper`（隐式 Any）
- **修复**：签名改 `str | os.PathLike[str] | None`；`self._stream: TextIO | None` 显式标注；补 `TextIO` 导入。
  验证：mypy 该文件 0 error；`AgentAuditLog` 运行时正常（哈希链写入 1 条）；89 passed 无回归。

## 本轮检查结论（无需修复，排除误报）

| 检查项 | 结论 |
|---|---|
| 前端死页面（34 个 views） | **0 个**。TaskHistory.vue 在 router/index.ts 中引用（相对路径检测误报） |
| NotImplementedError（23 处） | 全部为**抽象基类方法**（llm_client / provider_base / contracts / step_writer / trainer 等），强制子类实现的合法设计 |
| 测试中 type:ignore（test_observability_contract 等） | **合法用法**：故意传非法类型测试运行时校验（`Metric(value=True)` 等） |
| Provider 重复 import 头（12 个文件共享） | **一致架构模式**：每个 Provider 都需要 provider_base 基类符号，非无意义重复 |
| 两 middleware 相似代码块（9 处） | **同文件分支逻辑**（同一文件的 if/elif 分支内相似调用），提取会降低可读性 |
| `WRITE_SCOPES` 在 agent/middleware.py | 死代码，已随重复消除一并删除 |

## 回归汇总（两轮累计）

| 测试集 | 结果 |
|---|---|
| test_parametric_geometry.py | 80 passed |
| test_feature_extraction.py | 44 passed |
| 权限/middleware/审计相关 | 89+94 passed |
| CI 三目录（unit+integration+regression） | 3215 passed / 19 failed（全为环境：沙箱拒写 logs/、缺 mcp 包、无 torch） |
| ruff（engineering/python + mcp_server） | All checks passed |
| mypy（本次触碰文件） | 0 error |

## 剩余建议（后续轮次可选）
1. mypy 全量 ~1400 处/283 文件（attr-defined/arg-type/union-attr）——独立主战场，建议按模块分批
2. backup_service.py:142 既有 dict-item 类型错误
3. 15 个引用已删除模块的僵尸测试（有 skip 守卫，可清理或标注 deprecated）
4. 测试环境补装 mcp（FastMCP）/torch 消除 19 个环境失败噪音
