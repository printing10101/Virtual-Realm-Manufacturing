# 灵境制造 — 部署安装优化计划
## 目标：达到 Hermes Agent 水准的「一行命令安装、装完即用、文档与代码一致」

> 制定日期：2026-08-03 | 依据：本地仓库（v2.7.0）全量核查 + 远程 GitHub 仓库核查 + Hermes Agent 安装机制对照
> 状态：✅ 已定稿（2026-08-03，用户已确认 4 项关键决策）

**已确认决策（2026-08-03）**：
1. 桌面后端打包策略 = **方案 B：嵌入式 Python**（python-build-standalone + 包内 site-packages）
2. 本地 v2.7.0 **现在推送** GitHub（已做 git 盘点，见 P0-1）
3. 分发渠道 = **GitHub + 国内镜像**（Gitee Release / ghproxy / COS 等）
4. 发布范围 = **双主路收敛**（桌面安装包 + Docker 镜像；服务端一键/离线包标注「建设中」或移除承诺）

**P0-1 推送前置盘点（2026-08-03 实测）**：
- remote：`origin = github.com/printing10101/Virtual-Realm-Manufacturing.git` ✓
- 当前分支：`refactor/decouple-research-engineering`（V2.7.0 工作所在），**未建立 upstream**
- `main`：领先 origin/main **1 个提交**（`feat: V2.6.0 架构重构与契约层建设`，未推送）
- 工作区：**大量未提交修改**（Dockerfile、ci.yml、deploy/install.sh 等修复仍在工作区）+ 一批临时诊断脚本删除（`_fix_p2_7.*` 等，应随提交清理）
- 推送执行步骤：①审查并提交工作区改动（排除临时/产物目录）→ ②`git push -u origin refactor/decouple-research-engineering` → ③main 的 V2.6.0 提交：fast-forward 则直推，否则 PR 合并 → ④确认远程与本地一致

---

## 一、现状全景（2026-08-03 实测核查，证据已逐项核实）

### 1.1 版本断层（首要阻断）
| 位置 | 版本 | 证据 |
|---|---|---|
| 本地仓库 `tauri.conf.json` / `package.json` | **2.7.0** | 已读文件 |
| 远程 GitHub `printing10101/Virtual-Realm-Manufacturing` | **V2.5.0**（最新提交 2026-07-04） | WebFetch 实测 |
| 安装指南 / 国内部署指南 / `.env.sqlite.example` | **2.5.0** | 已读文件 |

**含义**：本地「上线版」的阶段2解耦、Dockerfile 修复、安全加固等大量改动**尚未推送到远程**。安装文档跟着远程 2.5.0 走，而本地 2.7.0 改变了目录结构（`python/` → `engineering/python/`）——这是「文档与代码脱节」的总根因。

### 1.2 目录结构迁移后未同步的残留（阶段2解耦遗漏）
| 文件 | 残留问题（均已 grep 证实） |
|---|---|
| `deploy/install.sh` | L96 `cd python`（应 `engineering/python`）；L121 systemd `WorkingDirectory=$PROJECT_ROOT/python`；L123 ExecStart venv 路径 → **整脚本失效** |
| `deploy/install.bat` | L73/L94 `cd python` → 失效 |
| `deploy/offline/install_offline.sh` | L113 `cd python` → 失效 |
| `deploy/offline/install_offline.bat` | L109/L122/L137 `cd python` → 失效 |
| `deploy/offline/prepare_offline.sh` | L140 `cp -r "$PROJECT_ROOT/python/app"` → 失效 |
| `.github/workflows/release.yml` | L182/L249 `cd python`；L260 `python/dist/`；L33 Python 3.10（实际需 3.11+/3.12）；L35 Node 18；L316 传 `BUILD_VERSION`（Dockerfile 无此 ARG）；**未传 `BASE_REGISTRY`**（华为云镜像缺 `python:3.12-slim` → 镜像构建会拉取失败）→ **发布流水线不可用** |
| `.github/workflows/ci.yml` | L267/L508 两处 `cd python` 残留 |
| `docs/user-guide/安装指南.md` | 排错节写「切到 python/ 目录」；软件依赖写「Python 3.10/3.11，暂不支持 3.12」与 Dockerfile `python:3.12-slim` 矛盾 |

> 注：根目录 `requirements.txt` 为薄包装（`-r engineering/python/requirements.txt`），设计正确，非缺陷。

### 1.3 桌面端结构性缺失（「开箱即用」不成立）
| 项 | 现状（已读源码证实） |
|---|---|
| `engineering/src-tauri/src/sidecar.rs` | 已**废弃 PyInstaller 二进制**，改为直接 `Command::new(python)` 执行 `start_server.py`；注释自述「binaries 目录为空（旧二进制过时已删除），sidecar spawn 始终失败，导致应用卡在启动中界面」 |
| 运行时定位 | `resolve_python_path()`：按 anaconda3 → `C:\Python3xx` → `%LOCALAPPDATA%\Programs\Python` → PATH 查找宿主 Python —— **依赖用户机器已装 Python** |
| `tauri.conf.json` bundle.resources | 仅 `../python/resources`（当前只有 `.gitkeep` 空目录）→ **打包产物不含后端代码** |
| 结论 | 当前 msi/nsis 产物即使构建出来，在无 Python 的机器上必然卡「启动中」；「开箱即用」只在开发模式（源码目录 + 本机 Python）下成立 |
| `package.json` `sidecar:build` | 调 `build_backend.py`（PyInstaller），已被 Rust 侧绕过 → **死脚本**，仍保留误导 |

### 1.4 发布设施与文档承诺的错位
- 文档承诺：GitHub Release 下载 `lingjing-setup-v2.5.0.exe`、`get.lingjing-mfg.com/install.sh`、`docker pull lingjing/manufacturing-api:2.5.0`、离线安装包。
- 现实：**13 个 workflow 中没有任何 Tauri/PyInstaller 桌面构建 job**（grep 全量证实）→ 桌面 EXE 无处产生；一键脚本 URL 指向外部域名（可达性未验证）；容器镜像未确认是否推送。
- 远程仓库 2.5.0 的 Releases 是否存在桌面产物：未验证（需登录态，列为待确认项）。

### 1.5 相对 Hermes 的体验差距（目标参照系）
| 维度 | Hermes Agent（参照） | 灵境制造现状 |
|---|---|---|
| 安装命令 | `curl -fsSL <url>/install.sh \| bash` 一条 | 文档四路（EXE/域名脚本/镜像/源码），大多不可达或失效 |
| 依赖处理 | 脚本自动装 uv + Python 3.11（无需 sudo） | 要求用户先装 Python 3.10+；`sudo` 进 systemd |
| 装完即用 | `hermes` 直接聊天；`hermes doctor` 自检 | 无等价 CLI；无自检命令 |
| 自包含 | uv 托管 Python，不污染系统 | 桌面依赖宿主 Python（版本漂移即挂） |
| 更新/卸载 | `hermes update` / `hermes uninstall` | 无 |
| 文档一致性 | 文档=代码（仓库内真实脚本） | 版本/路径/产物全面漂移 |

---

## 二、目标模型与验收标准

**目标**：任意干净机器（无预装依赖），一条命令或一个安装包，完成安装 → 启动 → 健康检查通过 → 界面可用；文档步骤与真实产物一一对应。

**验收标准（对标 Hermes）**：
- A1 服务端：干净 Ubuntu 22.04（无 Python）执行 `curl -fsSL <url>/install.sh | bash` 成功，`lingjing doctor` 全绿，`http://localhost:8765/api/health/ping` 返回 `{"status":"ok"}`。
- A2 桌面端：干净 Windows 11（无 Python/Node/Rust）安装 msi/nsis → 启动 → 后端自动就绪（健康检查通过）→ 主界面可用。
- A3 一键脚本 URL 在国内网络可达（用户位于中国大陆）。
- A4 文档（安装指南/国内部署指南/README）每一条命令在干净环境实测通过，版本号统一 2.7.0。
- A5 CI 中任何破坏安装链路的改动 → 红灯（安装冒烟测试纳入 PR 门禁）。
- A6 提供 `lingjing doctor/update/uninstall` 生命周期命令。

---

## 三、分阶段优化计划

### 阶段 0：发布底座对齐（先修「立即会失败」的问题，0.5–1 天）
> ✅ **已完成（2026-08-03）**：P0-1~P0-5 全部落地并验证，远程 main/refactor 已同步至 V2.7.0（5f717e8）。
> - P0-2 安装脚本族路径修复 + DB 初始化 API 同步（install.sh/install.bat/prepare_offline.*）
> - P0-3 release.yml 六类修复（版本/路径/pnpm 工作目录/wheel 步骤/BASE_REGISTRY/Dockerfile BUILD_VERSION ARG）
> - P0-4 ci.yml 旧路径 + matrix 版本
> - P0-5 version_sync.py 路径与逻辑修复，全仓统一 2.7.0
> - P0-1 推送完成（`git push origin refactor:main` fast-forward）
> 下一阶段：**阶段 1 服务端「一行命令」**（重写 install.sh 支持 curl|bash + uv 自装 Python、lingjing CLI、根 README）。
| 编号 | 任务 | 验收 |
|---|---|---|
| P0-1 | 将本地 v2.7.0 推送至 GitHub（先 `git status` 确认 remote/分支，确认 2.7.0 改动完整） | 远程 main 与本地一致 |
| P0-2 | 修复安装脚本族旧路径：`deploy/install.sh`、`install.bat`、`offline/*`、`prepare_offline.sh` → `engineering/python` | 每脚本 `bash -n` + 干净机实测 |
| P0-3 | 修复 `release.yml`：Python 3.10→3.12、`cd python`→`cd engineering/python`、`python/dist` 路径、`ruff` 路径、Dockerfile 补 `BUILD_VERSION` ARG（或删除该传参）、加 `BASE_REGISTRY=docker.io/library` build-arg | release 干跑（`act` 或 push tag 试跑）通过 |
| P0-4 | 修复 `ci.yml` L267/L508 旧路径 | CI 全绿 |
| P0-5 | 版本统一：跑 `scripts/version_sync.py`，同步 `.env.sqlite.example`、docs 至 2.7.0；版本一致性检查纳入 CI | 全仓库无 2.5.0 残留 |

### 阶段 1：服务端「一行命令」体验对齐（1–2 天）
> ✅ **已完成（2026-08-03）**，本机完整安装验证通过（Windows 版 A1/A6 验收）：
> - `lingjing doctor` 全绿、`start` 就绪 `HTTP 200 {"ping":true}`、`stop/restart/status` 完整工作
> - **验证过程中修复 40+ 处启动阻断 bug**（详见下节），这是本次验证的最大价值
> - 提交：cc5e7c9 / 10a7843 / 837e723（部分由用户并行提交）；⚠ 推送因 GitHub 网络超时未完成，恢复后需 `git push origin refactor/decouple-research-engineering:main`
| 编号 | 任务 | 验收 |
|---|---|---|
| P1-1 | 重写 `deploy/install.sh`：支持 `curl \| bash` 直拉执行；用 **uv** 自动安装 Python 3.12（对标 Hermes，无需 sudo）；创建 venv；装依赖（薄包装 requirements）；自动生成 `.env`（随机强 JWT/密钥）；初始化 DB；可选 systemd；结束打印访问地址 | 干净机一条命令装完（A1） |
| P1-2 | 新增 `lingjing` CLI（Python 入口，对标 hermes）：`doctor` / `start` / `stop` / `update` / `uninstall` | A6 |
| P1-3 | Windows 服务端 `deploy/install.ps1`：**按「双主路收敛」决策，本轮不重修**；文档明确「Windows 走桌面安装包，Linux 服务端走 Docker 镜像」 | 文档说清即可 |
| P1-4 | 补根 `README.md`（当前缺失）：「快速开始」一节放最顶部 | A4 |

### 阶段 2：桌面端「开箱即用」结构性修复（3–5 天，已定方案 B：嵌入式 Python）
> ✅ **核心完成（2026-08-03）**：
> - **P2-1 嵌入式运行时**：新增 `scripts/build_desktop_runtime.py`，本机构建验证成功（1.8GB、Python 3.12.13、37578 依赖文件 + 后端代码）；**自包含验证通过**（运行时自身 python 可 import fastapi/onnxruntime，后端导入到 JWT 安全门）
> - **P2-2 sidecar.rs**：嵌入式模式优先（resource_dir/desktop_runtime/runtime/python.exe + backend/start_server.py），回退宿主 Python；`cargo check` 通过
> - **P2-3 tauri.conf.json**：bundle.resources → `desktop_runtime`
> - **P2-4/P2-5**：新增 `.github/workflows/desktop-build.yml`（三平台矩阵 + Release 附加）；package.json `sidecar:build`（废弃 PyInstaller）→ `runtime:build`
> - ⚠ 剩余：完整 `pnpm tauri build` 端到端验证（需网络/GitHub Actions 或本机 pnpm）、体积优化（1.8GB 大头待裁剪，P2-6）
> - ⚠ git：并行操作曾误移除 engineering/python 跟踪，已恢复（HEAD 树 1197 文件）；**推送仍受阻（github.com:443 持续超时）**
| 编号 | 任务 | 验收 |
|---|---|---|
| P2-1 | **构建嵌入式 Python 运行时**：`python-build-standalone`（3.12，Windows x64 优先）打入 `engineering/src-tauri/resources/` 或 bundle.resources；`pip install --target` 把后端依赖装入包内 site-packages（确认 onnxruntime-only，零 research 依赖） | 运行时目录自包含、可独立启动 |
| P2-2 | `sidecar.rs` 改回 exec 打包内运行时：`Command::new(embedded_python)` + `start_server.py`（保留现有健康检查/日志/重启框架，改动集中在 `resolve_python_path` / `resolve_python_script_and_dir`，加 `LINGJING_EMBEDDED` 模式） | 开发模式（宿主 Python）与打包模式（嵌入式）双路径均通过 |
| P2-3 | 修复 `tauri.conf.json` bundle.resources → 打包产物含「嵌入式 Python + 后端代码 + 依赖」 | 无 Python 机器可跑（A2） |
| P2-4 | 新增 CI `desktop-build` job（windows-latest + ubuntu-latest + macos-latest；嵌入式运行时 + `pnpm tauri build`），产物上传 Release | 三平台产物生成 |
| P2-5 | `release.yml` create-release 挂载桌面产物；清理 `package.json` 死脚本 `sidecar:build`（PyInstaller 路线废弃）或改指嵌入式打包脚本 | 产物名与文档一致 |
| P2-6 | 体积报告：嵌入式 Python（~30MB）+ onnxruntime + 依赖，目标安装包 ≤ 500MB；大依赖（cadquery/geometry）评估裁剪或延迟安装 | 包体积报告 |

### 阶段 3：发布物与文档可信化（1 天）
| 编号 | 任务 | 验收 |
|---|---|---|
| P3-1 | 重写 `docs/user-guide/安装指南.md`（2.7.0 真实命令/产物/路径，删除不可达 URL 或标注「建设中」） | 每步实测通过 |
| P3-2 | 同步 `docs/国内部署指南.md`（离线包脚本路径、镜像源说明） | 离线包实测 |
| P3-3 | 发布设施建设（已确认：GitHub + 国内镜像）：一键脚本 URL = GitHub raw 源 + ghproxy/Gitee 镜像；容器镜像 = ghcr.io 源 + 华为云 SWR/阿里云 ACR 镜像；桌面产物 = GitHub Release + 国内镜像 | A3 |
| P3-4 | `docs-site/` 与根 README 同步 | 站点无死链 |

### 阶段 4：质量护栏（1–2 天）
| 编号 | 任务 | 验收 |
|---|---|---|
| P4-1 | CI 新增「安装冒烟」job：ubuntu runner 跑 `install.sh` + 健康检查；Windows runner 可选跑 msi 静默安装冒烟 | A5 |
| P4-2 | install 脚本加 shellcheck + `bash -n` 进 CI；`scripts/install.ps1`（已实现 embed-python 思路）review 并修正 | lint 绿 |
| P4-3 | 版本一致性检查（`version_sync.py`）进 PR 门禁 | 漂移即红灯 |

**总工期估算：约 2–2.5 周（含验证）。阶段 0/1/3 可并行，阶段 2（嵌入式 Python）为最长路径。**

---

## 四、关键决策（已于 2026-08-03 全部确认）

| 决策 | 结论 | 影响 |
|---|---|---|
| 1. 桌面后端打包策略 | **方案 B：嵌入式 Python**（python-build-standalone + 包内 site-packages） | 阶段 2 技术路线；自包含、可裁剪，需新建打包链路 |
| 2. 本地 v2.7.0 推送 | **现在推送**（git 盘点已完成，见首部 P0-1） | 阶段 0 第一个动作 |
| 3. 分发渠道 | **GitHub + 国内镜像**（ghproxy/Gitee Release/COS） | 阶段 3 发布设施；保障 A3 国内可达 |
| 4. 发布范围 | **双主路收敛**（桌面安装包 + Docker 镜像） | 服务端一键/离线包本轮不重修，文档明确入口 |

---

## 五、风险与约束
- **桌面包体积**：PyTorch 为体积大头；production 已零 research 依赖，确认 onnxruntime-only 后可显著瘦身（阶段 2 专项）。
- **国内网络**：GitHub Release/raw 下载不稳 → 必须国内镜像（决策点 3）；Docker 镜像华为云 SWR 路径已有。**2026-08-03 17:20 实测 github.com:443 连接超时**——推送/拉取需在网络窗口操作或配置代理。
- **Windows 打包**：需 WebView2（tauri.conf 已配 embedBootstrapper ✓）；PyInstaller 在 CI 上需缓存依赖避免超时。
- **发布即承诺**：文档只写已实测的路径，未建成的设施标注「建设中」，避免再次出现「文档承诺、产物缺失」。

---

## 附：阶段 1 安装验证修复清单（2026-08-03，40+ 处启动阻断）
> 安装验证（真实 uv venv + 全依赖启动）暴露的、日常测试未覆盖的问题——**证明「能跑通测试 ≠ 能启动」**。
1. **`__future__` + 装饰器 + 本地 Pydantic 模型陷阱**：精确扫描全 app/ 找到 60 处/23 文件（dynamic_adjustment、collision_check、dnc、documents、dxf_pipeline、equipment、explainability、goal_alignment、knowledge_graph、materials、process_routes、production、quality、skills、snapshots、task_checkout、template_*、tools、workflows、cad/process_router、projects/project_api、simulation/chatter/api、simulation/cutting_force/api）→ 批量删除 `from __future__ import annotations`（Python 3.11+ 无需）。
2. **拆分/迁移丢失**（V2.7.0 解耦 + 上午 God 拆分遗留）：
   - `rules/api.py`：整段丢失 117 行（router 定义 + 6 个 Request 模型 + 2 个函数）——从 592aedb 恢复完整版
   - `simulation/schemas.py`：缺 4 个模型（ConflictCheckRequest 等）——从拆分前恢复（曾误重复追加，已去重）
   - `simulation/_helpers.py`：缺 logging/APIRouter/HTTPException/Path/asyncio/time/timezone/validate_user_path/SimulationRequest
   - `process_understanding/routes.py`：router 未实例化 + ExplainRequest 类型整个丢失 + PredictionData 未导入 + logger 未定义
   - `rag/rag_retrieval.py`：_extract_query_entities/_CLUSTER_TAG_FILTERS/_QUERY_ENTITY_PATTERNS 丢失（从 592aedb 恢复），RAG_SOURCE_QUERY_TIMEOUT_SEC 历史缺失（本地补定义）
   - `rag/_retrieval_models.py`：RESULT_CACHE_SIZE 循环依赖（本地化 DEFAULT_MAX_SIZE）+ ENABLE_RESULT_CACHE 循环依赖（本地化 RESULT_CACHE_ENABLED）
   - `budget/budget_enforcer.py`：静态 shim 缺 re-export 导入（从 enforcer/cost_optimizer 补）
3. **缺 std import**（16 文件）：time×9、asyncio、uuid、Path、Any、timezone——批量修复；⚠ 自动插入脚本曾把 import 插进多行括号（8 个 provider 文件），已修复
4. **接口漂移**：main.py 用 dependencies 无参 `get_ring_log_buffer` 却带 base_dir 调用 → 改导入 utils/ring_buffer 版
5. **FastAPI 联合返回注解**（4 处）：gcode_generation×2 + cam_validation×2 的 `FileResponse|JSONResponse` → 加 `response_model=None`（AST 全量扫描）
6. **启动环境**：Redis 连接失败（.env REDIS_URL 指向 Docker 主机名）→ install.sh 生成 .env 时置空 REDIS_URL（redis_client 未配置自动降级内存缓存）
7. **CLI Windows 兼容**：is_running 用 tasklist（os.kill(pid,0) 在 Windows 不验证存在）；PID 文件删除容错
8. **剩余非启动链 pyflakes 问题 ~20 处**（cost_budget CostDimension、closed_loop_bench Artifact、mtconnect _local_attr_name、explainability 缺失异常类、plugin_system re-export 等）——不影响启动，列入阶段 4 清理
