# 灵境制造 — 二维转三维（2D→3D）实现代码

> 提取自 `engineering/python/app/dxf/` 与 `engineering/python/app/cad/`
> 核心链路：DXF 图纸 → 特征提取 → CadQuery 三维建模

---

## 一、文件地图

| 文件 | 职责 | 阶段 |
|---|---|---|
| `dxf/dxf_parser.py` | DXF 文件实体解析（直线/圆/弧/文本/标注/多段线） | 前置 |
| `dxf/feature_extractor.py` | 从 2D 图纸提取外形尺寸、孔中心/直径/深度 | 前置 |
| `dxf/polyline_outline.py` | 多段线 → 外轮廓/内孔识别 | 前置 |
| `dxf/dxf_to_model.py` | **★核心转换器**：CadQuery 布尔运算建模 | 核心 |
| `dxf/pipeline.py` | 端到端流水线编排（6 阶段） | 编排 |
| `cad/cadquery_gen.py` | 从参数/三视图生成 CadQuery 脚本建模 | 另一入口 |
| `cad/advanced_features.py` | 高级特征（倒角/圆角/台阶/槽） | 扩展 |
| `cad/_brep_validator.py` | 建模后 B-rep 拓扑校验 | 质量保障 |

### 数据流

```
DXF 文件
   │ DxfParser.parse()
   ▼
DxfParseResult ──▶ FeatureExtractor.extract()
                        │
                        ▼
              FeatureExtractionResult
                (overall_length/width/height, holes[]含center/diameter/depth)
                        │
        ┌───────────────┴───────────────┐
        ▼                               ▼
 DxfToModelConverter                 DxfToModelConverter
 .convert(feature_result)            .convert_from_polylines(polylines)
   │ box() 建基准立方体                 │ PolylineOutlineProcessor 识别外/内轮廓
   │ cylinder()+cut() 挖孔             │ lineTo()+close()+extrude() 拉伸成体
   ▼                                  ▼
 CadQuery Workplane ──▶ export_stl() / export_step()
        │
        ▼
 process_planning (工艺规划) ──▶ G代码
```

---

## 二、核心转换器 `dxf/dxf_to_model.py`

### 主入口 `convert()`

```python
def convert(self, feature_result, user_id=None, source_dxf=None):
    result = ModelConversionResult()
    length = feature_result.overall_length
    width  = feature_result.overall_width
    height = feature_result.overall_height

    if length <= 0 or width <= 0:
        result.errors.append("零件外形尺寸无效，无法创建3D模型")
        return result
    if height <= 0:
        height = 10.0
        result.warnings.append("零件高度无效，使用默认值10mm")

    base = self._create_base_solid(length, width, height)   # 建外形立方体

    hole_count = 0
    for hole in feature_result.holes:
        try:
            base = self._add_hole(base, hole, height)       # 布尔减挖孔
            hole_count += 1
        except (...) as e:
            result.warnings.append(...)                     # 单孔失败不阻塞整体

    result.workplane = base
    result.hole_count = hole_count
    return result
```

### 建外形 `_create_base_solid()`

```python
def _create_base_solid(self, length, width, height):
    # XY平面建 box，底部位于 Z=0，高度沿 Z 轴正向
    base = cq.Workplane("XY").box(length, width, height, centered=(True, True, False))
    return base
```

### 挖孔 `_add_hole()`

```python
def _add_hole(self, workplane, hole, base_height):
    hole_radius = hole.diameter / 2.0
    hole_depth = hole.depth
    if hole.hole_type == "through_hole" or hole_depth <= 0:
        hole_depth = base_height * 1.2            # 通孔穿透
    hole_depth = min(hole_depth, base_height * 1.5)

    hole_solid = (cq.Workplane("XY")
        .workplane(offset=-hole_depth * 0.1)
        .center(hole.center_x, hole.center_y)
        .circle(hole_radius)
        .extrude(hole_depth * 1.2))               # 圆柱体（略穿透）

    result = workplane.cut(hole_solid)            # ★布尔减
    return result
```

### 多段线轮廓建模 `convert_from_polylines()`

```python
def convert_from_polylines(self, polylines, height=10.0):
    processor = PolylineOutlineProcessor()
    outlines = processor.extract_outlines(polylines)
    outer = outlines[0]                           # 最大面积=外轮廓
    holes = [o for o in outlines[1:] if o.is_hole]

    # 1. 画外轮廓并拉伸成体
    wp = cq.Workplane("XY").moveTo(outer.vertices[0][0], outer.vertices[0][1])
    for v in outer.vertices[1:]:
        wp = wp.lineTo(v[0], v[1])
    wp = wp.close()
    base = wp.extrude(height)

    # 2. 挖掉内部孔
    for hole in holes:
        hwp = cq.Workplane("XY").moveTo(hole.vertices[0][0], hole.vertices[0][1])
        for v in hole.vertices[1:]:
            hwp = hwp.lineTo(v[0], v[1])
        hwp = hwp.close().extrude(height * 1.2)
        base = base.cut(hwp)                       # ★布尔减
    return result
```

### 导出

```python
def export_stl(self, conversion_result, output_path, tolerance=0.001):
    cq.exporters.export(conversion_result.workplane, str(path),
                        exportType="STL", tolerance=tolerance)

def export_step(self, conversion_result, output_path):
    cq.exporters.export(conversion_result.workplane, str(path), exportType="STEP")
```

---

## 三、轮廓处理器 `dxf/polyline_outline.py`

识别外轮廓与内孔（建模前置）：

```python
def extract_outlines(self, polylines):
    # 1. 收集闭合多段线
    closed = [outline for p in polylines if (outline := self._polyline_to_outline(p))
              and outline.is_closed and len(outline.vertices) >= 3]
    # 2. Shoelace 公式算面积，降序排列
    infos.sort(key=lambda x: x._area, reverse=True)
    # 3. 最大=外轮廓；顶点在外轮廓内=内孔
    outer = infos[0]
    for c in infos[1:]:
        if self._point_in_polygon(c.vertices[0], outer.vertices):   # 射线法
            c.is_hole = True
```

---

## 四、三视图生成 `cad/cadquery_gen.py`

另一条 2D→3D 路径：从**三视图图片**提取几何参数建模。

```python
# 1. 从三视图图片提取几何参数（CV 管道 + 头文件兜底）
params = await extract_geometry_params_from_views(views)

# 2. 生成 CadQuery 脚本
script = await generate_script_from_params(params)

# 3. 执行并导出，导出前 B-rep 拓扑校验
path = await execute_and_export(script, task_id, output_format)

# 4. 带高级特征 + 失败重生成（借鉴 Pointer-CAD 生成后校验闭环）
path, report, attempts = generate_3d_model_with_retry(params, features)
```

脚本安全性：`_CadQueryScriptValidator` AST 审计，拒绝危险 dunder 属性访问与 import，防沙箱逃逸。

---

## 五、端到端编排 `dxf/pipeline.py`

6 阶段流水线（3D 建模为 Stage 3，失败可降级继续工艺规划）：

```python
class DxfProcessPipeline:
    def run(self, file_path, material="45#钢", ...):
        # Stage 1: DXF解析  →  DxfParser
        # Stage 2: 特征提取 →  FeatureExtractor
        # Stage 3: 3D模型转换 → DxfToModelConverter  ★二维转三维
        # Stage 4: 数据组装 → 特征转 process_planning 格式
        # Stage 5: 工艺规划 + G代码生成 → ProcessPlanningPipeline
        # Stage 6: 结果验证
```
