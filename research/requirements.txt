# =============================================================================
# 灵境制造 - 科研侧 Python 依赖（research/）
# =============================================================================
# 说明：
#   阶段2 解耦后，训练栈依赖（torch / torchdiffeq / mlflow / optuna 等）集中
#   在本文件，仅科研环境安装。工程侧（engineering/python/requirements.txt）
#   通过 ONNX Runtime 消费训练好的模型，不再依赖 torch。
#
#   安装方式（科研环境独立 venv）：
#     cd research/
#     pip install -r requirements.txt
#
#   模型训练完成后，导出为 ONNX 格式投放至
#   engineering/python/app/simulation/chatter/checkpoints/model.onnx
#   供工程侧 ONNX Runtime 加载。
#
# 国内镜像加速：
#   pip config set global.index-url https://mirrors.aliyun.com/pypi/simple/
# =============================================================================

# Core Training Framework
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu          # LTC/CFC 训练主框架（若需 GPU：torch==2.5.1+cu121 + cu121 extra-index-url）
torchdiffeq==0.2.3        # 连续时间 ODE 求解器（DL-LNN LTCCell 实现 dopri5/rk4 积分，见 ACADEMIC_REVIEW_REPORT.md AR-03）
numpy==1.26.4
pandas==2.2.3             # 数据处理（PHM2010 / UNIWEAR 数据集分析）
scikit-learn==1.6.1       # 传统 ML 基线 + 数据预处理
xgboost==2.1.3            # 梯度提升树基线（benchmarks/xgboost_baseline 依赖）

# 实验追踪与超参搜索（学术诚信硬约束 D-2：复现性保护）
mlflow==2.16.0            # 实验追踪（训练超参/指标记录，严格固定版本保证可复现性）
optuna==4.1.0             # 超参搜索（experiments/optuna_search.py 依赖）
tensorboard==2.18.0       # 训练可视化

# 数据集与可视化
matplotlib==3.10.0        # 数据可视化
seaborn==0.13.2           # 统计可视化
h5py==3.12.1              # HDF5 文件处理（PHM2010 数据集依赖）

# ONNX 导出（训练完成后导出供工程侧 ONNX Runtime 消费）
onnx==1.17.0              # ONNX 模型导出
onnxruntime==1.20.1       # ONNX Runtime（导出时验证 + 与工程侧版本对齐）

# Jupyter（实验分析 notebook）
jupyter==1.1.1
ipykernel==6.29.5

# -----------------------------------------------------------------------------
# 说明：
#   1. shared/ 为零依赖契约层，无需在此声明依赖
#   2. 工程侧依赖见 engineering/python/requirements.txt
#   3. 版本号与 engineering/python/requirements.txt 严格对齐（numpy/pandas/sklearn 等）
#      避免双环境不一致导致导出的 ONNX 模型在工程侧加载失败
# -----------------------------------------------------------------------------
