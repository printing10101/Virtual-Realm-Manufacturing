# multimodal_jepa/ — JEPA 多模态探索

被 `research.multimodal_jepa.*` 绝对导入（4 处），**目录不可重命名/移动**。三个独立 JEPA 子项目：

| 子目录 | 内容 |
|---|---|
| `ijepa_3d/` | I-JEPA 3D 实现（含 chamfer_heuristic、inference_bridge） |
| `jepa_world_model/` | JEPA 世界模型 |
| `vjepa_machining/` | V-JEPA 加工场景适配 |
