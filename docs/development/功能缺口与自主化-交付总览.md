# 功能缺口填补 + 自主化路线图 —— 交付总览报告

**报告日期**: 2026-08-21  
**状态**: 🟡 全部代码骨架/测试/文档已落地；接线待环境恢复（见 §5）

---

## 1. 一句话总结

**工厂实用所需的三大功能缺口（实时监控/数据飞轮/机床方言扩展）的实现计划 + 全部代码骨架已交付，
路线图 9 个可纯新增任务（P1-2/P1-3/P2-1/2/3/P3-1/P3-2/P4-1/P4-2/P5-2）的代码+测试+设计文档已全部落地。
唯一剩余：文件锁解除后的「接线」+「门禁验证」（接线指南-v3-总集成.md 为唯一执行手册）。**

---

## 2. 累计交付全景（Round 1-16）

| 类别 | 数量 | 明细 |
|---|---|---|
| **设计文档** | 17 份 | 功能缺口总计划 / MTConnect 设计 / 数据飞轮 Schema / 参数优化设计 / 端对端验证 / 接线指南 v3（总集成）/ parametric 白盒化 / dxf 声明化 / FastAPI 路由声明化 / 方言生命周期 / ElementPlus Headless / Store-Router 封装（+补充）/ 参数优化补充 / 接线补充 v2 等 |
| **后端代码** | 20 文件 | MTConnect（streaming/conditions/bridge）+ 数据飞轮（契约/ORM/Repository/API）+ 参数优化（baseline/recommender/evaluator/API）+ 白盒（parametric 状态机/dxf 声明/路由声明/方言生命周期）+ 实时 WS 端点 |
| **方言镜像** | 4 文件 | Siemens 840D + Heidenhain TNC640（hooks + dialect.yaml） |
| **前端代码** | 15 文件 | 飞轮 API/Store/采集表单/监控面板 + 参数优化 API/推荐面板 + Headless useDataTable + CRUD Store 工厂 + Router 工厂 + 测试 |
| **脚本** | 1 份 | sovereignty_ratio.py（自主占比统计 + CI 门禁） |
| **测试** | 26 文件 ~356 用例 | 后端 12 文件（~280 用例）+ 前端 9 文件（~76 用例） |

## 3. 路线图进度

| 任务 | 状态 | 说明 |
|---|---|---|
| P1-1 feature_extraction 白盒化 | ✅ | 已推送 origin/main（commit 2e749c1） |
| P1-2 parametric 状态机白盒化 | 🟡 | 模块+测试完成，pipeline.py 委托待接线 |
| P1-3 dxf 六阶段声明化 | 🟡 | 模块+测试完成，pipeline.py 委托待接线 |
| P2-1/2/3 数据飞轮 | 🟡 | 契约/ORM/Repository/API 骨架完成，路由注册待接线 |
| P3-1 Element Plus Headless | 🟡 | useDataTable 落地，组件迁移待接线 |
| P3-2 Store/Router 封装 | 🟡 | defineCrudStore + createAppRouter 落地，index 接入待接线 |
| P4-1 FastAPI 路由声明化 | 🟡 | _route_registry 落地，engineering.py 委托待接线 |
| P4-2 方言完整生命周期 | 🟡 | _lifecycle 落地，registry.py 委托待接线 |
| P5-1 LNN CFC/LTC 改写 | ⬜ | 需 torch 环境（独立大项） |
| P5-2 自主占比统计 | 🟡 | sovereignty_ratio.py 落地，CI 接入待接线 |

## 4. 数据飞轮 + 参数优化完整闭环（代码骨架级）

```
MTConnect 采集（streaming/bridge）
    → cutting_experience 落库（契约/ORM/Repository）
    → 统计聚合（/experience/stats）
    → ParameterRecommender 推荐（L0→L1→物理钳制）
    → 前端面板展示（ParameterRecommendPanel）
    → 实测回流（/experience/capture + /optimizer/evaluate/compare）
    → A/B 对比 → 参数持续优化
```

## 5. 唯一阻塞：接线（需环境恢复）

**阻塞条件（17 轮连续确认）**：
1. pwsh 完全不可用（前台/后台均 exit 1 无输出）→ 无法运行 pytest/ruff/mypy/vitest
2. 已存在文件 write/edit 全部 EACCES (Win32 5) → 无法修改 __init__.py/engineering.py/删除垃圾文件
3. glob/grep 工具异常（read/write/edit 正常，不影响）

**恢复路径**：环境恢复后，按 **`docs/development/接线指南-v3-总集成.md`** 一次性执行：
① 删 4 垃圾文件 → ② __init__ 导出（3+4 处）→ ③ 路由注册（P4-1 声明表 + 前端 createAppRouter）→
④ 委托接线（parametric 3 处 + dxf 3 处 + registry 3 处）→ ⑤ bridge feed 修复 → ⑥ 全量门禁验证（~356 用例 + ruff + mypy + coverage + vitest + sovereignty）

## 6. 后续大项（环境恢复后）

| 事项 | 前置 |
|---|---|
| 接线 + 门禁验证 | 环境恢复（§5） |
| Phase F 端对端验证 | 真实 MTConnect 机床（≥50 数据记录） |
| P5-1 LNN CFC/LTC 改写 | torch 环境（research/ 侧） |
| 独立任务（不属本目标） | Ollama 12 文件提交 / .venv3/4/5 gitignore / torch 迁 research/ / ruff format / StepModelViewer 拆分 / 远程市场 |
