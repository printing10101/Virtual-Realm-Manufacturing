# 功能缺口代码接线指南（锁解除后执行手册）

**创建日期**: 2026-08-20  
**目的**: 2026-08-20 功能缺口会话因环境限制（pwsh 不可用 + 已存在文件 EACCES），
代码骨架以「纯新增文件」落地但未接线。本指南供文件锁解除后的会话一次性完成接线与验证。

---

## ⚠️ 背景

- 会话期间 **仅 CreateFileW（新文件）可用**；write/edit 已存在文件全部 EACCES (Win32 5)
- pwsh 完全不可用 → 无法运行 pytest/ruff/mypy/vitest
- 所有新增代码已用「直接子模块导入」避免依赖 `__init__.py` 导出 → 当前可独立编译，
  但未接入包导出/路由注册，测试未运行验证

---

## 🔧 步骤 1：删除垃圾/错误文件（4 个）

| 文件 | 问题 | 处置 |
|---|---|---|
| `engineering/python/app/api/v1/experience.py` | 错误导入 `from app.api.v1.deps import require_permission`（deps.py 不存在） | **删除**（正确版本是 experience_routes.py） |
| `engineering/python/app/services/domain/cutting_experience_service.py` | 错误导入 `from app.database.models import CuttingExperienceRecord`（__init__ 未导出） | **删除**（正确版本是 cutting_experience_repository.py） |
| `engineering/python/tests/unit/_write_probe_test.py` | 探针文件 | **删除** |
| `engineering/python/tests/unit/_edit_probe.py` | 探针文件 | **删除** |

## 🔧 步骤 2：包导出接线（3 个 __init__.py）

### 2.1 `engineering/python/app/integrations/mtconnect/__init__.py`
在既有导出后追加：
```python
from app.integrations.mtconnect.streaming import (
    MTConnectStreamServer, StreamEvent, AlertEvent, StreamConsumer,
)
from app.integrations.mtconnect.conditions import (
    ConditionChecker, ChatterDetector, Alert, AlertCondition,
    AlertPriority, AlertType,
)
from app.integrations.mtconnect.experience_bridge import MTConnectExperienceBridge
```
并把新名字加入 `__all__`。

### 2.2 `engineering/python/app/database/models/__init__.py`
追加：
```python
from app.database.models.cutting_experience import (
    CuttingExperienceRecord,
)
```
并加入 `__all__`。

### 2.3 `engineering/python/app/contracts/__init__.py`
追加：
```python
from app.contracts.cutting_experience import (
    CoolantMode, CuttingExperience, CuttingParameters, CuttingResults,
    ExperienceQuery, ExperienceStats, MachiningAnomaly, MachiningResult,
    MachiningType,
)
```
并加入 `__all__`。

## 🔧 步骤 3：路由注册

`engineering/python/app/api/routers/engineering.py`：
```python
from app.api.v1 import experience_routes
...
# === 切削体验采集（数据飞轮 P2-3）===
app.include_router(experience_routes.router)
```

## 🔧 步骤 4：已知待修（bridge feed 换算）

`engineering/python/app/integrations/mtconnect/experience_bridge.py`：
`feed_mm_per_rev = (sample.feedrate or 0.0) / spindle_rpm`（当前是 `/1000.0`，
物理上 mm/min ÷ rpm 才是 mm/rev）。同步更新
`engineering/python/tests/unit/test_mtconnect_experience_bridge.py` 断言：
`test_valid_sample_converts` 中 `feed_mm_per_rev == 0.5` → `500/8000 = 0.0625`。

## 🔧 步骤 5：全量验证（质量铁律）

```bash
# 后端（系统 Python 3.11）
set PYTHONUTF8=1
py -3.11 -m pytest engineering/python/tests/unit/test_mtconnect_monitoring.py engineering/python/tests/unit/test_mtconnect_experience_bridge.py engineering/python/tests/unit/test_cutting_experience_contract.py engineering/python/tests/unit/test_cutting_experience_repository.py engineering/python/tests/unit/test_dialect_declared_hooks_phaseE.py --no-header -q -o addopts=''
# ruff（CI 等价）
ruff check app/
# mypy
py -3.11 -m mypy --config-file mypy.ini app/integrations/mtconnect app/contracts/cutting_experience.py app/database/models/cutting_experience.py app/services/domain/cutting_experience_repository.py app/api/v1/experience_routes.py

# 前端
cd engineering && npx vitest run src/api/__tests__/cuttingExperience.test.ts src/stores/__tests__/experienceStore.test.ts
```

门禁：ruff 0 违规 / mypy 0 错误（排除既有 torch 噪声）/ 行覆盖≥90% / 分支≥80%。

## 🔧 步骤 6：Phase D/F 后续（另起会话）

- Phase D 参数优化闭环：按 `docs/development/参数优化-AI 引擎设计.md` 实施
- Phase F 端对端验证：按 `docs/development/端对端验证计划.md` 实施

---

## 📌 新增文件清单（本会话交付，供验收核对）

### 文档（5）
1. `docs/development/功能缺口实现总计划.md`
2. `docs/development/MTConnect-实时监控集成设计.md`
3. `docs/development/数据飞轮-Schema 设计.md`
4. `docs/development/参数优化-AI 引擎设计.md`
5. `docs/development/端对端验证计划.md`

### 后端（7）
6. `app/integrations/mtconnect/streaming.py`
7. `app/integrations/mtconnect/conditions.py`
8. `app/integrations/mtconnect/experience_bridge.py`
9. `app/contracts/cutting_experience.py`
10. `app/database/models/cutting_experience.py`
11. `app/services/domain/cutting_experience_repository.py`
12. `app/api/v1/experience_routes.py`

### 方言（4）
13. `postprocessor-plugins/siemens_840d/hooks.py`
14. `postprocessor-plugins/siemens_840d/dialect.yaml`
15. `postprocessor-plugins/heidenhain_tnc640/hooks.py`
16. `postprocessor-plugins/heidenhain_tnc640/dialect.yaml`

### 前端（4）
17. `src/api/cuttingExperience.ts`
18. `src/stores/experienceStore.ts`
19. `src/components/experience/ExperienceCapture.vue`
20. `src/components/realtime/MachineMonitor.vue`

### 测试（7）
21. `tests/unit/test_mtconnect_monitoring.py`
22. `tests/unit/test_mtconnect_experience_bridge.py`
23. `tests/unit/test_cutting_experience_contract.py`
24. `tests/unit/test_cutting_experience_repository.py`
25. `tests/unit/test_dialect_declared_hooks_phaseE.py`
26. `src/api/__tests__/cuttingExperience.test.ts`
27. `src/stores/__tests__/experienceStore.test.ts`
