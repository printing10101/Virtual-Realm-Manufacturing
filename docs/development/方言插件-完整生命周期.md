# P4-2 方言插件完整生命周期

**创建日期**: 2026-08-21  
**状态**: 🟡 生命周期状态机白盒模块 + 测试落地；registry.py 委托接线待文件锁解除

---

## 🎯 目标

补全方言插件的**完整生命周期语义**。现有 `DialectRegistry` 只有
discover → compile → register 单向流程，缺卸载/重建/状态查询。
P4-2 提供白盒生命周期状态机，后续可被 registry.py 委托。

## 📦 已交付

### 白盒模块
`app/postprocessor/dialect/_lifecycle.py`（纯 stdlib，不 import app 任何模块）：

| 声明/函数 | 说明 |
|---|---|
| `DialectLifecycleStage` | 生命周期阶段枚举（discovered/compiled/registered/unregistered/failed） |
| `LIFECYCLE_TRANSITIONS` | 转移规则表（含错误建议） |
| `can_transition(cur, tgt)` | 转移合法性判定 |
| `assert_transition_allowed` | 断言式校验（非法抛 ValueError） |
| `next_stage_after_success(cur, op)` | 操作成功后的下一阶段 |
| `next_stage_after_failure(cur)` | 操作失败 → failed |
| `can_discover(cur)` | 是否可重新发现（failed/unregistered 可恢复） |
| `is_terminal(cur)` | 终态判定（registered） |

状态转移图：
```
DISCOVERED → COMPILED → REGISTERED → UNREGISTERED → (可重新 REGISTERED)
    ↘ FAILED → DISCOVERED（恢复）
COMPILED ↘ FAILED
REGISTERED ↘ FAILED
```

### 测试
`engineering/python/tests/unit/test_dialect_lifecycle.py`（~22 用例）：
正向流转 / 非法跳转 / 失败路径 / 恢复 / 字符串输入 / 规则覆盖 / 操作映射 / 帮助函数。

## 🔧 待接线（文件锁解除后）

### registry.py 委托（3 处）
```python
# app/postprocessor/dialect/registry.py
from app.postprocessor.dialect._lifecycle import (
    DialectLifecycleStage, next_stage_after_success, next_stage_after_failure,
)

# 1. DialectRegistry 增加生命周期跟踪
#    self._stage: dict[str, DialectLifecycleStage] = {}
#    discover() 成功后：self._stage[id] = next_stage_after_success(cur, "discover")

# 2. compile_all() 成功后：next_stage_after_success(cur, "compile")
#    失败路径：next_stage_after_failure(cur)

# 3. register_to() 成功后：next_stage_after_success(cur, "register")
```

### 新增能力（供 API/前端使用）
```python
def unregister(self, dialect_id: str) -> bool:
    """卸载方言（从 PostProcessorRegistry 移除）。"""
    # 白盒判定：can_transition(REGISTERED, UNREGISTERED)

def lifecycle_status(self, dialect_id: str) -> str:
    """查询方言生命周期状态。"""
```

## ✅ 验收标准（门禁）

1. ruff check app/postprocessor/dialect/ 全绿
2. mypy 0 错误
3. `_lifecycle.py` 行覆盖 **100%**
4. 既有方言测试（委托后行为不变）全绿
5. 状态枚举与生命周期语义对齐（测试锁定）

## 📝 变更日志

### v1.0 (2026-08-21)
- 白盒生命周期状态机 `_lifecycle.py` 落地
- 测试 `test_dialect_lifecycle.py` 落地（~22 用例）
- 待办：registry.py 委托 + unregister/lifecycle_status API（文件锁解除后）
