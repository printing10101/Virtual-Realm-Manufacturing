# 接线补充说明 v2（Round 7-10 新增文件）

**创建日期**: 2026-08-21  
**关联**: `docs/development/功能缺口-接线指南.md`（主接线手册，本文为其补充）

---

## 📦 本轮新增文件（Round 7-10）

### P1-2 / P1-3 白盒模块
| 文件 | 说明 |
|---|---|
| `app/parametric_geometry/_review_state_machine.py` | 审核状态机（详见 parametric_geometry-白盒化.md） |
| `app/dxf/_pipeline_stages.py` | 六阶段编排声明（详见 dxf-pipeline-六阶段声明化.md） |

### P5-2 脚本
| 文件 | 说明 |
|---|---|
| `scripts/sovereignty_ratio.py` | 自主占比统计（--json/--target 支持 CI 门禁） |

### Phase A 实时通道（本轮）
| 文件 | 说明 |
|---|---|
| `app/api/v1/monitor_ws.py` | WebSocket 实时监控端点（/monitor/ws，demo 降级 + 心跳 + 权限） |
| `tests/api/test_monitor_ws.py` | WS 测试（5 用例） |

---

## 🔧 接线补充

### 1. monitor_ws 路由注册（并入主指南步骤 3）
```python
# engineering/python/app/api/routers/engineering.py
from app.api.v1 import monitor_ws
...
# === 实时监控 WebSocket（Phase A）===
app.include_router(monitor_ws.router)
```

### 2. sovereignty_ratio 接入 CI（可选，主指南步骤 5 追加）
```yaml
# .github/workflows/ci.yml 追加 job
- name: Sovereignty ratio gate
  run: python scripts/sovereignty_ratio.py --target 0.50
```

### 3. 验证命令追加
```bash
py -3.11 -m pytest engineering/python/tests/unit/test_parametric_review_state_machine.py engineering/python/tests/unit/test_dxf_pipeline_stages.py engineering/python/tests/unit/test_sovereignty_ratio.py engineering/python/tests/api/test_monitor_ws.py --no-header -q -o addopts=''
```

---

## 📌 累计交付（Round 1-10 全量）

- **文档 10 份**（总计划/MTConnect/数据飞轮/参数优化/端对端/接线指南/参数优化补充/parametric 白盒化/dxf 声明化/本补充说明）
- **后端 17 文件**（MTConnect 3 + 飞轮 4 + optimizer 4 + parametric 状态机 + dxf 声明 + monitor_ws + 2 路由）
- **方言 4 文件**（Siemens 840D + Heidenhain TNC640）
- **前端 9 文件**（API 2 + store 1 + 组件 4 + 测试 2）
- **脚本 1 份**（sovereignty_ratio.py）
- **测试 21 文件**（~283 用例）

### 待接线清单（不变）
① 删 4 垃圾文件（experience.py/cutting_experience_service.py/_write_probe_test.py/_edit_probe.py）
② 3 个 __init__ 导出（mtconnect/database.models/contracts）
③ 路由注册 experience_routes + optimizer_routes + monitor_ws
④ bridge feed 修复（feedrate÷rpm）+ 测试断言同步
⑤ pipeline.py 委托（parametric 3 处 + dxf 3 处）
⑥ 全量门禁验证（pytest + ruff + mypy + vitest + sovereignty 门禁）
