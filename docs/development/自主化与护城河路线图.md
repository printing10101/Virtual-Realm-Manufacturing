# 灵境制造 · 自主化与护城河路线图（AI 执行手册）

> **本文档是给 AI agent（DeepSeek Harness）的执行手册，不是战略 PPT。**
> 每个任务都有：入口路径 / 具体动作 / **生产级验收标准（硬门禁）** / 记忆指针。
> **验收标准 = 顶尖生产级**：任何任务未达 §4 门禁即视为未完成，禁止标记 ✅。
> AI 每次会话开始时，先读 `.dsh-memory/MEMORY.md` 中的「路线图进度」段落，接着未完成的任务干。

---

## 0. 总体目标与原则

| 项 | 内容 |
|----|------|
| **最终目标** | 自主代码占比 20% → 50% + 建立「工艺数据飞轮 + 插件生态」护城河 |
| **总工期** | 10 个月（4 个阶段） |
| **核心原则** | 只重写「业务逻辑白盒层」，保留 CadQuery/OpenCASCADE/PyTorch 等框架调用 |
| **护城河定位** | 「基于实测加工数据驱动的智能制造工艺平台」——数据 + 算法 + 生态，**不是代码全自研** |
| **质量红线** | **生产级标准**：每个交付物必须达到可直接投产的质量（§4 门禁全过） |
| **专利** | 暂不申报（用户决定），但每个自主化任务必须沉淀技术文档，方便未来申报 |
| **禁止事项** | 不重写数据库引擎 / 不替换几何内核 / 不替换 PyTorch |

---

## 1. AI 工作流约定（每次会话必须遵守）

```
启动会话：
  1. 读 .dsh-memory/MEMORY.md → 找「路线图进度」段 → 找 NEXT_TASK
  2. 读本文档对应任务段落（含 §4 门禁）
  3. 用 todo_write 建立本次会话任务清单（含验收项）
  4. 执行代码任务（保留框架调用）
  5. 逐项过 §4 生产级门禁（测试/覆盖率/性能/安全/文档）
  6. 全部门禁通过 → 更新进度表（✅）+ 更新验收记录
     任一门禁未过 → 状态保持 ⬜，记录缺口，不提交完成
  7. memory_log 记录完成内容 + 门禁证据 + 更新 NEXT_TASK
  8. 按提交规范 commit（type: refactor/feat/fix，scope 见任务）
```

**提交规范**：`refactor(python): <模块> <动作>` 或 `feat(data): ...`，header ≤100 字符，commitlint 门禁。

**测试环境**（记忆已确认）：
- 系统 Python 3.11：`py -3.11`
- 必须 `PYTHONUTF8=1` + `-o addopts=''`（pytest-cov 未装）
- torch/scipy 环境残缺：与 torch 相关的测试失败为既有问题，非改动引入（用 git stash 验证基线）

---

## 2. 阶段总览（依赖顺序）

```
Phase 0  [✅ 已完成] 依赖架构分析 + 护城河盘点 + 本路线图
   ↓
Phase 1  [1-2 月] 算法白盒化：feature_extraction / parametric_geometry / dxf pipeline
   ↓       目标：自主占比 +13%
   ↓
Phase 2  [2-3 月] 数据飞轮地基：自有数据 Schema + 存储抽象层 + 采集 API
   ↓       目标：护城河数据闭环启动
   ↓
Phase 3  [3-4 月] 前端自主化：Element Plus → Headless UI 包装 + Store/Router 自主实现
   ↓       目标：自主占比 +15%
   ↓
Phase 4  [2-3 月] 后端框架包装：FastAPI 路由声明化 + 方言插件完整接线
   ↓       目标：自主占比 +10%
   ↓
Phase 5  [1-2 月] AI 模型自主化：LNN 结构改写（CFC/LTC）+ 最终占比验证
   ↓       目标：自主占比 +12%，总达 50%
```

**依赖规则**：
- Phase 2 依赖 Phase 1（数据 Schema 要基于白盒化后的模块边界）
- Phase 3/4 可并行（前后端独立）
- Phase 5 依赖 Phase 1（模型结构改写先有特征提取白盒）

---

## 3. 生产级验收门禁体系（★所有任务必须全过★）

> 这是全局硬性标准，任务清单里的「验收」是此体系的落地。**任何一项不达标 = 任务未完成。**

### 3.1 代码质量门禁

| # | 门禁 | 标准 | 验证命令 |
|---|------|------|----------|
| Q1 | 静态检查 | `ruff check app/` 全规则（CI 等价）**0 违规** | `ruff check app/` |
| Q2 | 类型安全 | mypy 严格模式 **0 错误**（既有 torch 噪声除外，须列清单说明） | `mypy --config-file mypy.ini <改动文件>` |
| Q3 | 复杂度 | 新增函数圈复杂度 ≤ 10（mccabe）；超标必须拆分 | `ruff check --select C901` |
| Q4 | 代码规模 | 新文件 ≤ 500 行；函数 ≤ 60 行；无超过 3 层的嵌套 | 人工/脚本 |
| Q5 | 无危险模式 | 无 `shell=True` / 无 SQL 字符串拼接 / 无 `eval`/`exec`（受控沙箱除外） | 安全扫描脚本 |

### 3.2 测试门禁（生产级覆盖率）

| # | 门禁 | 标准 | 验证方式 |
|---|------|------|----------|
| T1 | 行覆盖率 | 新增/修改代码 **≥ 90%** | pytest-cov |
| T2 | 分支覆盖率 | **≥ 80%**（条件分支全覆盖） | pytest-cov --branch |
| T3 | 边界用例 | 每个公共函数覆盖：空输入 / 极值 / None / 负数 / 重复值 / 类型错误 | 测试清单核对 |
| T4 | 错误路径 | 每个 except 分支 ≥1 个触发用例；异常类型 + 错误码断言 | 测试清单核对 |
| T5 | 回归基线 | 相关既有测试**全绿**；黄金/契约测试逐字符/逐字段比对零差异 | pytest |
| T6 | 幂等性 | 相同输入重复执行 → 相同输出（状态机/IO 任务必须） | 重复执行断言 |

### 3.3 性能门禁（可量化基准）

| # | 门禁 | 标准 | 验证方式 |
|---|------|------|----------|
| P1 | 基准回归 | 核心路径有性能基准（pytest-benchmark），改动后 **≥ 改动前 95%** | pytest-benchmark |
| P2 | 复杂度上界 | 10 倍输入规模 → 耗时增长 < 10 倍（无 O(n²)+ 无 N+1 查询） | 规模测试 |
| P3 | 内存上界 | 100MB 级输入峰值内存有明确上限（记录在文档） | tracemalloc/监控 |
| P4 | 无泄漏 | 连续 1000 次调用无内存增长、无句柄泄漏 | 循环测试 |

### 3.4 安全门禁

| # | 门禁 | 标准 |
|---|------|------|
| S1 | 输入校验 | 所有外部输入经 Pydantic/模式校验；非法输入返回 4xx + 错误码 |
| S2 | 路径安全 | 所有文件路径操作有白名单/防目录穿越（既有方言 API 模式） |
| S3 | 权限 | 每个 API 端点有权限声明 + 未授权返回 403 的测试 |
| S4 | 审计 | 所有写操作（创建/修改/删除）有审计日志（who/what/when） |
| S5 | 敏感数据 | 日志/异常不泄露密钥、路径、用户数据（log_sanitizer 覆盖） |

### 3.5 可靠性门禁

| # | 门禁 | 标准 |
|---|------|------|
| R1 | 优雅降级 | 外部依赖（DB/Redis/模型）不可用时降级而非崩溃；有降级测试 |
| R2 | 失败恢复 | 长任务中断后可从检查点恢复（涉及状态机/流水线的任务） |
| R3 | 超时取消 | 长任务支持超时/取消，资源正确释放（有测试） |
| R4 | 并发安全 | 共享状态有锁/原子操作；并发测试 ≥2 线程/任务（涉及共享状态的任务） |

### 3.6 可观测性与文档门禁

| # | 门禁 | 标准 |
|---|------|------|
| O1 | 结构化日志 | 关键路径有结构化日志（含上下文/耗时） |
| O2 | 错误码 | 异常有唯一错误码，前端可映射 |
| O3 | API 文档同步 | API 变更同步 docs/api-reference.md（CI check_api_docs_sync 过） |
| O4 | 技术文档 | 每任务产出设计文档（docs/development/），含：动机/架构/边界/复杂度分析 |
| O5 | 覆盖率报告 | 每任务产出覆盖率报告（数字 + 缺口说明） |

### 3.7 端到端门禁（阶段收尾必做）

| # | 门禁 | 标准 |
|---|------|------|
| E1 | 冒烟测试 | 既有冒烟用例（11 项）全过 |
| E2 | 全量回归 | 前端 1891 测试全绿 + vite build；后端 unit 全量无新增失败（环境问题除外并说明） |
| E3 | 真实数据验证 | 用真实 DXF/STEP/NC 样本验证（tests/golden/ 已有基线复用） |

---

## 4. 任务清单（每个任务 = 一次或多次会话单元）

### ✅ Phase 0（已完成）

| ID | 任务 | 状态 |
|----|------|------|
| P0-1 | 依赖架构分析（Layer 1-4 分层） | ✅ 2026-08-20 |
| P0-2 | 护城河资产盘点（A 类算法 / B 类数据） | ✅ 2026-08-20 |
| P0-3 | 本路线图创建（含生产级门禁体系 §3） | ✅ 2026-08-20 |

---

### 🔧 Phase 1：算法白盒化（1-2 个月，目标 +13%）

#### P1-1 `feature_extraction/` RANSAC 两步特征提取白盒化

- **入口**：`engineering/python/app/feature_extraction/`（2284 行）
- **目标**：把 RANSAC 平面-圆环两步拟合 + 审核状态机从「调用式」改为「自有算法逻辑」
- **具体动作**：
  1. 通读 `pipeline.py` / `plane_extractor.py` / `cylinder_extractor.py`，识别哪些是 CadQuery/Open3D 调用、哪些是自有逻辑
  2. 把特征判定逻辑（凸台/凹陷/孔的分类规则）抽成纯 Python 自有模块 `_feature_classifier.py`
  3. 把审核状态机抽成 `FeatureReviewStateMachine`（无框架依赖）
  4. 保留 RANSAC 拟合本身对 numpy/sklearn 的调用（算法内核可保留，包装逻辑自主化）
- **验收（生产级）**：
  - [ ] Q1-Q5 全过（ruff 0 违规 / mypy 0 错误 / C901 ≤10 / 无危险模式）
  - [ ] T1-T6 全过：行覆盖 ≥90%、分支 ≥80%；新增测试 ≥20 用例（含：无特征输入、单特征、多特征重叠、退化几何、极值坐标、None 输入、异常中间态）
  - [ ] 审核状态机状态流转测试：全状态可达、非法转移拒绝、幂等重入
  - [ ] P1-P4 全过：特征提取有性能基准（1000 点/10000 点/100000 点三档）；10 倍输入 <10 倍耗时；连续 1000 次无泄漏
  - [ ] S1-S5 全过：输入坐标范围校验、无路径操作、写操作审计
  - [ ] R1-R4 全过：模型加载失败降级、状态机中断恢复
  - [ ] O1-O5 全过：设计文档 `docs/development/feature_extraction-白盒化.md`（含算法复杂度分析）
  - [ ] E1-E3 全过（阶段内）
- **提交**：`refactor(python): feature_extraction RANSAC 判定逻辑白盒化`

#### P1-2 `parametric_geometry/` STEP 生成两轮审核白盒化

- **入口**：`engineering/python/app/parametric_geometry/`（2281 行）
- **目标**：两轮审核状态机（工程师确认 mesh→STEP 转换）从 CadQuery 绑定逻辑中解耦
- **具体动作**：
  1. 定位两轮审核状态流转代码（pending→first_review→second_review→approved）
  2. 抽成 `StepReviewWorkflow`（纯 Python 状态机 + 事件驱动）
  3. STEP 文件生成保留 CadQuery 调用，但审核记录/回滚/版本对比逻辑自有化
- **验收（生产级）**：
  - [ ] Q1-Q5 全过
  - [ ] T1-T6 全过：行覆盖 ≥90%、分支 ≥80%；新增状态机测试 ≥15 用例（非法转移/越权操作/并发提交/幂等）
  - [ ] 审核流转测试：一票否决路径、两轮意见冲突处理、撤回重审、超时自动升级
  - [ ] P1-P4 全过：审核状态查询性能基准；版本对比在 100 版本规模下 <1s
  - [ ] S1-S5 全过：审核操作权限校验、审计日志（谁/何时/为何）
  - [ ] R1-R4 全过：审核中间态崩溃恢复、并发审核锁
  - [ ] O1-O5 全过：设计文档 `docs/development/parametric_geometry-白盒化.md`
  - [ ] E1-E3 全过（阶段内）
- **提交**：`refactor(python): parametric_geometry 两轮审核状态机白盒化`

#### P1-3 `dxf/pipeline.py` 六阶段编排白盒化

- **入口**：`engineering/python/app/dxf/pipeline.py`（六阶段流水线）
- **目标**：DXF→STEP→G-code 的六阶段编排（阶段定义/依赖/错误处理）自主化
- **具体动作**：
  1. 把硬编码的阶段顺序抽成声明式 `StageDefinition` 列表（阶段名/输入/输出/校验器）
  2. 编排器 `PipelineOrchestrator` 自有实现（阶段注册/拓扑/跳过/降级策略）
  3. 保留每阶段内部的 CadQuery/ezdxf 调用
- **验收（生产级）**：
  - [ ] Q1-Q5 全过
  - [ ] T1-T6 全过：行覆盖 ≥90%、分支 ≥80%；新增编排器测试 ≥15 用例（阶段跳过/失败降级/循环依赖检测/空流水线/重复注册）
  - [ ] 编排语义测试：依赖拓扑正确、单阶段失败不影响后续独立阶段、降级策略生效
  - [ ] P1-P4 全过：流水线端到端性能基准（小/中/大 DXF 三档）；无 N+1
  - [ ] S1-S5 全过：输入文件类型/大小校验、路径白名单、审计
  - [ ] R1-R4 全过：阶段崩溃恢复、可重入、幂等
  - [ ] O1-O5 全过：设计文档 `docs/development/dxf-pipeline-声明化.md`
  - [ ] E1-E3 全过（阶段内）
- **提交**：`refactor(python): dxf 六阶段编排声明化`

---

### 🔧 Phase 2：数据飞轮地基（2-3 个月）

#### P2-1 自有数据 Schema 设计（`cutting_experience`）

- **入口**：`engineering/python/app/data/cutting_parameters.json`（现状 158 条）+ `app/database/models/`
- **目标**：设计「实测加工经验数据」Schema，替代/扩展公开手册参数库
- **具体动作**：
  1. 设计 `cutting_experience` 表：material / tool / machine / parameters / result / chatter_feedback / source / timestamp
  2. SQLAlchemy 模型 + Alembic 迁移（SQLite/PostgreSQL 双兼容）
  3. 保留既有 `cutting_parameters.json` 作为种子数据源
- **验收（生产级）**：
  - [ ] Q1-Q5 全过
  - [ ] T1-T6 全过：模型 CRUD 全路径 + 约束（唯一性/外键/非空/级联删除）测试 ≥20 用例
  - [ ] 数据完整性：迁移脚本幂等（重复执行无副作用）；回滚脚本可用
  - [ ] P1-P4：100 万条量级查询性能基准（索引生效验证）
  - [ ] S1-S5：数据校验（材料名白名单、参数范围）、写入审计
  - [ ] R1-R4：事务回滚、并发写入
  - [ ] O1-O5：设计文档 `docs/development/data-schema.md`（含 ER 图 + 字段字典）
  - [ ] E1-E3（阶段内）
- **提交**：`feat(data): cutting_experience 实测数据模型`

#### P2-2 存储抽象层（StorageEngine）

- **入口**：`engineering/python/app/database/`
- **目标**：封装一个存储抽象层，业务代码不直接触碰 SQLAlchemy/Redis 细节
- **具体动作**：
  1. `StorageEngine` Protocol：get/put/query/delete + 事务
  2. SQLAlchemy 实现 + 内存实现（测试用）
  3. cutting_experience 读写走抽象层
- **验收（生产级）**：
  - [ ] Q1-Q5 全过
  - [ ] T1-T6 全过：抽象层行为一致性测试（内存实现 vs SQL 实现输出一致）≥20 用例
  - [ ] 契约测试：两实现逐操作比对（golden 模式）
  - [ ] P1-P4：读写性能基准；连接池不泄漏
  - [ ] S1-S5：SQL 注入防护验证（恶意输入测试）、审计
  - [ ] R1-R4：事务原子性（中途失败回滚）、断连重连
  - [ ] O1-O5：设计文档 `docs/development/storage-engine.md`
  - [ ] E1-E3（阶段内）
- **提交**：`feat(data): 存储抽象层`

#### P2-3 数据采集 API（实加工反馈入口）

- **入口**：`engineering/python/app/api/v1/`
- **目标**：提供「加工结果反馈」采集端点（工程师录入实际参数 + 结果）
- **具体动作**：
  1. POST /api/v1/cutting-experience（写入实测数据）
  2. GET 列表/统计（供数据看板）
  3. 前端采集表单（可选，可后置）
- **验收（生产级）**：
  - [ ] Q1-Q5 全过
  - [ ] T1-T6 全过：API 测试 ≥15 用例（成功/校验失败/无权限/并发/幂等）
  - [ ] API 契约测试：请求/响应 schema 冻结（防漂移）
  - [ ] P1-P4：P95 延迟基准；批量写入性能
  - [ ] S1-S5：权限（engineer:write）、输入校验、审计、限流
  - [ ] R1-R4：DB 不可用降级返回明确错误、幂等键防重复提交
  - [ ] O1-O5：API 文档同步（check_api_docs_sync 过）
  - [ ] E1-E3（阶段内）
- **提交**：`feat(api): cutting_experience 采集端点`

---

### 🔧 Phase 3：前端自主化（3-4 个月，目标 +15%）

#### P3-1 Element Plus → Headless UI 包装

- **入口**：`engineering/src/`（Vue3 + Element Plus，~80% 开源）
- **目标**：把 Element Plus 组件调用封装进自有组件层（`src/components/headless/`），业务代码不再直接 import Element Plus
- **具体动作**：
  1. 建 `src/components/ui/` 自有组件包装层（Button/Input/Table/Dialog 等高频组件）
  2. 业务视图逐步改 import 到包装层
  3. 保留 Element Plus 作为底层实现（可替换性）
- **验收（生产级）**：
  - [ ] 前端门禁：vue-tsc 0 错误 / eslint 0 error（warning 须说明）/ vite build 成功
  - [ ] 全量回归：1891 测试全绿 + 新增包装层测试 ≥30 用例（props 透传/事件/插槽/样式隔离/无障碍 aria）
  - [ ] 行为一致性：包装组件 vs 原生 Element Plus 逐属性比对测试（golden 模式）
  - [ ] 无回归：业务视图无直接 `el-` 标签（grep 全库验证）
  - [ ] 可替换性：仅改包装层内部即可换底层库（架构验证：无业务代码直接依赖 Element Plus import）
  - [ ] 可观测性：组件关键交互有埋点/日志
  - [ ] E1-E3（阶段内）
- **提交**：`refactor(web): Element Plus 包装层`

#### P3-2 Vue3 Store/Router 自主实现

- **入口**：`engineering/src/stores/`（20 个 store）+ `src/router/`
- **目标**：把 Pinia/Vue Router 调用封装进自有层（可替换性）
- **具体动作**：
  1. 建 `src/core/store.ts` 轻量状态容器（或封装层）
  2. 建 `src/core/router.ts` 路由封装
  3. 业务逐步迁移（先试点 2-3 个 store）
- **验收（生产级）**：
  - [ ] 前端门禁：vue-tsc / eslint / vite build 全过
  - [ ] 试点 store 迁移后行为一致性：原测试全绿（零行为差异）
  - [ ] 新增封装层测试 ≥20 用例（状态订阅/异步 action/错误处理/组件卸载清理）
  - [ ] 内存泄漏：store 销毁后监听器清零（测试断言）
  - [ ] 可替换性：业务不直接 import Pinia/Vue Router（grep 验证）
  - [ ] E1-E3（阶段内）
- **提交**：`refactor(web): store/router 封装层`

---

### 🔧 Phase 4：后端框架包装 + 插件接线（2-3 个月，目标 +10%）

#### P4-1 FastAPI 路由声明化包装

- **入口**：`engineering/python/app/api/v1/`（127 文件）
- **目标**：把 FastAPI 路由装饰器封装进自有 `@route` 声明层（业务不直接碰 FastAPI）
- **具体动作**：
  1. 建 `app/api/decorators.py`：自有 `@api_route(method, path, permission)` 声明
  2. 建 `app/api/router.py`：声明收集 + 转 FastAPI
  3. 试点迁移 5-10 个路由，验证行为一致
- **验收（生产级）**：
  - [ ] Q1-Q5 全过
  - [ ] T1-T6：声明层测试 ≥15 用例（方法/路径/权限/参数绑定/响应模型）
  - [ ] 行为一致性：迁移后路由的 OpenAPI schema 与迁移前逐字段比对（契约测试）
  - [ ] API 文档同步（check_api_docs_sync 过）
  - [ ] S1-S5：权限声明强制（漏声明路由启动即报错）、审计
  - [ ] P1-P4：路由注册性能（100 路由 <50ms）、无泄漏
  - [ ] E1-E3（阶段内）
- **提交**：`refactor(api): 路由声明化包装`

#### P4-2 方言插件完整接线

- **入口**：`engineering/python/plugins/` + `app/postprocessor/dialect/`
- **目标**：方言插件以 IPlugin 形式纳入统一插件系统（当前仅市场显示，未完整生命周期）
- **具体动作**：
  1. dialect 插件实现 IPlugin 接口（load/enable/disable）
  2. init_plugin_system 配置 plugin_dirs 指向 postprocessor-plugins/
  3. 前端插件市场真实显示安装状态
- **验收（生产级）**：
  - [ ] Q1-Q5 全过
  - [ ] T1-T6：插件生命周期测试 ≥15 用例（load/enable/disable/unload/重复加载/损坏 manifest）
  - [ ] 黄金测试：插件方言输出与内置方言逐字节一致（复用 tests/golden/）
  - [ ] S1-S5：插件沙箱（子进程/权限）、manifest 校验、路径白名单
  - [ ] R1-R4：插件崩溃不影响宿主、禁用后资源释放
  - [ ] 端到端：init → market 显示真实安装状态 → enable → 输出 → disable → shutdown 全链路
  - [ ] E1-E3（阶段内）
- **提交**：`feat(plugins): 方言插件完整生命周期`

---

### 🔧 Phase 5：AI 模型自主化（1-2 个月，目标 +12%）

#### P5-1 LNN CFC/LTC 网络结构改写

- **入口**：`engineering/python/app/ai/lnn/`（43 文件）
- **目标**：把 CFC/LTC（连续时间卷积/液体时间常数）网络结构用自己的实现重写（保留 PyTorch 张量运算）
- **具体动作**：
  1. 通读 `ai/lnn/` 现有实现，识别哪些是直接调外部库、哪些是自有结构
  2. 重写网络层结构（LTC 细胞/CFC 门控）为自有实现
  3. 保留 torch.nn 作为张量后端
- **验收（生产级）**：
  - [ ] Q1-Q5 全过（torch 文件既有噪声除外并说明）
  - [ ] T1-T6：数值一致性测试——重写后输出与原实现 **逐元素误差 < 1e-6**（golden 数值基线）
  - [ ] 梯度测试：重写层反向传播梯度与数值梯度一致（gradcheck）
  - [ ] 训练收敛测试：小数据集上 loss 单调下降（≥N epoch 达到阈值）
  - [ ] P1-P4：前向/反向性能基准（改动后 ≥ 原 95%）；10 倍序列 <10 倍耗时
  - [ ] R1-R4：模型加载失败降级、推理超时
  - [ ] O1-O5：设计文档 `docs/development/lnn-结构改写.md`（含数学推导）
  - [ ] E1-E3（阶段内）
- **提交**：`refactor(ai): LNN CFC/LTC 结构自主化`

#### P5-2 最终自主占比验证

- **入口**：全仓库
- **目标**：统计自主代码占比，验证 ≥50%
- **具体动作**：
  1. 写占比统计脚本 `scripts/sovereignty_ratio.py`（按模块归类：自主/框架调用/数据）
  2. 生成报告 `docs/自主占比报告.md`
- **验收（生产级）**：
  - [ ] 统计脚本本身过 Q1-Q5
  - [ ] 统计方法文档化（归类规则可复核：抽查 20 个文件人工核对一致）
  - [ ] 占比 ≥50%（按业务代码口径）+ 分层明细（每层占比）
  - [ ] 报告含：各阶段完成度 / 门禁通过率 / 剩余缺口清单
  - [ ] 终审：全量回归 E1-E3 + 阶段验收汇总
- **提交**：`chore(scripts): 自主占比统计`

---

## 5. 进度追踪表（AI 每次会话后更新）

| ID | 任务 | 状态 | 完成日期 | 门禁证据摘要 |
|----|------|------|----------|--------------|
| P0-1 | 依赖架构分析 | ✅ | 2026-08-20 | |
| P0-2 | 护城河盘点 | ✅ | 2026-08-20 | |
| P0-3 | 路线图创建（含 §3 门禁） | ✅ | 2026-08-20 | |
| P1-1 | feature_extraction 白盒化 | ✅ | 2026-08-20 | ruff 0 / mypy 0 / C901 过；新增 53 用例（分类 100% + 状态机 100% 行覆盖）；Q1-Q5/Q/T1-T6/S1/S5/R1/R4/O4/O5 过；设计文档 feature_extraction-白盒化.md |
| P1-2 | parametric_geometry 白盒化 | ⬜ | | |
| P1-3 | dxf pipeline 声明化 | ⬜ | | |
| P2-1 | cutting_experience Schema | ⬜ | | |
| P2-2 | 存储抽象层 | ⬜ | | |
| P2-3 | 数据采集 API | ⬜ | | |
| P3-1 | Element Plus 包装层 | ⬜ | | |
| P3-2 | Store/Router 封装 | ⬜ | | |
| P4-1 | FastAPI 路由声明化 | ⬜ | | |
| P4-2 | 方言插件接线 | ⬜ | | |
| P5-1 | LNN 结构改写 | ⬜ | | |
| P5-2 | 自主占比验证 | ⬜ | | |

**NEXT_TASK（当前指针）**：P1-2 parametric_geometry/ STEP 生成两轮审核白盒化

**状态定义**：
- `⬜` = 未开始 或 **门禁未全过**（即使代码写完了，只要任一门禁缺口，必须保持 ⬜ 并在备注记录缺口）
- `✅` = 代码完成 **且** §3 全部适用门禁通过，附门禁证据

---

## 6. 记忆联动（AI 必须遵守）

每次会话结束：
1. `memory_log` 追加：`[路线图] <任务ID> <完成/进行中/门禁缺口>：<做了什么>，<门禁证据>，<下一步>`
2. 更新 MEMORY.md 的「路线图进度」段落：进度表 + NEXT_TASK + 门禁缺口清单
3. 仅当全部适用门禁通过才在本文档进度表标记 ✅ + 日期

---

## 7. 风险与纪律

| 风险 | 对策 |
|------|------|
| 单会话上下文限制 | 任务已拆成最小可完成单元；每会话只推进 1-2 个任务；门禁未过记录缺口下次续做 |
| torch/scipy 环境残缺 | 相关测试失败先 git stash 验证基线，非改动引入不算回归（但须在门禁证据中说明） |
| 自主化过度（技术债） | 遵守「只白盒业务逻辑，保留框架调用」原则；每任务文档记录边界 |
| 前端 1891 测试回归 | 每次前端改动跑全量 vitest + vue-tsc + vite build |
| API 文档同步门禁 | 每次 API 改动同步 docs/api-reference.md（check_api_docs_sync） |
| **验收标准妥协** | **严禁因进度压力降低门禁**；门禁缺口 = 任务未完成，记录在案下次续做 |

---

## 8. 验收执行细则（AI 必读）

1. **门禁是硬性的，不是建议**：§3 每一项都是「必须」，没有「尽量」。
2. **覆盖率报告必须真实**：用 pytest-cov 实测，不估算；缺口说明具体到函数。
3. **性能基准先建后改**：改动前先记录基线（pytest-benchmark 存档），改动后对比。
4. **契约/黄金测试优先**：凡有既有行为（黄金 NC / OpenAPI schema / 状态流转），一律用逐字段比对防回归。
5. **失败也要留痕**：门禁未过的会话，memory_log 记录：哪个门禁没过、差多少、下一步计划。这不是失败，是正常迭代。
6. **安全是底线**：S1-S5 任何一项不适用时须书面说明「为何不适用」，不得静默跳过。
