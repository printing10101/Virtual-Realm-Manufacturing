# Phase D 参数优化引擎 — 补充交付说明（Round 5）

**创建日期**: 2026-08-20  
**关联**: `docs/development/功能缺口-接线指南.md`（主接线手册）+ `docs/development/参数优化-AI 引擎设计.md`（设计文档）

---

## 📦 本轮新增（纯白盒，零框架依赖，CI 可独立测试）

### 引擎（3 文件）
| 文件 | 内容 |
|---|---|
| `app/optimizer/baseline.py` | L0 基线参数库：15+ 材料×加工类型经验条目 + 别名归一化 + 可覆盖扩展 |
| `app/optimizer/recommender.py` | 分层推荐器：L0 基线 → L1 统计（回调注入）→ 物理安全钳制 + 优化目标微调 |
| `app/optimizer/evaluator.py` | 推荐效果评估（0-1 得分）+ A/B 批量对比（提升率） |

### API（1 文件）
| 文件 | 端点 |
|---|---|
| `app/api/v1/optimizer_routes.py` | POST /recommend / POST /evaluate / POST /compare / GET /baselines（权限 optimizer:read） |

### 测试（2 文件，~45 用例）
| 文件 | 覆盖 |
|---|---|
| `tests/unit/test_optimizer_whitebox.py` | baseline 查找/别名/覆盖、recommender 分层降级/目标微调、evaluator 评分/对比（~28 用例） |
| `tests/api/test_optimizer_api.py` | mini FastAPI app：recommend/evaluate/compare/baselines 端点（~13 用例） |

---

## 🔧 接线补充（并入主接线指南步骤 2/3）

### 步骤 2.4 — contracts 不需要动（optimizer 无新契约，用 dict）

### 步骤 3（路由注册）追加：
```python
from app.api.v1 import optimizer_routes
...
# === 参数优化（Phase D）===
app.include_router(optimizer_routes.router)
```

### 步骤 5 验证命令追加：
```bash
py -3.11 -m pytest engineering/python/tests/unit/test_optimizer_whitebox.py engineering/python/tests/api/test_optimizer_api.py --no-header -q -o addopts=''
ruff check app/optimizer app/api/v1/optimizer_routes.py
py -3.11 -m mypy --config-file mypy.ini app/optimizer app/api/v1/optimizer_routes.py
```

---

## 🎯 设计要点（供验收）

1. **优雅降级**：无基线无数据 → 返回 None（API 404 提示）；统计回调异常 → 自动回退 L0
2. **物理安全**：所有推荐参数经 `clamp_to_safe_bounds` 钳制，`clamped` 标记审计
3. **可解释**：`Recommendation.to_dict()` 含 strategy/confidence/basis（依据+样本数）
4. **可扩展**：L2/L3（LNN/贝叶斯）预留——`ParameterRecommender` 构造参数可注入更高级策略

---

## 📌 累计交付（Round 1-5）

- 文档 7 份（总计划/MTConnect/数据飞轮/参数优化/端对端/接线指南/本说明）
- 后端 12 文件（streaming/conditions/bridge/契约/ORM/repository/experience 路由/optimizer 引擎×3/optimizer 路由）
- 方言 4 文件（Siemens 840D + Heidenhain TNC640）
- 前端 6 文件（API/store/采集表单/监控面板/2 测试）
- 测试 11 文件（~160 用例）
