# 接线前预检报告（代码一致性自检）

**创建日期**: 2026-08-21  
**用途**: 记录环境恢复前对全部新增文件做的静态一致性自检结果，
接线执行者可据此确认「测试引用已全部对齐生产代码」，放心执行门禁验证。

---

## 1. 自检结论

**全部 7 个核心测试文件与生产代码的符号引用 100% 对齐，未发现断链。**
环境恢复后按 `接线指南-v3-总集成.md` 执行即可，无需修复引用问题。

## 2. 逐文件核验结果

| 测试文件 | 引用模块 | 核验结果 |
|---|---|---|
| `tests/unit/test_optimizer_whitebox.py` | `app.optimizer.baseline`（BaselineEntry/BaselineLibrary/DEFAULT_BASELINE/_normalize_material/lookup_baseline）、`app.optimizer.evaluator`（compare_parameter_sets/evaluate_recommendation）、`app.optimizer.recommender`（OptimizationTarget/ParameterRecommender/RecommendationStrategy/clamp_to_safe_bounds） | ✅ 全部存在 |
| `tests/unit/test_parametric_review_state_machine.py` | `app.parametric_geometry._review_state_machine`（ST_*/RV_* 常量 + 9 个函数） | ✅ 全部存在 |
| `tests/unit/test_dxf_pipeline_stages.py` | `app.dxf._pipeline_stages`（STAGES/StageKey/StageStatus + 7 个函数） | ✅ 全部存在 |
| `tests/unit/test_route_registry.py` | `app.api.routers._route_registry`（RouterSpec/validate_spec/validate_specs/is_duplicate_registration/register_routers/group_by_domain） | ✅ 全部存在 |
| `tests/unit/test_sovereignty_ratio.py` | `scripts/sovereignty_ratio.py`（sys.path 加载，is_comment_or_blank 等） | ✅ 加载路径正确 |
| `tests/api/test_monitor_ws.py` | `app.api.v1.monitor_ws`（router + _demo_sample） | ✅ 存在 |
| `tests/unit/test_mtconnect_experience_bridge.py` | `app.contracts.cutting_experience`（MachiningResult）、`app.integrations.mtconnect.experience_bridge`（MTConnectExperienceBridge/_MIN_SPINDLE_RPM）、`app.integrations.mtconnect.parser`（Sample） | ✅ 存在 |

## 3. 待接线时的断言同步点（v3 指南 §5）

`tests/unit/test_mtconnect_experience_bridge.py:38`：
```python
assert exp.parameters.feed_mm_per_rev == 0.5   # 当前（对应 feedrate/1000）
```
修复 `experience_bridge.py` 为 `feed_mm_per_rev = feedrate / spindle_rpm` 后，
此断言必须同步改为：
```python
assert exp.parameters.feed_mm_per_rev == 0.0625  # 500 / 8000 = 0.0625
```

## 4. 其他确认

- 所有白盒模块均为纯 stdlib（零框架 import），CI 可独立跑全量覆盖
- API 测试均使用 mini FastAPI app + `LNN_PERMISSION_ENFORCED=false` 绕过认证
- sovereignty_ratio 测试通过 `sys.path.insert` 加载 scripts/ 目录（CI 收集目标内）
