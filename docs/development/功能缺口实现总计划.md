# 灵境制造 - 工厂实用功能缺口实现计划

**创建日期**: 2026-08-20  
**目标**: 填补功能缺口，使项目从 TRL 7 (技术验证完成) → TRL 8-9 (工厂级实用)

---

## 📊 当前差距分析

### 核心差距总结

| 差距类型 | 差距内容 | 技术实现难度 | 时间估算 | 优先级 |
|---|---|---|---|---|
| **功能缺口** | 实时监控/MTConnect 集成 | 中 | 3 周 | 🔴 P0 |
| **功能缺口** | 数据飞轮闭环 | 高 | 4 周 | 🔴 P0 |
| **兼容性** | Siemens/Heidenhain 现场验证 | 低 | 1 周 | 🟡 P1 |
| **工程化** | 权限分级/审计日志 | 中 | 2 周 | 🟡 P1 |
| **认证** | ISO 9001 可追溯性 | 高 | 6 周 | 🟢 P2 |

**总体准备度**: 85% (小型机加工厂可用), 60% (中大型企业需定制)

---

## 🎯 实施路线图总览

### Phase A: MTConnect 实时监控集成 (3 周)
**目标**: 实现物理机床实时数据采集与控制
- Week 1: MTConnect 适配器基础框架
- Week 2: 实时数据采集管道 + WebSocket 推送
- Week 3: 颤振检测 + 刀具寿命预测可视化

### Phase B: cutting_experience Schema 落地 (2 周)  
**目标**: 数据库底层实现
- Week 1: Schema 定义 + SQLAlchemy 模型
- Week 2: 仓库层实现 + 迁移脚本

### Phase C: 数据采集 API (3 周)
**目标**: 后端采集端点 + 前端界面
- Week 1: HTTP 接口设计 + WebSocket 协议
- Week 2: 前端数据录入界面
- Week 3: 实时数据可视化面板

### Phase D: 参数优化闭环 (4 周)
**目标**: LNN 参数推荐 + A/B 测试
- Week 1-2: LNN 参数推荐引擎
- Week 3: A/B 测试框架
- Week 4: 真实案例验证

### Phase E: Siemens/Heidenhain 方言扩展 (1 周，并行)
**目标**: 声明化方言扩展
- 模板编写 + hooks 实现
- 黄金测试验证

### Phase F: 端对端验证 (1 周)
**目标**: 1 台真实机床全流程验证
- STEP→G 代码→加工结果
- 数据飞轮闭环验证

---

## 📋 详细实施计划

### Phase A: MTConnect 实时监控集成

#### Week 1: MTConnect 适配器框架
**文件结构**:
```
engineering/python/app/integrations/mtconnect/
├── __init__.py
├── client.py          # MTConnect 客户端 ( POLLING + 事件驱动)
├── agv.py             # Agent 视图定义 (XML 解析)
├── data_items.py      # 数据项 Schema 定义
├── adapter.py         # 适配器抽象基类
└── streaming.py       # WebSocket 实时流推送
```

**核心 API**:
```python
class MTConnectClient:
    def connect(self, agent_url: str) -> bool
    def poll_sensor_data(self, items: List[str]) -> Dict[str, Any]
    def subscribe(self, callback: Callable[[Dict], None])
    def disconnect()
```

**门禁**:
- ✅ ruff check 全绿
- ✅ mypy 0 错误
- ✅ 单元测试 15 个 (mock MTConnect Agent)
- ✅ 集成测试 (真实 Agent 模拟)

#### Week 2: 实时采集管道
**功能**:
- Polling interval 动态调整 (5s/10s/30s)
- 数据缓存 + MQTT 消息队列 (可插拔)
- WebSocket 实时推送 (前端订阅)

**测试**:
- 模拟 1000 条/s 数据流，延迟 <100ms
- 断线重连 + 幂等性验证

#### Week 3: 颤振检测 + 刀具寿命
**算法复用**: `app/simulation/cutting_force.py`
- 实时加速度数据分析
- 颤振预警阈值配置
- 刀具寿命预测可视化

---

### Phase B: cutting_experience Schema 落地

#### Week 1: Schema + SQLAlchemy
**文件**:
```
engineering/python/app/contracts/
├── cutting_experience/
│   ├── __init__.py
│   ├── schema.py          # Pydantic schema
│   ├── models.py          # SQLAlchemy ORM
│   └── enums.py           # 枚举类型
```

**关键字段**:
```python
class CuttingExperience(BaseModel):
    id: UUID
    job_id: UUID
    machine_id: str
    program_number: str
    tool_id: str
    parameters: dict  # {depth, feed, spindle, ...}
    results: dict     # {cycle_time, surface_roughness, tool_wear}
   异常情况：dict    # {chatter, vibration, error_code}
    created_at: datetime
```

**门禁**:
- ✅ mpress 迁移脚本
- ✅ 单元测试 20 个
- ✅ SQL 查询性能测试 (索引优化)

#### Week 2: 仓库层
```python
class CuttingExperienceRepository:
    def create(record: CuttingExperience) -> UUID
    def get_by_job(job_id: UUID) -> List[CuttingExperience]
    def query_conditions(condition: QueryCondition) -> List[...]
    def aggregate_stats(stats_type: str) -> Dict
```

---

### Phase C: 数据采集 API

#### Week 1: HTTP + WebSocket 接口
**端点设计**:
```
POST /api/v1/experience/capture      # 单次采集
WS /ws/experience/stream             # 实时流
GET /api/v1/experience/jobs/{job_id} # 批量查询
GET /api/v1/experience/stats         # 统计数据
```

**安全**:
- ⚠️ 权限校验 (`require_permission("experience:write")`)
- 🔒 数据量上限 (防止 DoS)
- ✅ 审计日志 (谁在什么时候采集了什么)

#### Week 2: 前端界面
**组件**:
- `src/views/ExperienceCapture.vue` (手工录入)
- `src/components/realtime/MachineMonitor.vue` (实时波形)
- `src/stores/experienceStore.ts` (WebSocket 订阅)

**测试**:
- vitest 30 个用例
- E2E 测试 (真实采集流程)

#### Week 3: 数据可视化
**图表**:
- 切削力实时曲线 (ECharts)
- 刀具寿命预测图
- 颤振预警热力图

---

### Phase D: 参数优化闭环

#### Week 1-2: LNN 参数推荐引擎
**模型复用**: `app/models/lnn/`
```python
class ParameterOptimizer:
    def train_new_model(new_data: List[CuttingExperience])
    def recommend_params(geometry: Geometry, material: Material) -> OptimizationResult
    def evaluate_recommendation(recommendation, actual_result) -> float
```

**算法**:
- 贝叶斯优化 (Bayesian Optimization)
- 主动学习 (Active Learning)
- A/B 测试对比

#### Week 3: A/B 测试框架
**功能**:
- 实验分组 (control vs treatment)
- 统计显著性验证
- 自动终止条件 (效果达标)

#### Week 4: 真实案例验证
**指标**:
- 切削效率提升 ≥10%
- 刀具寿命延长 ≥15%
- 加工误差 <0.05mm

---

### Phase E: Siemens/Heidenhain 方言扩展 (并行)

#### 文件:
```
postprocessor-plugins/
├── siemens_840d/
│   ├── dialect.yaml
│   └── templates/
│       ├── header.j2
│       ├── cycle_drill.j2
│       └── cycle_bore.j2
└── heidenhain_tnc640/
    ├── dialect.yaml
    └── templates/
        ├── header.j2
        └── cycle_drill.j2
```

**门禁**:
- ✅ 黄金测试 (与现有 GNC 代码逐字符比对)
- ✅ hooks 模式验证
- ✅ 前端管理页展示

---

### Phase F: 端对端验证

#### 真实机床测试
**测试场景**:
1. STEP 导入 → 3D 模型
2. 工艺规划 → 刀路生成
3. NC 代码生成 → 仿真验证
4. 机床加工 → 数据采集
5. 数据飞轮优化 → 下一轮参数

**交付**:
- 📄 真实案例报告
- 🎬 视频演示
- 📊 性能对比数据

---

## 🚨 质量门禁体系

### 每阶段必须满足:
1. **代码质量**
   - ✅ `ruff check app/` 全绿
   - ✅ `mypy` 0 错误
   - ✅ C901 ≤ 10

2. **测试要求**
   - 🔢 单元测试 ≥ 90% 行覆盖
   - 🔢 分支覆盖率 ≥ 80%
   - 🔢 边界 + 错误路径测试

3. **文档同步**
   - 📝 API 文档更新
   - 📝 设计文档更新
   - 📝 用户手册更新

4. **性能验证**
   - ⚡ 响应时间 <500ms
   - ⚡ 无内存泄漏 (1 小时压力测试)
   - ⚡ 10x 输入 <10x 耗时

---

## 📅 时间规划

| 阶段 | 开始日期 | 结束日期 | 里程碑 |
|---|---|---|---|
| Phase A | 2026-08-20 | 2026-09-10 | MTConnect 实时采集 |
| Phase B | 2026-09-03 | 2026-09-17 | Schema + 仓库层 |
| Phase C | 2026-09-03 | 2026-09-24 | API + 前端界面 |
| Phase D | 2026-09-10 | 2026-10-08 | 参数优化闭环 |
| Phase E | 2026-08-20 | 2026-08-27 | Siemens/Heidenhain |
| Phase F | 2026-10-08 | 2026-10-15 | 真实机床验证 |

**总工期**: 8 周 (2 个月)

---

## 🔐 风险评估与应对

| 风险 | 概率 | 影响 | 应对措施 |
|---|---|---|---|
| MTConnect Agent 兼容性差 | 中 | 高 | 提供 mock 测试 + 真实 Agent 适配 |
| 数据采集量不足 | 高 | 中 | 手工录入 + 模拟数据 |
| LNN 模型效果不达预期 | 中 | 高 | 保留规则引擎 fallback |
| 现场调试资源不足 | 高 | 中 | 远程调试工具 + 文档完善 |

---

## 📝 交付清单

### 代码交付
- [ ] `app/integrations/mtconnect/` 完整模块
- [ ] `app/contracts/cutting_experience/` Schema + 仓库
- [ ] `app/api/v1/experience/` 采集 API
- [ ] `src/views/Experience*.vue` 前端界面
- [ ] `postprocessor-plugins/siemens_840d/` 方言
- [ ] 完整测试套件

### 文档交付
- [ ] `docs/development/MTConnect-集成设计.md`
- [ ] `docs/development/数据飞轮-Schema 设计.md`
- [ ] `docs/development/参数优化-AI 引擎.md`
- [ ] API 文档 (OpenAPI 3.0)
- [ ] 用户手册更新

### 数据交付
- [ ] 真实案例报告 1-2 份
- [ ] 性能对比数据
- [ ] 视频演示

---

## 🎯 成功标准

1. **功能性**: 1 台真实机床稳定跑通"图纸→加工"全流程
2. **性能**: NC 代码生成成功率 ≥95%, 加工误差 <0.05mm
3. **用户体验**: 数据采集 ≤3 分钟/次，参数推荐响应 <1s
4. **数据飞轮**: 至少 1 个案例实现效率提升 ≥10%

---

**备注**: 本计划需根据实际现场情况微调，每阶段结束进行评审和调整。
