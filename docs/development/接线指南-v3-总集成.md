# 接线指南 v3（总集成版）——功能缺口 + 路线图全部新增文件

**创建日期**: 2026-08-21  
**替代**: `功能缺口-接线指南.md`（v1）+ `接线补充说明-v2.md` + 各白盒化设计文档中的「待接线」段  
**用途**: 文件锁解除后，按本指南**一次性完成全部接线与验证**，然后即可交付上线。

---

## 0. 前置确认

- 环境恢复标志：pwsh 可用（`Write-Output ok` 有输出）且已存在文件可 edit（无 EACCES）
- 恢复后先 `git status` 确认 15 轮新增文件全部在 working-tree（未提交）

---

## 1. 删除垃圾/错误文件（4 个）

| 文件 | 原因 |
|---|---|
| `engineering/python/app/api/v1/experience.py` | 错误导入 `from app.api.v1.deps import require_permission`（deps.py 不存在）→ 正确版是 experience_routes.py |
| `engineering/python/app/services/domain/cutting_experience_service.py` | 错误导入 `from app.database.models import CuttingExperienceRecord`（未导出）→ 正确版是 cutting_experience_repository.py |
| `engineering/python/tests/unit/_write_probe_test.py` | 探针文件 |
| `engineering/python/tests/unit/_edit_probe.py` | 探针文件 |

```bash
rm engineering/python/app/api/v1/experience.py \
   engineering/python/app/services/domain/cutting_experience_service.py \
   engineering/python/tests/unit/_write_probe_test.py \
   engineering/python/tests/unit/_edit_probe.py
```

---

## 2. `__init__.py` 导出（3 处）

### 2.1 `engineering/python/app/integrations/mtconnect/__init__.py` 追加
```python
from app.integrations.mtconnect.streaming import (
    MTConnectStreamServer, StreamEvent, AlertEvent, StreamConsumer, WebSocketAlertHandler,
)
from app.integrations.mtconnect.conditions import (
    ConditionChecker, ChatterDetector, Alert, AlertCondition, AlertPriority, AlertType,
)
from app.integrations.mtconnect.experience_bridge import MTConnectExperienceBridge
```

### 2.2 `engineering/python/app/database/models/__init__.py` 追加
```python
from app.database.models.cutting_experience import CuttingExperienceRecord
```

### 2.3 `engineering/python/app/contracts/__init__.py` 追加
```python
from app.contracts.cutting_experience import (
    CuttingParameters, CuttingResults, MachiningAnomaly, CuttingExperience,
    ExperienceQuery, ExperienceStats,
)
```

### 2.4 白盒模块导出（4 处）
```python
# app/parametric_geometry/__init__.py 追加
from app.parametric_geometry._review_state_machine import (
    ST_PENDING, ST_RUNNING, ST_STEP_GENERATED, ST_REVIEWED,
    ST_SUCCEEDED, ST_FAILED, ST_CANCELLED,
    can_execute, can_review, can_finalize, all_features_reviewed,
    next_status_after_review, is_terminal,
)

# app/dxf/__init__.py 追加
from app.dxf._pipeline_stages import (
    STAGES, StageKey, StageStatus,
    stage_name, stage_failure_is_fatal, should_abort_after,
    progress_of, summarize_pipeline,
)

# app/postprocessor/dialect/__init__.py 追加
from app.postprocessor.dialect._lifecycle import (
    DialectLifecycleStage, can_transition, assert_transition_allowed,
    next_stage_after_success, next_stage_after_failure, can_discover, is_terminal,
)

# app/api/routers/__init__.py 追加
from app.api.routers._route_registry import (
    RouterSpec, validate_specs, register_routers, is_duplicate_registration, group_by_domain,
)
```

---

## 3. 路由注册（P4-1 声明表，一次性接线）

`engineering/python/app/api/routers/engineering.py` 改为声明式：

```python
from app.api.routers._route_registry import RouterSpec, register_routers
from app.api.v1 import experience_routes, monitor_ws, optimizer_routes

ENGINEERING_ROUTERS: list[RouterSpec] = [
    # ... 既有 11 路由（simulation/chatter/cutting_force/project/step_import/rules/
    #     dxf_pipeline/collision_check/tools/nl2cad/postprocessor_dialects）...
    RouterSpec("experience", experience_routes.router, "engineering", "加工经验飞轮"),
    RouterSpec("optimizer", optimizer_routes.router, "engineering", "参数优化"),
    RouterSpec("monitor_ws", monitor_ws.router, "engineering", "实时监控 WS"),
]

def register(app: FastAPI) -> None:
    register_routers(ENGINEERING_ROUTERS, app.include_router)
```

前端路由（P3-2）：`engineering/src/router/index.ts` 改用 `createAppRouter({ routes, titleSuffix })`。

---

## 4. 委托接线（pipeline.py 3 处 + registry.py 3 处 + engineering.py 3 处）

### 4.1 parametric_geometry/pipeline.py（3 处委托）
```python
from app.parametric_geometry._review_state_machine import (
    can_execute, can_review, can_finalize, is_valid_review_status,
    all_features_reviewed, next_status_after_review,
)
# run_pipeline:     if not can_execute(task.status): raise ...
# review_step_feature: if not can_review(task.status): raise ...
#                     if not is_valid_review_status(review_status): raise ...
#                     all_reviewed = all_features_reviewed([...]); new_status = next_status_after_review(...)
# finalize_step:    if not can_finalize(task.status): raise ...
```

### 4.2 dxf/pipeline.py（3 处委托）
```python
from app.dxf._pipeline_stages import STAGES, stage_name, should_abort_after, summarize_pipeline
# Stage 名称用 stage_name(StageKey.X)
# Stage3 失败降级判定：should_abort_after(StageKey.MODEL_CONVERT, failed=True) == False
# result.summary = summarize_pipeline(statuses, success)
```

### 4.3 postprocessor/dialect/registry.py（3 处委托 + 新能力）
```python
from app.postprocessor.dialect._lifecycle import (
    DialectLifecycleStage, next_stage_after_success, next_stage_after_failure,
)
# self._stage: dict[str, DialectLifecycleStage] = {}
# discover():   self._stage[id] = next_stage_after_success(cur, "discover")
# compile_all(): self._stage[id] = next_stage_after_success(cur, "compile")（失败 → next_stage_after_failure）
# register_to(): self._stage[id] = next_stage_after_success(cur, "register")
# 新增：unregister(dialect_id) / lifecycle_status(dialect_id)
```

---

## 5. bridge feed 物理修复 + 测试断言同步

`experience_bridge.py`：
```python
feed_mm_per_rev = (sample.feedrate or 0.0) / spindle_rpm   # 原来是 /1000.0
```
同步更新 `test_mtconnect_experience_bridge.py` 断言：`0.5 → 0.0625`（500/8000）。

---

## 6. 全量门禁验证（顺序执行）

```bash
# 6.1 新增白盒+API 测试
cd engineering/python
$env:PYTHONUTF8=1; $env:PYTEST_DISABLE_PLUGIN_AUTOLOAD=1
py -3.11 -m pytest tests/unit/test_mtconnect_monitoring.py \
  tests/unit/test_cutting_experience_contract.py \
  tests/unit/test_cutting_experience_repository.py \
  tests/unit/test_mtconnect_experience_bridge.py \
  tests/unit/test_optimizer_whitebox.py \
  tests/unit/test_parametric_review_state_machine.py \
  tests/unit/test_dxf_pipeline_stages.py \
  tests/unit/test_sovereignty_ratio.py \
  tests/unit/test_route_registry.py \
  tests/unit/test_dialect_lifecycle.py \
  tests/api/test_optimizer_api.py \
  tests/api/test_monitor_ws.py \
  --no-header -q -o addopts=''

# 6.2 ruff（CI 等价：只查 app/）
ruff check app/

# 6.3 mypy（Windows 必须显式 config）
py -3.11 -m mypy --config-file mypy.ini app/parametric_geometry/_review_state_machine.py \
  app/dxf/_pipeline_stages.py app/api/routers/_route_registry.py \
  app/postprocessor/dialect/_lifecycle.py app/optimizer/

# 6.4 覆盖率（白盒模块目标 100%）
py -3.11 -m coverage run --source=app.parametric_geometry._review_state_machine,app.dxf._pipeline_stages,app.api.routers._route_registry,app.postprocessor.dialect._lifecycle -m pytest \
  tests/unit/test_parametric_review_state_machine.py tests/unit/test_dxf_pipeline_stages.py \
  tests/unit/test_route_registry.py tests/unit/test_dialect_lifecycle.py --no-header -q -o addopts=''
py -3.11 -m coverage report -m

# 6.5 前端（Headless + Store + Router + 飞轮组件）
cd ../../src
npx vitest run composables/__tests__/useDataTable.test.ts \
  stores/__tests__/defineCrudStore.test.ts \
  router/__tests__/createAppRouter.test.ts \
  api/__tests__/parameterOptimizer.test.ts api/__tests__/cuttingExperience.test.ts \
  stores/__tests__/experienceStore.test.ts components/__tests__/MachineMonitor.test.ts \
  components/__tests__/ExperienceCapture.test.ts components/__tests__/ParameterRecommendPanel.test.ts
npx vue-tsc --noEmit
```

---

## 7. 验收清单（27+ 文件，全部接线后应通过）

### 后端（20 文件）
| 类别 | 文件 |
|---|---|
| MTConnect | streaming.py / conditions.py / experience_bridge.py |
| 数据飞轮 | contracts/cutting_experience.py / database/models/cutting_experience.py / services/domain/cutting_experience_repository.py / api/v1/experience_routes.py |
| 参数优化 | optimizer/{baseline,recommender,evaluator}.py / api/v1/optimizer_routes.py |
| 白盒 | parametric_geometry/_review_state_machine.py / dxf/_pipeline_stages.py / api/routers/_route_registry.py / postprocessor/dialect/_lifecycle.py |
| 实时 | api/v1/monitor_ws.py |

### 方言（4 文件）
siemens_840d/{hooks,dialect}.yaml + heidenhain_tnc640/{hooks,dialect}.yaml

### 前端（15 文件）
cuttingExperience.ts / experienceStore.ts / ExperienceCapture.vue / MachineMonitor.vue / parameterOptimizer.ts / ParameterRecommendPanel.vue / headless/useDataTable.ts / crud/defineCrudStore.ts / router/createAppRouter.ts + 7 测试文件

### 脚本（1 文件）
scripts/sovereignty_ratio.py

### 测试（26 文件，~356 用例）
全部应在 6.1/6.5 中通过

---

## 8. 完成后的收尾

1. 跑 `python scripts/sovereignty_ratio.py --target 0.50`（自主占比门禁）
2. 更新路线图进度表：P1-2/P1-3/P2-1/2/3/P3-1/P3-2/P4-1/P4-2/P5-2 全部标 ✅
3. 更新 AGENTS.md / 记忆系统
4. commit（建议分 3 个：`feat(python)` / `feat(api)` / `feat(frontend)`）
5. Phase F 端对端验证（需真实 MTConnect 机床，≥50 数据记录）
