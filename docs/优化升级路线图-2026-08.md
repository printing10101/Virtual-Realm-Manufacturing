# 优化升级路线图（2026-08）

> 定位：把灵境制造作为公司主力产品推向市场。本路线图回答"从哪些地方入手做全面优化升级"——
> 不仅是代码，更是整个项目。执行原则：**不堆功能、做深信任**；每项完成即在本文件打勾并登记证据。

---

## 0. 总体判断

项目最不缺的是"代码和功能"（后端 1000+ 模块、25 万行、3441 项单测全绿），最缺的是
**信任资产**（真机验证、E2E 回归、交付闭环）。优化重心是四条线并行：

| 主线 | 回答的问题 | 现状 |
|---|---|---|
| A. 核心能力做实 | 客户敢不敢用 | 真机未接（mock 仿真器）、后处理器未实测 |
| B. 质量体系做深 | 承诺能否兑现 | 覆盖率 24%、E2E 曾是空壳（本轮已修复） |
| C. 交付闭环做顺 | 卖出能否装上 | 桌面打包未全新机验证、无自动更新/授权体系 |
| D. 仓库资产治干净 | 长期开发效率 | 双轨制残留、根目录杂物、research/ 未解耦 |

---

## 1. 本轮已完成

### 叙事对标 W 系列·第七批（2026-09，对应《产品叙事与战略对标-2026-09.md》）——本季度批次收官
- [x] **W10.2 Onboarding 制造流程引导**：App 新增第二个 Tour 实例（8 步 walkthrough：全流程总览 → 自然语言建模 → 工艺规划 → 仿真模拟 → 刀具路径 → 数据飞轮 → 审批中心 → 物理校验兜底收尾），目标选择器为侧边栏 `a[href]` 稳定定位；触发链路：全局引导完成/跳过后链式播放，老用户（全局已完成但流程未完成）下次启动补放一次，完成/跳过写 `flow_tour_completed_v1` 标记；文案中英双语，首体验即传达「AI 说的不算，物理说了算」卖点
- 证据：`src/__tests__/App.flowTour.test.ts`（6 用例：双实例/步骤目标/链式触发/去重/老用户补放/完成标记）；vue-tsc 零错误、vitest 1923 全绿

### 叙事对标 W 系列·第六批（2026-09，对应《产品叙事与战略对标-2026-09.md》）
- [x] **W9.4 性能白皮书成文**：`docs/性能白皮书-2026-09.md`——10 套件中 9 个成功实测（CAM 全流程均件 0.118s、闭环 CAM 校验仅占周期 1.8%、安全盾 100% 拦截 + 0.057ms 开销、持续压测 3.7 万 rps 零错误），含使用纪律（单机限定语）、已知缺口诚实清单（world_model bench 维度 bug、api 基准需在线服务）与维护约定。基准历史产物已 gitignore（白皮书数字为准）
- [x] **W10.1 MCP 扩面确认 + 开关**：探查勘误——factory（4 工具）/device（AAS 描述符自动生成 7 工具）注册**已在既有代码中落地**（工具面 6→17），此前探查报告漏读文件尾；本批补齐 docstring 与实际行为的一致性，并新增 `LINGJING_MCP_FACTORY_TOOLS=0` 关闭开关（关=仅 LNN 工具面）
- [x] **基准运行器健壮化**：`run_perf_benchmark.py` 重构为按套件容错（单套件失败不拖垮全部，失败清单随结果 JSON 留痕）；`lnn_inference_bench` torch 守卫补 `OSError`（坏 DLL 安装不再杀死基准）。既有 40 用例 + 22 工厂/设备用例全绿
- 证据：`docs/性能白皮书-2026-09.md`；ruff 全绿

### 叙事对标 W 系列·第五批（2026-09，对应《产品叙事与战略对标-2026-09.md》；⚠️ 本批存在并行会话同时施工，已做协调）
- [x] **W8a 统一嵌入检索 API（由并行会话落地，本会话让位）**：`app/api/v1/embedding_retrieval.py`（/encode /index /query /status，已注册 ai 域）+ `tests/api/test_embedding_retrieval.py`（5 用例通过）——与叙事方案 W8.1 完全同构（文档字符串即引用该方案）。本会话同期构建的重复面（`unified_embedding.py` 路由 + `unified_retrieval_service.py`，会产生第二个检索器实例）已主动删除，避免双 API 面与索引状态分裂。**遗留缺口**（后续可在 /embedding 之上叠加，注意与并行会话协调）：结构化工艺条目（材料硬度/工艺参数/精度等级）的便捷摄取编码——当前 /index 接收原始 512 维向量，语义轴编码（MaterialAxis/ProcessAxis/...）需调用方自行完成
- [x] **W8.2 RAG 答案引用溯源（本会话）**：`app/rag/service.py` 的 `/api/v1/rag/query` 两种管线（enhanced/baseline）响应均新增 `sources` 字段——每条检索建议带 `rank/source/doc_id/similarity/preview`，doc_id 即知识条目 ID 可反查来源文档（与 W2 血缘体系对齐）；归一化辅助函数兼容 ChromaDB 嵌套结构与增强管线扁平结构
- 证据：`tests/unit/test_rag_citations.py`（10 用例：归一化/引用抽取/两管线端到端）+ ruff 全绿；全会话回归 146 通过（并行改动未破坏既有套件）

### 叙事对标 W 系列·第四批（2026-09，对应《产品叙事与战略对标-2026-09.md》）
- [x] **W6 世界模型物理预演 + 护盾×主权联动**
  - **W6a 物理预演卡 API**：`POST /api/v1/world-model/preview`——轨迹预测（颤振/磨损/粗糙度/置信度）+ 阈值带（颤振 0.3/0.5 引用 rl_agent 契约与奖励层既有判据；磨损 0.2mm 引用 VB 判据）+ SafetyShield 物理裁决 + 主权保守缩放 + 主权决策，一次调用返回确认页所需的完整预演卡数据
  - **真实 bug 修复（SafetyShield 安全回退正确性）**：strict 模式回退动作取自 `prev_action` 引用，当调用方传入的 prev 恰为被检查的违规动作时（单动作预演等场景），"安全回退"会静默返回违规动作本身。已在回退分支补边界校验，回退动作不合法则落到默认安全动作（零调整）
  - **W6b 护盾×主权联动**：`app/services/sovereignty.py` 新增保守系数（L0=0.5 / L1=0.7 / L2-L4=1.0），护盾过滤后的动作按等级阻尼——"等级越低护盾介入越深"从话术变成行为
  - **W6c 前端物理预演 Tab**：Workspace 新增第 5 个 Tab（`WorkspacePreviewTab.vue` + `api/worldModel.ts`），输入状态/候选动作 → 预演卡展示 verdict（预演通过/需要注意/不建议执行）、指标 vs 阈值带、安全盾裁决、最终动作（含阻尼标记）、主权确认提示；中英双语
- 证据：`tests/api/test_world_model_preview.py`（7 用例）+ `tests/unit/test_sovereignty_conservatism.py`（6 用例行为矩阵）+ 护盾闭环集成回归 19 通过；ruff 全绿、vue-tsc 零错误、vitest 1917 通过

### 叙事对标 W 系列·第三批（2026-09，对应《产品叙事与战略对标-2026-09.md》）
- [x] **W7.2 dreaming 最小 API + 飞轮看板「它学会了什么」**：反思管线提取为服务层 `app/dreaming/service.py`（CLI 与 API 单一事实来源，CLI `reflect` 已委托）；新增 `app/api/v1/dreaming.py` 七端点（reflect 触发反思 / rules 草稿×灰度联查 / publish 人工放行入影子 / promote 灰度晋级（FULL 强制双重校验）/ rollback 一键回滚 / status / learnings 事件流），注册进 workflows 域（ADR-021）。**打通了此前完全断裂的闭环**：ProgressivePublisher 在 dreaming 之外原为零调用，反思合成的草稿从未进入灰度管线——现在「反思→草稿→人工放行→灰度→晋级/回滚」全程可经 API 驱动；前端飞轮看板新增第 5 个 Tab（时间线事件卡：新学会规则/进入灰度/晋级/降级，含置信度与原因，中英双语）
- 证据：`tests/api/test_dreaming_api.py`（14 用例，全程 tmp 隔离）+ dreaming 既有套件回归 44 通过；vue-tsc 零错误 + vitest 1917 通过 + ruff 全绿

### 叙事对标 W 系列·第二批（2026-09，对应《产品叙事与战略对标-2026-09.md》）
- [x] **W9.1 主权设置接线后端（信任红线 5 解除）**：新增 `app/services/sovereignty.py`——五级自主度策略引擎（语义与前端 useSovereigntySettings 严格一致），sqlite3 持久化（`python/data/sovereignty_settings.db`，`SOVEREIGNTY_DB` 可覆盖）；治理域新增 `GET/PUT /api/v1/governance/sovereignty` + `POST .../sovereignty/reset`（写操作需 `governance:write`）；**真实行为挂点**：`/rl-agent/training/start` 在策略要求确认时以 FORBIDDEN 拒绝自动启动，`/rl-agent/act` 响应附带 `sovereignty` 决策（`requires_confirmation` 供执行方消费）；前端 `useSovereigntySettings` 改为后端权威 + localStorage 离线缓存（新增 `src/api/governance.ts`），vue-tsc 零错误
- [x] **W7.1 dreaming 补测试（自进化闭环可暴露）**：新增灰度发布 16 用例（SHADOW→CANARY→…→FULL 逐级晋级、指标阈值阻断、跨级拒绝、FULL 双重沙箱校验、应用失败自动回落 SHADOW、跨实例状态持久化、SHADOW 再降级触发 DEPRECATED）+ 回滚管理器 7 用例（硬约束立即废弃、普通异常逐级降级、demote 失败直接回滚兜底、冷却期/历史留痕/持久化）——「AI 自我改进永远可灰度、可回滚」话术自此有测试锚点

### 叙事对标 W 系列·第一批（2026-09，对应《产品叙事与战略对标-2026-09.md》）
- [x] **W1.1 校验失败自动修复闭环**：`app/agent/orchestrator.py` 编排器升级——`validate_safety` 不再一票否决即中断，而是返回结构化诊断（错误码/行号/建议值），按「参数级修复→重新生成→文本级修复→重验」顺序自动修复，预算默认 3 次（`LNN_ORCHESTRATOR_MAX_REPAIRS` 可调，0=关闭）；预算耗尽/不可修复（空程序等）显式转人工并附完整诊断。修复动作含追加 M30、负进给取绝对值、参数 clamp。`PipelineResult` 新增 `repair_count`/`repair_history` 随 trace 落盘，`get_statistics` 新增 `repaired_pipelines`/`repair_actions_total`
- [x] **W9.2 监控演示数据治理（信任红线解除）**：`app/api/v1/monitor_ws.py` 默认不再静默回落 demo/模拟数据——未配置 `MTCONNECT_AGENT_URL` 且未显式开启 `LNN_MONITOR_ALLOW_DEMO=1` 时，推送 `agent_disconnected` 告警而非虚构机床数据；demo 数据仅在显式演示模式可用且强制带 `source=demo` 标记
- [x] **W10.4 skill_marketplace 遮蔽修复（真 bug）**：删除与 `skill_marketplace.py`（真实实现，含并发锁/原子写/zip 打包）同名的 stub 目录包——Python 正则包优先级导致市场 API 一直返回 stub 假数据；同步加固 skill_id/version 白名单校验（拒绝 `../` 等路径穿越载荷进文件名）。防回归测试锁定「同名 stub 目录不得复活」。**勘误**：探查报告所称 `integrations/opcua` 为"1 行空壳"系误报（实际含 adapter/cli/parser+测试），未做改动
- [x] **W10.3 Copilot 死代码下线**：删除无任何页面挂载的 `src/components/Copilot/` 三件套及其测试（真实审批交互在 ApprovalDashboard），`components.d.ts` 同步清理
- 证据：`tests/unit/test_agent_orchestrator_repair.py`（10 用例）、`tests/api/test_monitor_ws.py`（15 用例，含默认不虚构数据红线用例）、`tests/unit/test_skill_marketplace_shadowing.py`（7 用例）、定向回归 72 通过 + 前端 vitest 1917 通过

### A 线：仿真强制闭环（2026-09 批次，产品级闸门，无机器依赖的信任核心）
- [x] **体素材料去除仿真校验**：新增 `app/cam_validation/voxel_validator.py`——合成盒状毛坯体素网格（零 trimesh/STL 依赖），复用 Rust compute-core 切削原语，检测切削段过切毛坯底面 / 快速段（G00）在安全高度下切入材料两类致命碰撞，自动回退纯 Python 内核。接入阶段 7 管线（无开关硬约束，仅体素尺寸/刀具/段数上限等性能旋钮），结果随 cam_report.json 导出（`voxel_simulation_report`）
- [x] **DNC 下发硬闸**：`POST /api/v1/dnc/nc-program/send` 发送前强制追溯——程序必须对应一个 SUCCEEDED 且体素通过（`voxel_check_passed=True`）的阶段 7 校验任务，否则 403 + 错误码 8013（`ErrorCode.NC_NOT_VALIDATED`）。外部/历史程序逃生阀 `LNN_DNC_ALLOW_UNVALIDATED_NC=1`（fail-closed，开启留 warning 审计日志）。导出侧不设闸（阶段 6 导出产物是阶段 7 的输入，导出闸会造成流水线死锁）
- 证据：`tests/unit/test_voxel_validator.py` + `tests/unit/test_cam_voxel_pipeline.py`（管线级含报告导出断言）+ `tests/unit/test_dnc_nc_gate.py` + `tests/api/test_dnc_api.py` 闸门契约用例

### A 线：后处理器验证（2026-09 批次，实测矩阵的软件前置层）
- [x] **手动锚定合规层（L1）**：新增独立方言合规校验器 `tests/utils/nc_dialect_checker.py`（纯标准库、零复用生成器代码）+ 全量 golden 合规测试 `tests/regression/test_postprocessor_manual_compliance.py`（22 基线 × strict/structural + 9 个负样本防空转）。**首轮即抓出 5 个真实缺陷**（Heidenhain 会话式程序泄漏 G00/G01、两个 hooks 方言静默加载失败、4 国产方言注释嵌套括号、Siemens PATH 行格式、Heidenhain RTCP 输出 Fanuc 指令），全部修复
- [x] **方言覆盖补全**：`heidenhain_tnc640_declared` / `siemens_840d_declared`（hooks 模式）纳入黄金基线（此前因 declaration.py 不支持列表 hooks 从未注册成功）
- [x] **机型 × 验证状态官方矩阵**：`docs/development/postprocessor-验证矩阵.md`（L1 语法/L2 黄金/L3 编程站/L4 实机四级定义 + 存疑项登记）
- 证据：合规+golden+一致性 90 用例、后处理器相关全量 605+ 用例通过

### 归档批次：2026-08-28

#### A/B 线：数据飞轮采集 API 修复与接线
- [x] **双实现合并**：删除未注册的旧 `experience_routes.py`（前缀 `/api/cutting/experience`、无权限控制），统一为 `app/api/v1/cutting_experience/routes.py`（前缀 `/api/v1/experience`，与前端 `@/api/cuttingExperience` 一致），在 `api/routers/engineering.py` 接线
- [x] **修复 UUID 主键 bug**：ORM 主键为 `exp_{hex}` 字符串，原路由路径参数为 `UUID` 类型，capture 返回的 id 查详情必 422；repository 增加 `_normalize_pk`（契约 UUID 与 ORM 主键双向归一化）
- [x] **错误风格统一**：路由从裸 `HTTPException` 迁移到 `app.core.exceptions` 分级异常（503→`ServiceUnavailableException`/2002、404→`RecordNotFoundException`/3002、422→`ValidationException`/1002）
- [x] **测试重写**：`tests/api/test_cutting_experience_routes.py` 从容忍断言（`assert status in [200,400,503]`，几乎永不失败）重写为真实链路精确断言（临时 SQLite + 真实注册/登录 JWT + 精确状态码与错误码），20/20 通过
- [x] **E2E 空壳转实**：`tests/e2e/test_core_links.py` 原 9 个用例全部 skip+pass；现实现 4 条真实链路（system 探活 / auth 注册-登录-登出 / 数据飞轮采集-查询-统计-删除闭环 / 仿真入口参数校验）+ 架构一致性断言（含路由接线防回归），10/10 通过
- [x] **恢复 conftest 防护**：`collect_ignore = ["app"]` 的"临时注释"已恢复并验证（pytest.ini `norecursedirs` 已含 app，注释本就多余；该防护是 E-P0-3 防复发机制的一部分）

#### B/D 线：全量测试门禁修复（38 个存量失败清零）
- [x] **heartbeat 套件复活（8 个）**：补 `pytestmark = pytest.mark.asyncio`（pytest-asyncio strict 模式下该套件从未真正运行过）；修复 `_scheduler` 私有符号漂移（改为替换 `app.dependencies.get_scheduler`）；修复 cost record 字段漂移（`resource_type`→`cost_type`、`timestamp`→`recorded_at`）
- [x] **修复真 bug：CrossModalAttentionFusion 新模态 KeyError**（`app/data/pipeline/fusion.py`）：投影矩阵只在首次 `fuse()` 按当次模态集合初始化，管道以可变模态组合复用同一融合器时（后追加 tool_state）直接崩溃；改为惰性补齐 `_ensure_projections`（`fuse`/`get_attention_weights` 对齐）
- [x] **环境守卫补齐（诚实 skip 而非伪失败）**：`sentence_transformers`×2、`torch`×3、`trimesh`×1 按 S7/仓库惯例加 `importorskip`
- [x] **过时断言修正**：`test_response`（FILE_NOT_FOUND 已有独立映射 1008，并补 fallback 分支测试）、`test_step_import` T44（HTTP 200+信封 → HTTP 404 统一错误体契约）
- [x] 本机补装 `pytest-asyncio`（与 CI 环境对齐）。结论：38 个失败全部为**存量漂移/环境缺依赖**，与数据飞轮改动无关

#### D 线：治理
- [x] pr.yml 过时注释修正（前端阈值 65%→34% 与 `vitest.config.ts` 实际对齐）
- [x] API 文档同步门禁复核：`scripts/check_api_docs_sync.py` → FULLY SYNCHRONIZED
- [x] 文档同步：数据飞轮 API.md / Repository 设计.md 的旧文件引用更新；两份历史接线指南头部加状态标注
- [x] 仓库卫生：删除 9 个 commit_msg 草稿、runs/ 训练产物、取消跟踪 playwright-report；.gitignore 增补会话产物规则

---

## 2. A 线：核心能力做实（决定产品能不能卖）

- [ ] **真实机床接入**（负责人：用户本人，节奏自定）
  - 优先 FANUC FOCAS 或 MTConnect 真机一种协议打通
  - 替换 `app/dnc/mock_agent.py` 的生产角色（保留为测试设施）
  - 验收：真机状态/负载/程序号在设备监控页实时刷新
- [ ] **后处理器实测矩阵**
  - 11 种后处理器逐机型在真实机床上试切验证
  - 每机型沉淀 golden NC 输出文件，做回归测试（`pytest.mark.postprocessor`）
  - 产出"机型 × 验证状态"官方支持清单（对外承诺边界）
- [x] **仿真强制闭环**：AI 生成的 NC 代码必须通过体素仿真校验才能导出/下发（产品级流程约束，不只是功能存在）——✅ 2026-09 完成，见 §1「仿真强制闭环」（软件闸门已落地；参数合理性仍靠颤振引擎 + 文献实测数据，实机复核归 L4）
- [ ] **工艺知识库冷启动**：公开切削手册 + 种子客户老师傅经验入 cutting_experience 库；试点数据回收流程化（每次试点 = 飞轮充能）

## 3. B 线：质量体系做深（决定承诺能否兑现）

- [ ] **覆盖率棘轮（ratchet）**：`.coveragerc` `fail_under` 只升不降（当前 24%），每次迭代 +1~2%；优先补 `gcode_generation/`、`postprocessor/`、`process_planning/`（直接上机的路径）
- [ ] **清除形式主义测试**：全库扫描 `assert ... in [200, 400, 503]` 式多选一断言并重写（本轮已清掉 cutting_experience 一处）
- [ ] **架构测试违规清零**：`tests/architecture/` 中 `pytest.skip` 降级的遗留分层违规逐个修复，最终移除 skip
- [ ] **双轨制收敛**（定截止日期）：
  - `api/v1/` 平铺路由 vs `api/routers/` 聚合层 → 新路由一律走领域聚合
  - 裸 `HTTPException` vs `AppException` → 新代码一律分级异常
  - 模块级懒单例 vs `dependencies.py` DI → 测试可替换性差的逐个迁移

## 4. C 线：交付闭环做顺（决定卖出能否装上）

- [ ] **桌面打包验证**（发布报告 Go/No-Go 第 2 项）：`npm run app:build` 产出的安装包在全新机器验证「图纸→3D→工艺→NC」全流程 + 重启后 agent 状态不丢
- [ ] **CI 干净环境验证**（Go/No-Go 第 3 项）：GitHub Actions runner 上依赖解析 + 全量测试与本地一致
- [ ] **自动更新**：Tauri updater（签名更新通道），桌面产品没有 OTA 每次升级都是上门服务
- [ ] **崩溃/错误上报**：Sentry 或 self-hosted GlitchTip（制造业客户数据不出厂）
- [ ] **授权体系**：试用期 / 机型模块授权 / 离线激活——后加成本远高于先做
- [ ] **服务端部署形态**：Docker 镜像 + 首启建库验证，覆盖"内网服务器全厂共用"需求

## 5. D 线：仓库资产治干净（决定长期效率）

- [ ] research/（11055 个 py 文件）物理解耦为独立仓库；共享产物以版本化模型文件进入工程侧
- [ ] release 流程固化：tag → 全量测试 → 打包验证 → release workflow 自动化

## 6. 明确不做

- 不大重构、不换框架（现架构分层健康）
- 不开新功能线（功能广度已远超信任深度）
- 科研侧（论文/竞赛）与产品线分开管理，不占主线窗口

---

## 7. 节奏

| 窗口 | 内容 |
|---|---|
| 本月 | 本轮已完成项（见 §1）+ 仓库清理提交 + 打包验证 + 后处理器 golden test 框架 |
| 1-3 个月 | 真机接入 + 后处理实测 + 仿真闭环 + 3-5 家种子客户试点（采集：编程省时、一次上机通过率） |
| 3-6 个月 | 覆盖率棘轮 24%→40%+ / 自动更新 + 监控 + 授权 / research 解耦 / 服务端形态 |
