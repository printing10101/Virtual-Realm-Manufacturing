# 变更摘要 V1.4.0

> **文档版本**: V1.4.0  
> **发布日期**: 2026-05-09  
> **上一版本**: V1.3.0  
> **提交范围**: `bec9f12..f726017` (共6次提交)  
> **文件变更**: 128个文件 (新增21457行，删除3701行)

---

## 目录

1. [功能新增](#一功能新增)
2. [功能优化](#二功能优化)
3. [问题修复](#三问题修复)
4. [接口变更](#四接口变更)
5. [兼容性说明](#五兼容性说明)
6. [其他变更](#六其他变更)

---

## 一、功能新增

### 1.1 LNN (神经逻辑网络) AI推理与训练系统

全新构建完整的LNN模型系统，涵盖模型定义、推理、训练、注册等全生命周期管理：

| 模块 | 说明 |
|------|------|
| **LNN核心引擎** (`python/app/ai/lnn/engine.py`) | 600行，实现LNN前向传播、反向传播、状态管理等核心推理逻辑 |
| **基础LNN模型** (`python/app/ai/lnn/models/base_lnn.py`) | 393行，定义抽象LNN模型基类和标准接口 |
| **PyTorch LNN模型** (`python/app/ai/lnn/models/torch_*.py`) | 新增4个PyTorch实现：`torch_base_lnn.py`(160行)、`torch_cfc_model.py`(180行)、`torch_hybrid_lnn.py`(224行)、`torch_ltc_model.py`(195行) |
| **CFC模型** (`python/app/ai/lnn/models/cfc_model.py`) | 263行，实现Closed-Form Cubic模型 |
| **Hybrid LNN** (`python/app/ai/lnn/models/hybrid_lnn.py`) | 347行，混合LNN模型实现 |
| **LTC模型** (`python/app/ai/lnn/models/ltc_model.py`) | 318行，实现Liquid Time-Constant模型 |
| **参数模型** (`python/app/ai/lnn/models/parameter_models.py`) | 54行，参数化LNN模型定义 |
| **模型注册表** (`python/app/ai/lnn/inference/registry.py`) | 482行，模型注册、版本管理、模型发现与验证 |
| **推理预测器** (`python/app/ai/lnn/inference/predictor.py`) | 380行，支持AMP加速、自动设备选择的高性能推理 |
| **批量推理** (`python/app/ai/lnn/inference/batch_inference.py`) | 206行，批量数据处理与推理 |
| **数据预处理** (`python/app/ai/lnn/preprocessing.py`) | 320行，数据清洗、标准化、特征工程 |
| **数据后处理** (`python/app/ai/lnn/postprocessing.py`) | 249行，结果解析、置信度计算、输出格式化 |
| **模型融合** (`python/app/ai/lnn/fusion.py`) | 478行，多模型结果融合与集成学习 |

### 1.2 LNN训练系统

| 模块 | 说明 |
|------|------|
| **LNN训练器** (`python/app/ai/lnn/training/trainer.py`) | 469行，支持多种优化器(Adam/SGD等)和损失函数 |
| **数据集** (`python/app/ai/lnn/training/dataset.py`) | 633行，通用LNN数据集加载与预处理 |
| **Bosch数据集** (`python/app/ai/lnn/training/bosch_dataset.py`) | 564行，Bosch CNC数据集专用加载器 |
| **评估器** (`python/app/ai/lnn/training/evaluator.py`) | 649行，模型评估指标计算(MSE/MAE/R²等) |

### 1.3 任务路由系统

| 模块 | 说明 |
|------|------|
| **任务路由器** (`python/app/ai/lnn/router/task_router.py`) | 654行，智能任务分类与模型选择 |
| **配置管理器** (`python/app/ai/lnn/config/config_manager.py`) | 674行，多层级配置管理(环境变量/配置文件/默认值) |

### 1.4 工作流编排器

| 模块 | 说明 |
|------|------|
| **工作流编排器** (`python/app/ai/lnn/workflow/workflow_orchestrator.py`) | 700行，多阶段任务编排、状态管理、异常恢复 |

### 1.5 内存优化器

| 模块 | 说明 |
|------|------|
| **内存优化器** (`python/app/ai/lnn/utils/memory_optimizer.py`) | 271行，内存监控、显存管理、内存复用优化 |

### 1.6 LNN API接口

| 接口 | 说明 |
|------|------|
| **LNN路由** (`python/app/api/v1/lnn.py`) | 497行，完整的LNN REST API |
| **磨损预测路由** (`python/app/api/v1/wear_prediction.py`) | 刀具磨损预测API |

### 1.7 LNN API端点详情

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/v1/lnn/predict` | POST | LNN模型推理预测 |
| `/api/v1/lnn/train` | POST | 启动LNN模型训练任务(支持异步、并发控制，最大3个并发任务) |
| `/api/v1/lnn/models` | GET | 列出所有已注册模型 |
| `/api/v1/lnn/models/{model_name}/info` | GET | 获取模型详细信息与验证状态 |
| `/api/v1/lnn/models/{model_name}/validate` | POST | 执行模型验证 |
| `/api/v1/lnn/health` | GET | LNN系统健康检查 |
| `/api/v1/lnn/tasks` | GET | 列出所有训练任务状态 |

### 1.8 参数智能代理(LNN增强)

| 模块 | 说明 |
|------|------|
| **参数代理** (`python/app/ai/parameter_agent_lnn.py`) | 579行，LNN驱动的参数优化建议生成 |

### 1.9 输入验证器

| 模块 | 说明 |
|------|------|
| **输入验证** (`python/app/core/input_validator.py`) | 181行，请求参数验证、注入防护、数据清洗 |

### 1.10 模型验证器

| 模块 | 说明 |
|------|------|
| **验证模块** (`python/app/models/validation.py`) | 84行，模型完整性与一致性验证 |

### 1.11 统一响应格式

| 模块 | 说明 |
|------|------|
| **响应封装** (`python/app/core/response.py`) | 统一的API响应格式(success/error封装) |

### 1.12 任务管理器

| 模块 | 说明 |
|------|------|
| **任务管理** (`python/app/core/task_manager.py`) | 27行，异步任务状态跟踪 |

### 1.13 部署基础设施

| 文件 | 说明 |
|------|------|
| **Dockerfile** (新增) | 45行，容器化构建配置 |
| **docker-compose.yml** (新增) | 101行，多服务编排(FastAPI/PostgreSQL/Redis) |
| **K8s部署配置** (`deploy/k8s/deployment.yml`) | 120行，Kubernetes Deployment/Service/ConfigMap/PVC |
| **Prometheus配置** (`deploy/prometheus/prometheus.yml`) | 24行，监控指标采集配置 |
| **告警规则** (`deploy/prometheus/alert_rules.yml`) | 47行，5条告警规则(高延迟/高错误率/内存/推理延迟/服务宕机) |
| **Grafana看板** (`deploy/grafana/`) | 16行，Grafana仪表盘自动配置 |

### 1.14 基准测试脚本

| 文件 | 说明 |
|------|------|
| **基准测试** (`python/scripts/run_benchmark.py`) | 35行，性能基准测试运行器 |
| **训练脚本** (`python/scripts/train_models.py`) | 267行，批量模型训练脚本 |

### 1.15 测试用例(LNN)

| 文件 | 说明 |
|------|------|
| **推理测试** (`python/app/ai/lnn/tests/test_lnn_inference.py`) | 785行，推理功能测试 |
| **模型单元测试** (`python/app/ai/lnn/tests/test_lnn_models_unit.py`) | 675行，各LNN模型单元测试 |
| **系统测试** (`python/app/ai/lnn/tests/test_lnn_system.py`) | 781行，LNN系统集成测试 |
| **路由器测试** (`python/app/ai/lnn/tests/test_task_router.py`) | 546行，任务路由器测试 |
| **PyTorch模型测试** (`python/app/ai/lnn/tests/test_torch_lnn_models.py`) | 505行，PyTorch LNN模型测试 |
| **基准测试** (`python/app/ai/lnn/tests/benchmark_lnn.py`) | 345行，LNN性能基准测试 |
| **参数代理测试** (`python/tests/test_parameter_agent_lnn.py`) | 367行，参数代理测试 |
| **工作流测试** (`python/tests/test_workflow_and_config.py`) | 903行，工作流编排与配置测试 |

### 1.16 LNN模型文件(预训练)

| 文件 | 说明 |
|------|------|
| `cutting_force_v1.pt` | 切削力预测模型权重 (~210KB) |
| `cutting_force_v1.torchscript.pt` | 切削力预测模型TorchScript格式 (~81KB) |
| `wear_prediction_v1.pt` | 磨损预测模型权重 (~314KB) |
| `wear_prediction_v1.torchscript.pt` | 磨损预测模型TorchScript格式 (~113KB) |

### 1.17 新文档

| 文件 | 行数 | 说明 |
|------|------|------|
| `docs/api/openapi.json` | 244行 | OpenAPI/Swagger API规范 |
| `docs/开发指南.md` | 283行 | 开发环境搭建与贡献指南 |
| `docs/用户手册.md` | 212行 | 用户使用手册 |
| `python/app/ai/lnn/ARCHITECTURE.md` | 752行 | LNN系统架构设计文档 |

### 1.18 配置文件

| 文件 | 说明 |
|------|------|
| `.env.example` | 环境变量模板(24项配置) |
| `python/config/lnn_workflow.yaml` | 71行，LNN工作流YAML配置 |

---

## 二、功能优化

### 2.1 主应用入口重构 (`python/app/main.py`)

- **新增91行**：全新的FastAPI应用入口，包含CORS中间件、请求指标追踪、Prometheus兼容的`/metrics`端点、`/api/v1/health`健康检查
- **指标追踪**：自动记录请求总数、各路径平均响应时间
- **线程安全**：使用`Lock`保护共享状态

### 2.2 LLM客户端优化 (`python/app/ai/llm_client.py`)

- **重构225行**：大幅精简和重构LLM客户端代码
- 改进错误处理和超时机制
- 优化连接池管理

### 2.3 AI代理优化 (`python/app/ai/agents.py`)

- **优化31行**：改进AI代理的请求处理和响应生成逻辑
- 增强代理间的协调机制

### 2.4 应用配置重构 (`python/app/config.py`)

- **重构85行**：采用dataclass模式，支持环境变量覆盖
- 新增环境配置分级(开发/测试/生产)
- 配置项验证与默认值管理

### 2.5 经验存储优化 (`python/app/services/experience_store.py`)

- **重构211行**：改进经验数据的存储与检索
- 优化查询性能，减少冗余数据

### 2.6 刀具磨损预测器优化 (`python/app/services/tool_wear_predictor.py`)

- **优化86行**：改进预测算法和模型加载逻辑
- 增强scikit-learn版本兼容性检测

### 2.7 前端组件优化

| 组件 | 变更 | 说明 |
|------|------|------|
| `src/App.vue` | +51/-行 | 应用主组件优化 |
| `src/views/Home.vue` | +49/-行 | 首页UI与交互优化 |
| `src/views/Workspace.vue` | +123/-行 | 工作区功能增强 |
| `src/views/Settings.vue` | +52/-行 | 设置页面优化 |
| `src/views/About.vue` | +42/-行 | 关于页面优化 |
| `src/stores/app.ts` | +19/-行 | Pinia状态管理优化 |
| `package.json` | 版本号0.1.0→1.4.0 | 版本统一 |

### 2.8 Tauri后端优化

| 模块 | 变更 | 说明 |
|------|------|------|
| `src-tauri/src/main.rs` | +32/-行 | 应用初始化与生命周期管理 |
| `src-tauri/src/database.rs` | +207/-行 | 数据库操作优化 |
| `src-tauri/src/commands/proxy.rs` | +45/-行 | 代理命令处理优化 |
| `src-tauri/src/commands/process.rs` | +40/-行 | 进程管理优化 |
| `src-tauri/src/commands/persistence.rs` | +64/-行 | 数据持久化优化 |
| `src-tauri/src/models.rs` | +11/-行 | 数据模型扩展 |
| `src-tauri/Cargo.toml` | 版本号1.2.0→1.4.0 | 版本统一 |
| `src-tauri/tauri.conf.json` | 版本号1.2.0→1.4.0 | 版本统一 |

### 2.9 RAG路由优化 (`python/app/rag/routes.py`)

- **优化10行**：改进RAG检索路由处理

### 2.10 数据Schema增强 (`python/app/models/schemas.py`)

- **新增104行**：扩展Pydantic数据模型，支持LNN请求/响应格式

---

## 三、问题修复

### 3.1 测试文件路径修复

| 测试文件 | 修复内容 |
|----------|----------|
| `test_phase_h.py` → `python/tests/test_phase_h.py` | 测试文件移动到正确目录，修复导入路径 |
| `test_process_trace.py` → `python/tests/test_process_trace.py` | 测试文件移动到正确目录，修复导入路径 |
| `test_process_trace_e2e.py` → `python/tests/test_process_trace_e2e.py` | 测试文件移动到正确目录，修复导入路径 |
| `test_traces_api.py` → `python/tests/test_traces_api.py` | 测试文件移动到正确目录 |

### 3.2 清理冗余文件

| 删除文件 | 原因 |
|----------|------|
| `build-fix.ps1` (24行) | 已被`build.ps1`替代 |
| `test_traces_api.ps1` (33行) | 已被Python测试替代 |
| `lingjing-manufacturing-backup.bundle` (~291KB) | 冗余备份文件 |

### 3.3 E2E集成测试修复 (`tests/e2e_integration.py`)

- **修复12行**：更新测试配置以适配新的API结构

### 3.4 构建脚本优化 (`build.ps1`)

- **优化7行**：修复构建流程中的路径和参数问题

---

## 四、接口变更

### 4.1 新增API端点

| 端点 | 方法 | 标签 | 说明 |
|------|------|------|------|
| `/api/v1/lnn/predict` | POST | LNN Models | LNN模型推理预测 |
| `/api/v1/lnn/train` | POST | LNN Models | LNN模型训练(异步) |
| `/api/v1/lnn/models` | GET | LNN Models | 列出已注册模型 |
| `/api/v1/lnn/models/{model_name}/info` | GET | LNN Models | 模型详情 |
| `/api/v1/lnn/models/{model_name}/validate` | POST | LNN Models | 模型验证 |
| `/api/v1/lnn/health` | GET | LNN Models | LNN系统健康检查 |
| `/api/v1/lnn/tasks` | GET | LNN Models | 训练任务列表 |
| `/api/v1/wear_prediction/*` | * | Wear Prediction | 刀具磨损预测接口 |
| `/metrics` | GET | - | Prometheus指标端点 |
| `/api/v1/health` | GET | - | 全局健康检查 |

### 4.2 数据模型变更

| 模型 | 变更 | 说明 |
|------|------|------|
| `LNNPredictRequest` | 新增 | LNN推理请求模型(含model_name, input_data, return_confidence) |
| `LNNTrainRequest` | 新增 | LNN训练请求模型(含model_name, data_path, hyperparameters) |
| `LNNModelInfo` | 新增 | 模型信息响应模型 |
| `PredictionResult` | 新增 | 预测结果模型(含value, confidence, inference_time) |

### 4.3 响应格式统一

- 所有LNN API使用统一的`success()`/`error()`响应封装
- 错误码定义在`ErrorCode`枚举中(NOT_FOUND, INVALID_REQUEST, INTERNAL_ERROR等)

### 4.4 Tauri命令变更

| 命令 | 变更 | 说明 |
|------|------|------|
| `src-tauri/src/commands/mod.rs` | -1行 | 移除已弃用的命令导出 |

### 4.5 API版本更新

| 配置项 | V1.3.0 | V1.4.0 |
|--------|--------|--------|
| `python/app/main.py` APP_VERSION | 1.3.0 | 1.4.0 |
| `python/app/config.py` 默认版本 | 1.3.0 | 1.4.0 |
| `docs/api/openapi.json` version | 1.3.0 | 1.4.0 |
| `package.json` version | 0.1.0 | 1.4.0 |
| `src-tauri/Cargo.toml` version | 1.2.0 | 1.4.0 |
| `src-tauri/tauri.conf.json` version | 1.2.0 | 1.4.0 |

---

## 五、兼容性说明

### 5.1 与V1.3.0的兼容性

| 方面 | 兼容性 | 说明 |
|------|--------|------|
| **API向后兼容** | ✅ 兼容 | 原有API端点保持不变，新增端点不冲突 |
| **数据格式** | ✅ 兼容 | Pydantic模型扩展，原有字段不变 |
| **配置文件** | ⚠️ 部分兼容 | 新增`.env.example`配置项，建议对照更新 |
| **模型文件** | ✅ 兼容 | 预训练模型为新增，不影响现有模型 |
| **Tauri桌面端** | ⚠️ 需重新构建 | Cargo.toml版本变更，需重新编译 |
| **前端** | ⚠️ 需重新构建 | package.json版本变更，需重新构建 |

### 5.2 迁移注意事项

1. **环境变量配置**：建议将`.env.example`复制为`.env`，并根据新配置项更新
2. **依赖安装**：新增多个Python依赖，建议执行`pip install -r requirements.txt`
3. **模型加载**：首次使用时LNN模型会自动从`python/models/lnn/`加载预训练权重
4. **K8s部署**：如使用Kubernetes部署，请参考`deploy/k8s/deployment.yml`更新配置
5. **监控集成**：新增Prometheus/Grafana支持，需额外部署监控组件

### 5.3 已知测试问题

| 测试文件 | 问题 | 影响 |
|----------|------|------|
| `test_phase_h.py` | 导入`app.services.incremental_solver`失败(模块不存在) | 该测试无法运行 |
| `test_process_trace.py` | 导入`app.core.process_trace`失败(模块不存在) | 该测试无法运行 |
| `test_process_trace_e2e.py` | 导入`app.core.process_trace`失败(模块不存在) | 该测试无法运行 |
| `test_rag_optimization.py` | 导入`DocumentExtractor`失败(类不存在) | 该测试无法运行 |

> **注意**：以上4个测试文件引用了当前代码库中不存在的模块，建议在后续版本中修复测试或补充对应模块。

---

## 六、其他变更

### 6.1 版本号统一

V1.4.0统一了所有组件的版本号，修复了之前各组件版本不一致的问题：

| 组件 | 原版本 | 新版本 |
|------|--------|--------|
| 前端(`package.json`) | 0.1.0 | **1.4.0** |
| Tauri(`Cargo.toml`) | 1.2.0 | **1.4.0** |
| Tauri配置(`tauri.conf.json`) | 1.2.0 | **1.4.0** |
| Python后端(`main.py`) | 1.3.0 | **1.4.0** |
| Python配置(`config.py`) | 1.3.0 | **1.4.0** |
| OpenAPI(`openapi.json`) | 1.3.0 | **1.4.0** |

### 6.2 文档更新

| 文档 | 变更 | 说明 |
|------|------|------|
| `docs/01-综合技术文档.md` | 大幅重构 | 版本号更新至1.4.0，架构图更新，API示例更新 |
| `docs/02-优化报告.md` | 更新 | 版本号与内容更新 |
| `docs/03-计算机架构报告.md` | 更新 | 版本号更新至1.4.0 |
| `docs/04-机械方向内容报告.md` | 更新 | 版本号更新 |

### 6.3 Git标签

- 创建并推送 `V1.4.0`  annotated tag
- 提交: `f726017 chore: bump version to V1.4.0`

### 6.4 统计概览

| 指标 | 数值 |
|------|------|
| 新增文件 | 约110个 |
| 修改文件 | 约25个 |
| 删除文件 | 3个 |
| 重命名文件 | 4个(测试文件迁移) |
| 新增代码行 | 21,457行 |
| 删除代码行 | 3,701行 |
| 净增代码行 | 17,756行 |
| LNN测试用例 | 约4,000行(6个测试文件) |
| 预训练模型 | 4个文件(~718KB) |

---

> **V1.4.0核心亮点**:
> 1. **完整的LNN AI系统**：从零构建完整的神经逻辑网络系统，涵盖模型定义、训练、推理、路由、编排全链路
> 2. **工业级部署支持**：Docker/K8s/Prometheus/Grafana全套生产部署方案
> 3. **统一版本管理**：消除前端/后端/Tauri版本号不一致问题
> 4. **完善的测试体系**：新增约4000行LNN专项测试代码
> 5. **预训练模型**：提供切削力预测和磨损预测两个预训练模型，支持开箱即用
