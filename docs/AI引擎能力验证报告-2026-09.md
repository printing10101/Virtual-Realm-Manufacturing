# AI 引擎能力验证报告（2026-09）

> 验证方法：三层证据——①AI 相关测试套件全量运行；②真实后端（dev 模式，权限开放）逐个驱动 AI 端点；③静态盘点（注册表/权重/依赖）。
> 验证环境：桌面运行时 Python（`desktop_runtime/runtime`，start_server.py，端口 8765），`LNN_ENV=dev`、`LNN_PERMISSION_ENFORCED=false`、验证用 `LNN_TOKEN`。
> 结论速览：**753 项 AI 测试零失败；在线实测二轮 14 项中 10 项通过；发现并修复 2 个 P1 引擎 bug；暴露 2 个环境级 P1 缺陷（chromadb 崩溃、LNN 无可用权重）。**

---

## 1. 测试证据层（Layer 1）

| 套件 | 覆盖 | 结果 |
|---|---|---|
| `tests/lnn/` | LNN 推理/流式 | ✅ |
| `tests/process_understanding/` | 任务分类/知识检索/方案质量/LLM 一致性 | ✅ |
| `tests/plugins/`（world_model/rl_agent） | 预测器/策略/价值/安全盾 | ✅ |
| `tests/integration/` | 统一嵌入端到端/闭环集成/资源占用 | ✅ |
| `tests/unit/`（rag/kg/嵌入一致性/跨层检索/颤振/LLM 韧性/主权/dreaming） | 单元 | ✅ |
| `tests/api/test_agent_gateway.py` | Agent 网关 | ✅ |
| **合计** | **753 passed / 19 skipped / 0 failed**（3m05s） | |

## 2. 在线实测层（Layer 2，真实后端）

### 2.1 能力矩阵

| # | 引擎/能力 | 端点验证 | 在线状态 | 证据摘要 |
|---|---|---|---|---|
| 1 | **LNN 推理** | /api/v1/lnn/models、/models/{n}/info、/predict | ⚠️ **有注册无权重** | 4 个模型（cutting_force/wear_prediction/surface_roughness/temperature，CFC 型）注册与元数据 API 正常；预测全部失败：`Unsupported model type: CFC`——仓库内无训练权重（models/lnn 仅有训练摘要 JSON），推理加载器不支持 CFC 类型 |
| 2 | **世界模型** | /api/v1/world-model/predict | ✅（本轮修复后） | 原始 8 维状态 + 4 维动作 → 5 步轨迹预测正常返回；修复前因服务层融合默认值崩溃（见 §3-P1-1） |
| 3 | **RL Agent + 安全盾** | /api/v1/rl-agent/act | ✅（本轮修复后） | ppo 策略、2 候选评估、安全通过；响应携带主权决策（W9.1 联动在线生效："主权策略要求人工确认后方可应用"）；修复前因 numpy/torch 输入混用崩溃（§3-P1-2） |
| 4 | **统一嵌入** | /api/v1/embedding/encode、/status | ✅ 编码/状态 | LNN 状态模态 [n,18] → 512 维融合向量正常；/index 与 /query 对批量语义的参数校验与 encode 输出形状不一致（422，见 §4-P2-3） |
| 5 | **RAG 检索** | /api/rag/query、/api/rag/stats | ⚠️ **查询可用 / stats 500** | 查询返回 13 条结果（知识库有默认数据）但 `sources=0`（增强管线返回 dict 形状未被归一化提取引用，§4-P2-2）；stats 500：chromadb Rust 绑定在桌面运行时上 panic（§4-P1-1） |
| 6 | **知识图谱** | /knowledge-graph/stats | ✅ API 正常（空库 0 节点/0 边） | 冷启动状态符合预期 |
| 7 | **工艺理解** | /health、/stats | ⚠️ health 正常 / **query 404** | 主接口路由注册疑点（见 §4-P2-4） |
| 8 | **工艺解释器** | /api/v1/process-understanding 系 | 同上（路由疑点一并核实） | — |
| 9 | **颤振预测** | /api/v1/chatter_prediction/* | ⚠️ 路由已注册，/health 404（子路径命名待确认，功能由测试层覆盖 ✅） | — |
| 10 | **工具磨损预测** | /api/v1/wear/models | ✅ Usui/Taylor 等机理模型清单正常（预测端点需输入特征，未在本轮驱动） | — |
| 11 | **SHARP 智能体** | /api/v1/sharp/status | ✅ v2.5.0，schema_planner/memory_augment/hybrid_toolset/react_loop 全部启用 | tool_registry 为空（冷启动符合预期） |
| 12 | **Dreaming** | /api/v1/dreaming/status、/learnings | ✅ 空状态（无反思数据，冷启动符合预期） | — |
| 13 | **主权策略** | GET/PUT /api/v1/governance/sovereignty | ✅ 读写闭环 + 持久化（L3→L2 切换即时生效） | — |
| 14 | **LLM 网关** | /api/v1/llm-providers 系 | ✅ API 正常，**0 个 Provider 配置**（数据主权默认形态；需用户配置本地 Ollama/云 API 后 LLM 增强路径才生效） | — |

二轮汇总：**14 项中 10 项通过**（首轮 13/24；未通过的 4 项为探测路径/载荷错误而非引擎缺陷，已在二轮修正后复核）。

## 3. 本轮发现并修复的 P1 引擎 bug

### P1-1 世界模型服务层融合默认值（影响 /predict 与 W6 预演卡）
`WorldModelService._build_world_model_config` 默认 `WORLD_MODEL_USE_FUSION=True`，但原始 /predict 路径只构造 8+4=12 维输入，与融合模式的 132 维输入层不匹配——**默认配置下所有未提供 unified_state 的预测必崩**。修复：服务默认改为 `use_fusion=False`（原始路径开箱即用），融合调用方显式设置环境变量。回归：world_model 套件 166 通过。

### P1-2 RL 策略推理 numpy/torch 混用（影响 /rl-agent/act 全部决策）
`RLAgentService.act` 把 numpy 状态数组直接传给 torch 版 PolicyNet/ValueNet → `linear(): argument 'input' must be Tensor` 崩溃。修复：新增 `_to_net_input`（torch 实现转 float32 Tensor，NumPy 回退实现原样传入）+ `_set_inference_mode`（nn.Module 默认 training=True 会让推理走随机采样分支，切 eval 保证确定性）。回归：闭环集成 56 通过。

### 顺带修正
`net.py` 的 `use_fusion` docstring 失真（写"默认 False"实为 True）。

## 4. 未修复的真实缺口

| 编号 | 缺口 | 影响 | 建议 |
|---|---|---|---|
| **P1-3** | **chromadb Rust 绑定在桌面运行时 panic**（`PersistentClient` 即崩，`pyo3_runtime.PanicException`） | RAG 向量库无法初始化：/api/rag/stats 500；RAG 查询当前依赖回退存储 | P1：锁定 chromadb 版本或升级/替换向量库；需在桌面运行时环境中专项排查 |
| **P1-4** | **LNN 无可用训练权重**（models/lnn 仅有训练摘要 JSON；推理加载器不支持 CFC 类型） | LNN 在线预测对注册表全部 4 个模型不可用——"753 测试全绿"与"在线预测可用"之间存在训练侧产出断层 | P1：research/ 侧产出可部署权重（.pt/.onnx）并入库；或推理侧补 CFC 加载支持 |
| P2-1 | LLM 网关 0 Provider | LLM 增强路径（工艺理解 LLM 模式/dreaming LLM 反思）默认降级为规则模式 | 属数据主权默认形态；首次运行引导（W10.2 后续）可加 Ollama 检测引导 |
| P2-2 | RAG 增强管线引用溯源 sources=0 | 知识建议不带出处（W8.2 只覆盖了管线返回 list 的形状） | P2：`_normalize_rag_items` 补 dict 形状（enhanced 实际返回字典）解包 |
| P2-3 | 统一嵌入 /index、/query 与 /encode 输出形状不一致 | 批量语义对不上（encode 返回单条 fused），检索往返无法直接串联 | P2：对齐批量契约（归并行会话的 embedding 面演进） |
| P2-4 | 工艺理解主接口 /query 404；颤振 /health 404 | 路由注册与前缀疑点 | P2：核实注册前缀与文档路径 |
| P3 | 知识图谱/飞轮/dreaming 均为空库冷启动 | 能力可用但无数据积累 | 随使用自然填充；飞轮充能见路线图 W4 |

## 5. 综合评估结论

1. **代码质量可信**：753 项 AI 测试零失败，且多数能力（WM/RL/嵌入/主权/dreaming/知识图谱/磨损/SHARP）在线实测与测试结论一致。
2. **"最后的十米"是断层所在**：两处 P1（chromadb 环境、LNN 权重断层）都不在 AI 算法本身，而在**运行环境兼容性**与**训练→部署的产出链**上——这正是路线图"信任资产"主线要补的部分。
3. **本轮两个 P1 修复的价值**：世界模型预测与 RL 决策从"线上必崩"恢复为"在线可用"，且 W6 预演卡、W9.1 主权联动在真实服务上得到端到端验证（RL 响应中实际携带主权决策字段）。
4. **对外宣称纪律**（对照 1.7 表）：RAG 检索、世界模型预测、RL 决策、主权策略可宣称"已在线验证"；LNN 在线预测、RAG 向量库在缺陷修复前**不得宣称生产可用**。

> 复现：① 测试层 `pytest tests/lnn tests/plugins tests/integration ...`；② 在线层启动 `desktop_runtime/runtime/python.exe start_server.py`（dev 模式）后按 §2.1 端点逐个驱动。
