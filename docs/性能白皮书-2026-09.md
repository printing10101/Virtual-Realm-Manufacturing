# 性能白皮书（2026-09）

> 定位：把七类性能基准的实测数据成文，作为对外宣称的证据底座（对应《优化升级路线图》W9.4 与《产品叙事与战略对标-2026-09.md》§4.5）。
> **使用纪律**：本报告数据来自单台开发机的自动化基准，**不是生产环境承诺**。对外引用必须带环境限定语（"在开发机实测"），禁止以"可达 XX 吞吐"作无条件宣称；机床相关能力一律按《postprocessor-验证矩阵》L1-L4 分级表述。

---

## 1. 测试环境与方法

| 项 | 值 |
|---|---|
| 运行时间 | 2026-09-07 03:19–03:21（本地） |
| 解释器 | 系统 Python 3.14（工程运行环境，ONNX Runtime 推理路径，无 torch） |
| 运行器 | `app/benchmarks/performance/run_perf_benchmark.py`（W9.4 起支持按套件容错：单套件失败不拖垮全部，失败清单随结果 JSON 留痕） |
| 复现命令 | `python -m app.benchmarks.performance.run_perf_benchmark` |
| 数据留存 | 结果 JSON 每次运行生成于 `app/benchmarks/performance/history/`（已 gitignore，不入库）；本报告数字即为 2026-09-07 那次运行的记录 |

## 2. 结果总览

本轮 10 个套件中 **9 个成功**，1 个失败（世界模型套件，已知缺陷，见 §4）。API 套件有运行但未启动后端服务，无有效数据（见 §4）。

| 能力域 | 关键指标 | 实测值 |
|---|---|---|
| **CAM 全流程**（图纸→NC） | 全流程总耗时（3 件） | **0.355 s**（均件 0.118 s） |
| | 图纸解析 p50 / 三维重建 / 工艺规划 | 0.029 s / 0.160 s / 0.067 s |
| | 刀轨生成 / NC 后处理 | 0.023 s / 0.008 s |
| **LNN 推理**（合成模型基准①） | 单次推理 p50 / p95 | 1.26 ms / 1.51 ms |
| | 批量吞吐（10/50/100 批） | 7.5k → 27.8k → 43.9k samples/s |
| **RL 决策 + 安全盾** | 单次决策 p50 / p95 | 2.03 ms / 3.10 ms |
| | SafetyShield 过滤（strict / non-strict p50） | 0.057 ms / 0.030 ms（strict 开销 +81%） |
| | 批量决策吞吐（100 批） | 1,410 decisions/s |
| | 策略缓存命中加速 | 18.2× |
| | 1,000 随机样本违规拦截率 | **100%**（边界 2.0% + 变化率 3.6%，全部回退安全动作） |
| **闭环加工优化工作流** | 端到端 p50 / 均值 | 12.5 ms / 14.3 ms |
| | 稳态吞吐 | 70.9 loops/s |
| | 阶段占比 | 预测 76.6%（瓶颈）；感知 1.6%；决策 15.2%；**CAM 校验 1.8%**；执行 2.2% |
| **并发与压力** | asyncio 任务吞吐（100 任务） | 11,630 rps |
| | 持续压测（3 s） | 36,856 rps，0 错误，成功率 100% |
| | 内存压力测试（50 轮） | 均值 12.7 ms，对象增长仅 +2（无泄漏迹象） |
| **数据库**（SQLite） | 简单查询 p50 / 索引查询 p50 | 0.022 ms / 0.192 ms |
| | 批量插入吞吐 | 69,315 rows/s |

## 3. 值得对外讲的三个数字（带限定语）

1. **"图纸进、代码出"全流程秒级**：单件 0.118 s（开发机实测），AI 起草不是等待瓶颈。
2. **"敢上机"几乎免费**：闭环工作流中 CAM 物理校验仅占周期 **1.8%**（0.26 ms）——安全闸门不构成性能代价。
3. **安全盾 100% 拦截 + 微秒级开销**：1,000 个随机越界/超速动作 100% 被拦截回退，strict 模式单次开销 0.057 ms。

## 4. 已知缺口（诚实清单）

| 缺口 | 影响 | 状态 |
|---|---|---|
| 世界模型基准套件崩溃 | 输入维度不匹配（合成状态 12 维 vs 模型 input_size 132），预演卡（W6）不受影响（走服务层真实 predictor 配置） | ⬜ 待修（bench 自身缺陷，已记录） |
| API 基准无有效数据 | 需本地运行中的后端服务（localhost:8000），本轮未启动 | ⬜ 白皮书 v2 需启动服务后补测 |
| 业务逻辑基准跳过 | business_logic_bench 已迁移 research/ | 按设计跳过 |
| LNN 套件为合成模型基准① | 测量的是投影计算吞吐，非真实 LTC 权重推理 | 对外表述必须注明 |
| 单机环境 | 所有数字为开发机实测，非生产 SLA | 对外必须带限定语 |

## 5. 维护约定

- 每次对外材料引用性能数字前，重跑基准并更新本报告日期与数据；
- 新增基准套件必须登记进 §2 表格并补充复现命令；
- 回归检测阈值（warning/critical 百分比）见 `app/benchmarks/performance/thresholds.py`。
