# 灵境制造 V4 计算机架构报告

> **文档版本**: v1.0  
> **评估日期**: 2026-04-29  
> **评估视角**: 计算机体系结构、硬件环境适配、系统集成、分布式部署  
> **适用对象**: 系统架构师、DevOps 工程师、基础设施团队

---

## 目录

1. [硬件环境分析](#一硬件环境分析)
2. [软件架构设计](#二软件架构设计)
3. [进程与内存管理](#三进程与内存管理)
4. [I/O 与存储架构](#四io-与存储架构)
5. [网络通信架构](#五网络通信架构)
6. [安全架构](#六安全架构)
7. [系统集成方案](#七系统集成方案)
8. [分布式部署策略](#八分布式部署策略)
9. [跨平台适配方案](#九跨平台适配方案)
10. [硬件加速策略](#十硬件加速策略)

---

## 一、硬件环境分析

### 1.1 目标硬件平台

| 硬件维度 | 最低配置 | 推荐配置 | 说明 |
|---------|---------|---------|------|
| **CPU** | x86_64 4 核 | x86_64 8 核+ | AI 推理密集，多核有益 |
| **内存** | 8 GB | 16 GB+ | Ollama 7B 模型需 ~4GB |
| **存储** | 10 GB SSD | 20 GB NVMe SSD | 模型 + 依赖 + 生成文件 |
| **GPU** | 无（CPU 推理） | NVIDIA RTX 3060+ | GPU 推理加速 |
| **显卡** | 支持 WebGL 2.0 | 支持 Vulkan | Three.js 3D 渲染 |
| **网络** | 无（本地模式） | 10 Mbps+ | 云端 API / 自动更新 |

### 1.2 硬件资源需求分析

#### 1.2.1 CPU 架构适配

```
x86_64 (Windows)    ✅ 完整支持
ARM64 (macOS)       ⚠️ 部分支持（Rust 可编译，Python 依赖需验证）
ARM64 (Linux)       ⚠️ 部分支持
```

**关键依赖的架构兼容性**:

| 依赖 | x86_64 | ARM64 | 说明 |
|------|--------|-------|------|
| Tauri 2 | ✅ | ✅ | 支持多架构编译 |
| Rust toolchain | ✅ | ✅ | cross-compilation 支持 |
| Python 3.11+ | ✅ | ✅ | 官方支持 |
| Ollama | ✅ | ✅ | 提供 ARM64 版本 |
| ChromaDB | ✅ | ⚠️ | hnswlib 需编译 |
| CadQuery | ✅ | ⚠️ | OCC 库需验证 |
| rusqlite (bundled) | ✅ | ✅ | SQLite 自带源码编译 |

#### 1.2.2 内存需求分层

| 运行模式 | 最小内存 | 推荐内存 | 说明 |
|---------|---------|---------|------|
| 纯 UI 模式 | ~300 MB | 500 MB | 无 AI 功能，仅查看和编辑 |
| 本地 LLM 模式 (3B) | ~3 GB | 4 GB | Ollama 加载 3B 模型 |
| 本地 LLM 模式 (7B) | ~5 GB | 8 GB | Ollama 加载 7B 模型 |
| 本地 LLM 模式 (14B+) | ~10 GB | 16 GB | 大模型需要更多内存 |
| 云端 LLM 模式 | ~500 MB | 1 GB | 本地无模型，走云端 API |

#### 1.2.3 GPU 加速分析

**推理加速**:

| GPU 类型 | 加速效果 | 适用场景 |
|---------|---------|---------|
| NVIDIA (CUDA) | 10-50x | 所有模型推理 |
| AMD (ROCm) | 5-20x | 部分模型支持 |
| Apple Silicon (Metal) | 5-15x | macOS 平台 |
| Intel (OpenVINO) | 3-10x | 集成显卡优化 |
| 纯 CPU | 基准 | 无 GPU 环境 |

**渲染加速**:

| GPU 特性 | 要求 | 说明 |
|---------|------|------|
| WebGL 2.0 | 最低 | Three.js 基础渲染 |
| WebGPU | 推荐 | 下一代渲染 API |
| Vulkan | 可选 | 原生高性能渲染 |

### 1.3 硬件环境检测

```rust
#[tauri::command]
pub fn get_app_info(app_handle: tauri::AppHandle) -> Result<AppInfo, String> {
    Ok(AppInfo {
        app_name: "灵境制造".to_string(),
        version: app_handle.package_info().version.to_string(),
        tauri_version: "2.0".to_string(),
        os: std::env::consts::OS.to_string(),
        os_version: os_info::get().version().to_string(),
        arch: std::env::consts::ARCH.to_string(),
        hostname: gethostname::gethostname()
            .to_string_lossy()
            .to_string(),
    })
}
```

---

## 二、软件架构设计

### 2.1 四层架构模型

```
┌─────────────────────────────────────────────────────┐
│              表示层 (Presentation Layer)              │
│  Vue 3 SPA (WebView2 / WebKit)                       │
│  ├── 用户界面渲染                                    │
│  ├── 事件处理与交互                                   │
│  └── 状态管理 (Pinia)                                │
├─────────────────────────────────────────────────────┤
│              系统层 (System Layer)                    │
│  Rust / Tauri Core                                   │
│  ├── 系统 API 调用 (文件、进程、网络)                  │
│  ├── 安全沙箱 (CSP、权限控制)                         │
│  └── 数据持久化 (SQLite、密钥链)                      │
├─────────────────────────────────────────────────────┤
│              服务层 (Service Layer)                   │
│  Python FastAPI Sidecar                              │
│  ├── AI 推理编排 (六 Agent)                           │
│  ├── CAD 引擎 (CadQuery)                             │
│  └── 知识库管理 (ChromaDB)                           │
├─────────────────────────────────────────────────────┤
│              推理层 (Inference Layer)                 │
│  Ollama / 云端 API                                   │
│  ├── 本地 LLM 推理                                   │
│  └── 云端 API 兼容接口                                │
└─────────────────────────────────────────────────────┘
```

### 2.2 进程模型

```
┌─────────────────────────────────────────┐
│          主进程 (Tauri/Rust)             │
│  ├── 主线程: 事件循环                     │
│  ├── 线程池: 文件 I/O                    │
│  └── 线程池: 数据库操作                   │
├─────────────────────────────────────────┤
│      渲染进程 (WebView2)                 │
│  ├── 主线程: UI 渲染                     │
│  ├── Worker: JavaScript 计算             │
│  └── GPU 进程: WebGL 渲染                │
├─────────────────────────────────────────┤
│      子进程 (Python FastAPI)             │
│  ├── 主线程: Uvicorn 事件循环             │
│  ├── Worker: 异步任务处理                 │
│  └── 子进程: ChromaDB                   │
├─────────────────────────────────────────┤
│      子进程 (Ollama)                     │
│  ├── 主线程: HTTP 服务器                  │
│  └── GPU/CPU: 模型推理                   │
└─────────────────────────────────────────┘
```

### 2.3 线程模型

#### 2.3.1 Rust 线程模型

```rust
// 主线程: Tauri 事件循环
fn main() {
    tauri::Builder::default()
        .setup(|app| {
            // 初始化代码在主线程执行
            Ok(())
        })
        .invoke_handler(tauri::generate_handler![...])
        .run(...);
}

// Tauri 命令默认在线程池执行
#[tauri::command]
pub async fn some_command() -> Result<String, String> {
    // 异步命令在 Tokio 运行时执行
    tokio::spawn(async { ... }).await
}
```

#### 2.3.2 Python 线程模型

```python
# Uvicorn 异步事件循环
# 所有 async def 在事件循环中执行
# 同步操作在线程池中执行
@app.post("/api/workflow/process-plan")
async def process_plan(request: ProcessPlanRequest):
    # 在事件循环中执行
    result = await orchestrator.execute_workflow(request.user_input)
    return success(data=result)
```

### 2.4 内存管理架构

#### 2.4.1 内存分层

| 层级 | 内存管理方式 | 说明 |
|------|------------|------|
| Rust | 所有权系统 (borrow checker) | 编译期保证内存安全，无 GC |
| Python | 引用计数 + GC | 自动垃圾回收 |
| JavaScript | V8 GC | 分代垃圾回收 |
| WebView2 | 浏览器内存管理 | 独立进程，可限制内存 |

#### 2.4.2 内存安全保证

```
┌─────────────────────────────────────┐
│  Rust 层: 编译期内存安全               │
│  ├── 所有权检查                       │
│  ├── 生命周期检查                     │
│  └── 借用检查                        │
├─────────────────────────────────────┤
│  Python 层: 运行时内存安全             │
│  ├── 引用计数                         │
│  ├── 循环 GC                         │
│  └── 异常安全                         │
├─────────────────────────────────────┤
│  JS 层: 浏览器内存管理                │
│  ├── V8 GC                           │
│  ├── 手动释放 (Three.js dispose)      │
│  └── 组件生命周期管理                  │
└─────────────────────────────────────┘
```

---

## 三、进程与内存管理

### 3.1 进程生命周期管理

```
启动流程:
1. Tauri 主进程启动
2. 创建应用数据目录
3. 初始化 SQLite 数据库
4. 启动 WebView2 渲染进程
5. (可选) 启动 Python Sidecar
6. (可选) 启动 Ollama

关闭流程:
1. 保存所有未保存数据
2. 停止 Python Sidecar
3. (可选) 停止 Ollama
4. 关闭数据库连接
5. 关闭 WebView2
6. 退出主进程
```

#### 3.1.1 进程状态机

```rust
#[derive(Debug)]
pub struct AppState {
    pub sidecar_pid: AtomicU32,
    pub sidecar_running: AtomicU32,
    pub sidecar_port: AtomicU32,
    pub sidecar_healthy: AtomicBool,
    pub sidecar_last_check: Mutex<Option<String>>,
}

// 状态转换:
// STOPPED -> STARTING -> RUNNING -> HEALTHY
// RUNNING -> UNHEALTHY -> STOPPED
// 任何状态 -> STOPPED (异常退出)
```

#### 3.1.2 健康检查机制

| 检查类型 | 方法 | 频率 | 超时 |
|---------|------|------|------|
| 进程存活 | `tasklist` (Win) / `kill -0` (Unix) | 按需 | 无 |
| HTTP 健康检查 | TCP 连接 `/health` | 按需 | 2 秒 |
| LLM 可用性 | `POST /api/chat` | 按需 | 30 秒 |

### 3.2 内存优化策略

#### 3.2.1 Rust 内存优化

```rust
// 使用 Mutex<Connection> 管理 SQLite 连接
pub struct Database {
    conn: Mutex<Connection>,  // 单个连接，互斥访问
}

// 优化方案：使用连接池
// pub struct Database {
//     pool: r2d2::Pool<SqliteConnectionManager>,
// }
```

#### 3.2.2 Three.js 内存管理

```typescript
// 组件卸载时完整释放资源
onUnmounted(() => {
  // 1. 停止动画循环
  if (animationId) cancelAnimationFrame(animationId)
  
  // 2. 释放模型 geometry 和 material
  if (modelMesh) {
    modelMesh.traverse((child) => {
      if (child instanceof THREE.Mesh) {
        child.geometry?.dispose()
        if (Array.isArray(child.material)) {
          child.material.forEach(m => m.dispose())
        } else {
          child.material?.dispose()
        }
      }
    })
    scene.remove(modelMesh)
  }
  
  // 3. 释放 WebGL 上下文
  if (renderer) {
    renderer.dispose()
    renderer.forceContextLoss()
    if (renderer.domElement.parentNode) {
      renderer.domElement.parentNode.removeChild(renderer.domElement)
    }
  }
  
  // 4. 移除事件监听
  window.removeEventListener('resize', onWindowResize)
})
```

#### 3.2.3 Python 内存优化

```python
# ChromaDB 内存管理
# ChromaDB 默认将所有向量加载到内存
# 对于大量知识条目，可考虑：

# 方案 1: 限制知识库大小
MAX_KNOWLEDGE_ENTRIES = 1000

# 方案 2: 使用持久化模式，按需加载
client = chromadb.PersistentClient(path="./chroma_db")

# 方案 3: 定期清理无用集合
def cleanup_old_knowledge():
    # 删除过期或冗余的知识条目
    pass
```

---

## 四、I/O 与存储架构

### 4.1 存储层次

```
┌─────────────────────────────────────────────┐
│           存储层次结构                        │
├─────────────────────────────────────────────┤
│ L1: CPU 缓存 (L1/L2/L3)                     │
│     ↓                                       │
│ L2: 内存 (RAM)                               │
│     ├── 运行时数据                           │
│     └── 缓存数据                             │
│     ↓                                       │
│ L3: SQLite 数据库 (lingjing.db)              │
│     ├── 应用设置 (key-value)                 │
│     └── 项目元数据 (projects)                │
│     ↓                                       │
│ L4: 文件系统                                 │
│     ├── JSON 备份文件 (.json.bak)            │
│     ├── 生成模型 (STL/OBJ/GLTF)              │
│     ├── 密钥链 (系统级)                      │
│     └── ChromaDB 持久化数据                  │
│     ↓                                       │
│ L5: 网络存储 (可选)                          │
│     └── 云端 API (仅非敏感数据)               │
└─────────────────────────────────────────────┘
```

### 4.2 数据库架构

#### 4.2.1 SQLite 表结构

```sql
-- 设置表 (key-value 存储)
CREATE TABLE IF NOT EXISTS settings (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL
);

-- 项目表
CREATE TABLE IF NOT EXISTS projects (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    description TEXT NOT NULL DEFAULT '',
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL,
    status TEXT NOT NULL DEFAULT 'active',
    model_path TEXT NOT NULL DEFAULT '',
    nc_program_path TEXT NOT NULL DEFAULT ''
);

-- 建议的索引优化
CREATE INDEX idx_projects_status ON projects(status);
CREATE INDEX idx_projects_created_at ON projects(created_at DESC);
CREATE INDEX idx_projects_name ON projects(name);
```

#### 4.2.2 并发控制

```rust
// 当前实现: 单连接 + Mutex
pub struct Database {
    conn: Mutex<Connection>,
}
// 所有操作串行执行，无并发

// 优化方案: 连接池
// pub struct Database {
//     pool: r2d2::Pool<SqliteConnectionManager>,
// }
// 支持多连接并发读取
```

#### 4.2.3 数据迁移架构

```
JSON → SQLite 迁移流程:
1. 应用启动时检测 SQLite 数据库
2. 如果数据库不存在，创建新数据库
3. 检测 JSON 备份文件是否存在
4. 如果存在，读取 JSON 数据
5. 写入 SQLite 数据库
6. 写入成功，重命名 JSON 为 .json.bak
7. 写入失败，保留原 JSON 文件
8. 后续操作使用 SQLite
9. SQLite 不可用时降级到 JSON
```

### 4.3 文件系统架构

#### 4.3.1 目录结构

```
%APPDATA%/lingjing/
├── lingjing.db              # SQLite 数据库
├── lingjing.db.bak          # 数据库备份
├── settings.json.bak        # 设置备份 (迁移后)
├── projects.json.bak        # 项目备份 (迁移后)
├── cloud_api_settings.json  # 云 API 配置 (不含密钥)
├── models/                  # 生成的 3D 模型
│   ├── e097cd83-xxxx.stl
│   └── ...
├── chroma_db/               # ChromaDB 持久化数据
│   └── ...
└── logs/                    # 日志文件 (可选)
    └── app.log
```

#### 4.3.2 路径安全模型

```
用户输入路径
    ↓
sanitize_path(user_path, base_dir)
    ├── 检测 ".." 遍历标记 → 拒绝
    ├── 自动创建父级目录
    └── 验证 starts_with(base_dir) → 拒绝
    ↓
安全路径 → 执行文件操作
```

### 4.4 I/O 性能优化

#### 4.4.1 异步 I/O

```python
# Python: 使用 aiofiles 进行异步文件操作
import aiofiles

async def read_file_async(file_path: str) -> str:
    async with aiofiles.open(file_path, mode='r') as f:
        return await f.read()
```

```rust
// Rust: Tokio 异步运行时
#[tauri::command]
pub async fn some_command() -> Result<String, String> {
    // 异步 I/O 操作
    tokio::fs::read_to_string(&path).await
        .map_err(|e| e.to_string())
}
```

#### 4.4.2 缓存策略

| 数据类型 | 缓存策略 | 说明 |
|---------|---------|------|
| 应用设置 | 内存缓存 | 启动时加载，修改时更新 |
| 项目列表 | 内存缓存 | 启动时加载，CRUD 时更新 |
| 知识库检索 | 查询缓存 | 相同查询返回缓存结果 |
| 3D 模型 | 浏览器缓存 | HTTP 缓存头控制 |

---

## 五、网络通信架构

### 5.1 通信协议栈

```
┌─────────────────────────────────────────────┐
│ 前端 ←→ Tauri: IPC (命令调用)                │
│  ├── invoke() / emit()                      │
│  └── JSON 序列化                             │
├─────────────────────────────────────────────┤
│ Tauri ←→ Python: HTTP (RESTful API)          │
│  ├── GET/POST/PUT/DELETE                    │
│  ├── JSON 请求/响应体                        │
│  └── SSE (流式推送，如模型下载进度)           │
├─────────────────────────────────────────────┤
│ Python ←→ Ollama: HTTP (Ollama API)          │
│  ├── POST /api/chat                         │
│  ├── POST /api/pull                         │
│  └── GET /api/tags                          │
├─────────────────────────────────────────────┤
│ Python ←→ 云端 API: HTTP (OpenAI 兼容)       │
│  ├── POST /v1/chat/completions              │
│  ├── Bearer Token 认证                       │
│  └── SSE 流式响应                           │
└─────────────────────────────────────────────┘
```

### 5.2 网络拓扑

```
[用户桌面环境]
    │
    ├── localhost:1420      ← WebView2 前端
    ├── localhost:8765      ← Python FastAPI
    ├── localhost:11434     ← Ollama
    │
    ├── 互联网
    │   ├── https://api.openai.com    (云端 API, 可选)
    │   ├── https://ollama.ai         (模型下载, 可选)
    │   └── https://update-server     (自动更新, 可选)
    │
    └── 本地网络
        └── 文件共享 (可选)
```

### 5.3 通信安全

| 通信路径 | 加密方式 | 认证方式 | 说明 |
|---------|---------|---------|------|
| 前端 ←→ Tauri | 进程内通信 | 进程隔离 | WebView2 沙箱 |
| Tauri ←→ Python | 无 (localhost) | 来源限制 (CORS) | 仅允许 Tauri 地址 |
| Python ←→ Ollama | 无 (localhost) | 无 | 本地服务 |
| Python ←→ 云端 | TLS 1.3 | Bearer Token | 加密传输 |

### 5.4 SSE 流式推送

```python
# Python: SSE 流式推送模型下载进度
from fastapi.responses import StreamingResponse

async def pull_model_stream(model_name: str):
    async def event_stream():
        async for progress in ollama_manager.pull_model(model_name):
            yield f"data: {json.dumps(progress)}\n\n"
    
    return StreamingResponse(
        event_stream(),
        media_type="text/event-stream"
    )
```

```typescript
// 前端: 接收 SSE 流式数据
const response = await fetch(`/api/ollama/models/pull/${modelName}`)
const reader = response.body.getReader()
while (true) {
  const { done, value } = await reader.read()
  if (done) break
  const text = new TextDecoder().decode(value)
  // 解析 SSE 数据
}
```

---

## 六、安全架构

### 6.1 安全分层

```
┌─────────────────────────────────────────────┐
│           安全分层架构                        │
├─────────────────────────────────────────────┤
│ 网络层: CORS 策略 + CSP 策略                 │
│  ├── 仅允许 Tauri 本地地址访问 API            │
│  └── 限制脚本、样式、图片来源                 │
├─────────────────────────────────────────────┤
│ 系统层: 路径白名单 + 系统密钥链               │
│  ├── 文件操作限制在应用目录内                 │
│  └── API Key 存储在 Windows Credential Manager│
├─────────────────────────────────────────────┤
│ 应用层: 数据分类 + 路由策略                   │
│  ├── 敏感操作强制本地推理                     │
│  └── 云端调用需用户确认                       │
├─────────────────────────────────────────────┤
│ 数据层: 本地存储 + 无遥测                     │
│  ├── 所有数据存储在本地                       │
│  └── 不收集用户行为数据                       │
└─────────────────────────────────────────────┘
```

### 6.2 Tauri 权限模型

```json
// src-tauri/capabilities/default.json
{
  "identifier": "default",
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
```

### 6.3 密钥管理架构

```
┌─────────────────────────────────────┐
│  密钥管理架构                         │
├─────────────────────────────────────┤
│  API Key → Windows Credential Manager│
│  ├── 系统级加密存储                  │
│  ├── 仅当前用户可访问                │
│  └── 应用无法直接读取原始密钥         │
├─────────────────────────────────────┤
│  其他配置 → JSON 文件                │
│  ├── 不含敏感信息                    │
│  └── 存储在应用数据目录              │
└─────────────────────────────────────┘
```

### 6.4 沙箱隔离

```
WebView2 沙箱:
├── 无法访问本地文件系统 (需通过 Tauri 命令)
├── 无法启动外部进程 (需通过 Tauri 命令)
├── 网络请求受 CSP 策略限制
└── 本地存储受同源策略保护
```

---

## 七、系统集成方案

### 7.1 Windows 集成

#### 7.1.1 系统要求

| 组件 | 要求 | 说明 |
|------|------|------|
| 操作系统 | Windows 10 1809+ | WebView2 运行时 |
| WebView2 | 内置或下载 | Windows 11 已内置 |
| MSVC 运行时 | 需要 | Rust 编译目标 |
| .NET | 不需要 | 纯原生应用 |

#### 7.1.2 注册表集成

```
安装后注册:
├── 文件关联 (.stl, .obj, .gltf)
├── 协议处理 (lingjing://)
├── 右键菜单集成
└── 开始菜单快捷方式
```

#### 7.1.3 系统集成

```rust
// Windows 系统集成
#[cfg(windows)]
pub fn register_file_association(extension: &str) -> Result<(), String> {
    // 注册文件关联
    // 设置默认打开程序
}
```

### 7.2 Ollama 集成

#### 7.2.1 安装检测

```python
class OllamaManager:
    async def is_available(self) -> bool:
        try:
            async with httpx.AsyncClient() as client:
                response = await client.get(f"{self.base_url}/api/tags", timeout=5.0)
                return response.status_code == 200
        except Exception:
            return False
    
    async def get_version(self) -> Optional[str]:
        # 获取 Ollama 版本
        pass
```

#### 7.2.2 模型管理

| 操作 | API | 说明 |
|------|-----|------|
| 列出模型 | `GET /api/tags` | 已安装模型列表 |
| 拉取模型 | `POST /api/pull` | SSE 流式进度 |
| 删除模型 | `DELETE /api/delete` | 释放磁盘空间 |
| 模型信息 | `POST /api/show` | 模型详情 |
| GPU 信息 | `/api/ollama/gpu-info` | GPU 加速支持 |

### 7.3 第三方服务集成

#### 7.3.1 云端 LLM 集成

```python
class CloudLLMClient(BaseLLMClient):
    def __init__(self, api_key: str, base_url: str, model: str):
        self.client = httpx.AsyncClient(
            base_url=base_url,
            headers={"Authorization": f"Bearer {api_key}"}
        )
        self.model = model
    
    async def chat_completion(self, messages: list, **kwargs) -> dict:
        response = await self.client.post(
            "/v1/chat/completions",
            json={"model": self.model, "messages": messages, **kwargs}
        )
        return response.json()
```

**支持的云端服务**:

| 服务 | 兼容 API | 说明 |
|------|---------|------|
| OpenAI | OpenAI API | GPT-3.5/4 |
| DeepSeek | OpenAI 兼容 | deepseek-chat |
| 其他 | OpenAI 兼容 | 任何兼容服务 |

---

## 八、分布式部署策略

### 8.1 部署架构

```
虽然灵境制造是桌面应用，但在以下场景可能涉及分布式部署：

┌─────────────────────────────────────────────┐
│  分布式部署场景                              │
├─────────────────────────────────────────────┤
│ 1. 企业内网部署:                              │
│    ├── 集中式模型服务器                      │
│    ├── 多客户端共享 LLM                      │
│    └── 统一知识库管理                        │
├─────────────────────────────────────────────┤
│ 2. 云端 API 部署:                             │
│    ├── Python 后端部署到云服务器               │
│    ├── 多客户端共享后端服务                   │
│    └── 负载均衡 + 自动扩展                    │
├─────────────────────────────────────────────┤
│ 3. 混合部署:                                  │
│    ├── 本地推理 + 云端推理混合                 │
│    ├── 本地优先，云端补充                      │
│    └── 离线降级策略                          │
└─────────────────────────────────────────────┘
```

### 8.2 企业内网部署方案

```
[企业内网架构]

                    ┌──────────────┐
                    │  模型服务器   │
                    │  (Ollama)    │
                    └──────┬───────┘
                           │
              ┌────────────┼────────────┐
              │            │            │
        ┌─────┴─────┐ ┌───┴────┐ ┌────┴─────┐
        │ 客户端 A   │ │客户端 B │ │ 客户端 C  │
        │ (Tauri)   │ │(Tauri) │ │ (Tauri)  │
        └───────────┘ └────────┘ └──────────┘
```

**配置方案**:

```typescript
// settingsStore - 配置内网 Ollama 地址
const settings = {
  ollama_url: "http://model-server:11434",
  python_backend_url: "http://localhost:8765",  // 本地
}
```

### 8.3 自动更新架构

```
[自动更新流程]

客户端                     更新服务器
  │                           │
  ├── 检查更新请求 ──────────> │
  │                           │
  │ <── 更新清单 (JSON) ──────┤
  │     ├── 版本号             │
  │     ├── 下载 URL           │
  │     └── 签名               │
  │                           │
  ├── 下载更新包 ───────────> │
  │                           │
  ├── 验证签名                │
  ├── 安装更新                │
  └── 重启应用                │
```

```json
// tauri.conf.json
{
  "plugins": {
    "updater": {
      "active": true,
      "endpoints": ["https://your-update-server.com/api/updater"],
      "pubkey": "your-public-key-here"
    }
  }
}
```

### 8.4 负载均衡方案（云端部署）

```
[云端部署架构]

用户 ──> 负载均衡器 ──> [Python FastAPI 实例 1]
                        [Python FastAPI 实例 2]
                        [Python FastAPI 实例 3]
                               │
                        ┌──────┴──────┐
                        │  Ollama 集群 │
                        │  (多 GPU)    │
                        └─────────────┘
```

---

## 九、跨平台适配方案

### 9.1 平台差异分析

| 维度 | Windows | macOS | Linux |
|------|---------|-------|-------|
| 桌面框架 | Tauri 2 ✅ | Tauri 2 ✅ | Tauri 2 ✅ |
| WebView | WebView2 | WebKit | WebKitGTK |
| 系统密钥链 | Windows Credential Manager | Keychain | libsecret |
| 进程管理 | taskkill | kill | kill |
| 文件路径 | `\` 分隔符 | `/` 分隔符 | `/` 分隔符 |
| 打包格式 | .exe, .msix | .app, .dmg | .AppImage, .deb |

### 9.2 跨平台适配代码

```rust
// 进程管理跨平台适配
fn is_process_alive(pid: u32) -> bool {
    #[cfg(windows)]
    {
        // Windows: 使用 tasklist
        let output = Command::new("tasklist")
            .args(["/FI", &format!("PID eq {}", pid), "/NH"])
            .output();
        // ...
    }
    
    #[cfg(unix)]
    {
        // Unix: 使用 kill -0
        Command::new("kill")
            .args(["-0", &pid.to_string()])
            .output()
            .map(|o| o.status.success())
            .unwrap_or(false)
    }
}
```

```rust
// 密钥链跨平台适配 (keyring crate 自动处理)
use keyring::Entry;

let entry = Entry::new("lingjing-v4", "cloud_api_key")?;
// Windows: Credential Manager
// macOS: Keychain
// Linux: libsecret / KWallet
```

### 9.3 编译目标配置

```toml
# Cargo.toml
[package]
edition = "2021"

# 编译目标
# Windows: x86_64-pc-windows-msvc
# macOS:   x86_64-apple-darwin / aarch64-apple-darwin
# Linux:   x86_64-unknown-linux-gnu
```

---

## 十、硬件加速策略

### 10.1 GPU 推理加速

```
[GPU 加速策略]

┌─────────────────────────────────────┐
│  NVIDIA GPU                         │
│  ├── CUDA 12.x                      │
│  ├── cuBLAS / cuDNN                │
│  └── TensorRT (可选)                │
├─────────────────────────────────────┤
│  AMD GPU                            │
│  ├── ROCm 5.x                       │
│  └── MIGraphX (可选)                │
├─────────────────────────────────────┤
│  Apple Silicon                      │
│  ├── Metal Performance Shaders      │
│  └── Core ML                        │
├─────────────────────────────────────┤
│  Intel GPU                          │
│  ├── OpenVINO                       │
│  └── oneAPI                         │
└─────────────────────────────────────┘
```

### 10.2 Three.js 渲染加速

| 优化项 | 方法 | 效果 |
|--------|------|------|
| 像素比限制 | `Math.min(devicePixelRatio, 2)` | 减少 50% 像素渲染 |
| 材质优化 | MeshStandardMaterial 替代 Phong | +20% 性能 |
| 几何体简化 | Draco 压缩 | 减少 60-80% 传输 |
| 实例化渲染 | InstancedMesh | 大批量模型 +10x 性能 |
| 帧率控制 | requestAnimationFrame | 自适应刷新率 |

### 10.3 存储加速

| 优化项 | 方法 | 效果 |
|--------|------|------|
| SQLite 索引 | 添加常用查询字段索引 | 查询速度 +50-80% |
| 连接池 | r2d2 连接池 | 并发能力提升 |
| 异步 I/O | tokio / aiofiles | 不阻塞主线程 |
| 内存映射 | mmap 大文件 | 减少 I/O 开销 |

### 10.4 网络加速

| 优化项 | 方法 | 效果 |
|--------|------|------|
| HTTP/2 | 升级协议版本 | 多路复用 |
| 连接复用 | Keep-Alive | 减少握手开销 |
| 压缩 | gzip / brotli | 减少 60-80% 传输 |
| 缓存 | HTTP 缓存头 | 减少重复请求 |

---

## 附录

### A. 硬件兼容性矩阵

| 硬件 | 支持状态 | 备注 |
|------|---------|------|
| Intel x86_64 | ✅ 完整 | 主流平台 |
| AMD x86_64 | ✅ 完整 | 主流平台 |
| Apple M1/M2 | ⚠️ 部分 | 需验证 Python 依赖 |
| NVIDIA GPU | ✅ 完整 | CUDA 加速 |
| AMD GPU | ⚠️ 部分 | ROCm 支持有限 |
| Intel GPU | ⚠️ 部分 | OpenVINO 优化 |

### B. 推荐系统配置

| 使用场景 | CPU | 内存 | GPU | 存储 |
|---------|-----|------|-----|------|
| 轻度使用 (仅 UI) | 4 核 | 8 GB | 集成显卡 | SSD 10 GB |
| 中度使用 (3B 模型) | 4 核 | 8 GB | 集成显卡 | SSD 15 GB |
| 重度使用 (7B 模型) | 8 核 | 16 GB | RTX 3060 | NVMe 20 GB |
| 专业使用 (14B+) | 12 核+ | 32 GB | RTX 4090 | NVMe 50 GB |

### C. 参考标准

- [Tauri 2 系统要求](https://v2.tauri.app/start/prerequisites/)
- [WebView2 运行时要求](https://developer.microsoft.com/microsoft-edge/webview2/)
- [Ollama 硬件要求](https://ollama.ai/)
- [SQLite 性能优化指南](https://www.sqlite.org/speed.html)