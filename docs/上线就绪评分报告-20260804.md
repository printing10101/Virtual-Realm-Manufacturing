# 灵境制造（上线版）— 上线就绪评分报告

> 评估日期：2026-08-04 ｜ 分支：refactor/decouple-research-engineering ｜ 版本：2.7.0
> 方法：全部以 CI 等价命令 fresh 实测（非文档推断），失败注入/门禁命令均复刻 CI 脚本。

## 一、门禁矩阵（最终实测）

| 门禁 | 基线（评估前） | 当前（fresh 实测） | 状态 |
|---|---|---|---|
| Python unit（pytest tests/unit） | 567 failed / 592 errors | **1927 passed**（130s） | 🟢 |
| 覆盖率（--cov-fail-under=24） | — | **24.45%**（≥24 门槛） | 🟢 |
| ruff check + format | 893 errors | **0 / 0**（前轮 fresh） | 🟢 |
| 前端 vitest（npx vitest run） | 331 failed / 1404 收集 | **1799 passed / 70 文件**（×3 复跑稳定） | 🟢 |
| eslint（--max-warnings=0） | 大量 | **0 / 0**（本轮全部改动文件） | 🟢 |
| vue-tsc（--noEmit） | — | **0 errors** | 🟢 |
| 集成测试（closed_loop + workflow_dag） | 26 failed | 前轮 **26/26**（fresh）；本轮 Docker daemon 未运行挂起（环境） | 🟡 |
| version_sync | — | 2.7.0 一致（前轮 fresh） | 🟢 |
| smoke / api-docs 同步 | — | 前轮 🟢 | 🟢 |

**Python 侧结论**：unit 1927/1927（基线 567 failed/592 errors → 0）、覆盖率 24.45%（诚实化后门槛达标）、ruff 893 → 0。

**前端侧结论（本轮 fe-1 主体）**：vitest 331 failed → **1799/1799**（进展谱：331→275→207→175→79→56→38→0→（hoisted 修复链浮出 42 真实失败）→0，两次独立全量复跑 + 最终全量共 3 次全绿）；eslint 0/0；vue-tsc 0 errors。

## 二、本轮新修复的生产 bug（真实上线影响）

| # | 文件 | Bug | 影响 |
|---|---|---|---|
| 1 | `src/stores/project.ts` | `structuredClone(manifest.value)` 对 Vue reactive proxy 抛 DataCloneError | **工程保存/另存为完全不可用**（🔴 最严重）→ `toRaw()` 修复 |
| 2 | `src/components/nl_input/ParamEditDialog.vue` | watch 回调 `structuredClone(val)` 对 reactive props 崩（同源） | 参数编辑对话框打开即崩 → `toRaw()` 修复 |
| 3 | `src/api/llmProviders.ts` | `getRegistryStatus` 返回类型/解包泛型错（LLMProvider \| null vs RegistryStatus） | 注册表状态类型错误（vue-tsc 拦截）→ 修正 |
| 4 | `src/locales/zh-CN.ts` + `en.ts` | `workspace.runPredict` key 缺失 | 生产 UI 显示原始 key → 补齐 |
| 5 | `src/stores/dxfImport.ts` | 3 个内部阶段函数未暴露 | 测试/工具无法调用 → 暴露 |
| 6 | 前轮 | llmProviders/simulation/nl2cad 的 `data=null` 误回退包装（unwrapData） | 空数据污染 → 统一解包修复 |

## 三、测试质量修复（fe-1 方法论沉淀）

- **vi.hoisted TDZ 铁律**：vi.mock 工厂引用顶层 const 必崩 → 批量转换 ~30 文件（4 轮扫描 + 手工补漏），修复后浮出 42 个被模块崩掩盖的真实测试失败并全部清零。
- **裸 `vi.mock('axios')` 禁止**：auto-mock 的 `create` 返回 undefined → http.ts 拦截器注册崩溃（顺序相关 flaky）→ 完整 axios 形状工厂。
- **stub 键名铁律（本轮新增）**：VTU 的 `global.stubs` 中 kebab 键（`'el-dialog'`）**不覆盖** setup.ts 的 Pascal 键（`ElDialog`）——必须用 PascalCase 键；否则局部 stub 静默失效（title 走 attrs 变 prop、v-if 失效、具名 slot 不渲染——CollisionAlertModal footer 之谜的根因）。
- **setup.ts 全局 stubs 增强**：ElAlert 补默认 slot（error-detail 渲染）、ElTableColumn 补 `$index` 作用域、ElUpload 渲染 accept 属性。
- **mockState 必须响应式**：普通对象状态使 `watch(status)` 不触发（定时器不停、6 vs 3 之谜）→ reactive() 包装。
- **生产不 return promise 的方法**：`await vm.method()` 无效 → `flushPromises()`。
- 大组件子组件化后的 stale 测试：SnapshotPanel（35→16 行为级）、DxfImportDialog（38→11 行为级）——删旧结构断言，改 vm 方法/事件/子组件 stub 断言。

## 四、诚实交代的未竟事项

1. **集成测试本轮未能 fresh 复跑**：Docker daemon 未运行（`docker ps` 连接失败）导致 closed_loop/workflow_dag 挂起（>10 分钟无输出，已 kill）。前轮 fresh 26/26 证据有效（wf_final.log），且本轮 Python 侧零改动；恢复 Docker 后需重跑确认。
2. **hyg-1 未做**：_git_backup 忽略规则、lint 脚本 --fix 确认、新组件（SnapshotListPanel/SnapshotDetailPanel/SnapshotCreateDialog/DxfFileUpload 等）独立测试——子组件测试由 SnapshotPanel/DxfImportDialog 行为级测试部分覆盖，但无独立文件。
3. **resource_usage 集成测试**（前轮 3 失败）：环境波动待复跑，未处理。
4. **mypy**：已装但未入 CI 门禁。
5. **853 个未提交文件**（用户 299 + 历轮累积）：待 hyg-1 后统一 commit（精确文件列表）。
6. **jsdom 未启用的 happy-dom 限制**：`isVisible()` 等视觉断言不可靠（已用 style/class 断言替代）。

## 五、评分评估

| 维度 | 基线 B+（76/100） | 现状 |
|---|---|---|
| 测试通过率（Python） | 大量失败 | **1927/1927** ✅ |
| 测试通过率（前端） | 331 失败 | **1799/1799**（×3 稳定）✅ |
| 静态检查 | eslint/vue-tsc 未达标 | **0/0 + 0 errors** ✅ |
| 覆盖率 | — | 24.45%（门槛达标，诚实口径）✅ |
| 生产 bug 修复 | — | 6 个真实 bug（含保存/另存为全炸）✅ |
| 集成/E2E | 部分失败 | 🟡（环境依赖，前轮 26/26） |

**综合评级：A-（约 86-88/100）**——相较 B+ 的关键跃升：前后端测试全绿 + 静态检查全绿 + 6 个真实生产 bug 修复（其中 1 个是"保存/另存为完全不可用"级别的 P0）。扣分项：集成测试受环境阻塞未能本轮 fresh 复跑、覆盖率仍低（24% 是下限门槛）、子组件独立测试缺失。

> 注：A- 的前提是 Docker 恢复后集成测试复跑 26/26 确认；若集成复跑出现真实失败，评级应回落到 B+。
