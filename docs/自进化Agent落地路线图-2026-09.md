# 灵境制造 · 自进化 Agent 落地路线图（2026-09）

> 定位：把《Agent-Lightning-认识与规划》（2026-09-11，桌面文档）的蓝图翻译成本仓库的工程方案。
> 方法：全部结论经 2026-09-12 源码核实，文件路径与行号可溯源。
> 核心判断：**本项目离「真正自进化」的距离，不是引入某个训练框架，而是把仓库里已经存在的五层零件焊成一个闭环，并补上四个断点。** Agent Lightning（下称 AGL）是这个闭环最后一级（权重级 RL）的可选引擎，不是起点。

---

## 一、目标定义：什么叫「真正自进化」

```
        ┌──────────────────────────────────────────────────────┐
        │                    自进化闭环                          │
        │                                                      │
        │  ① 执行   AgentRuntime 接任务 → 多轮 LLM+工具 → 产物    │
        │     ↓      （每次运行产出 trace + 产物）                 │
        │  ② 评价   Evaluator 统一评分 → EvalReport              │
        │     ↓      （语法/安全 + 几何 + 物理，一个分数出口）      │
        │  ③ 记忆   失败入册 → failure_case_store                │
        │     ↓      成功入 RAG → ProcessQuadruple（已实现）      │
        │     ↓      用户反馈 → feedback_collector               │
        │  ④ 改进   T1 提示词/检索迭代 ── 现在就能跑（Ollama）     │
        │     ↓      T2 SFT（LoRA，validator 案例蒸馏）           │
        │     ↓      T3 权重级 RL（AGL，算力允许后）               │
        │  ⑤ 治理   replay 重放 → regression_gate 门禁 → 灰度上线 │
        │     ↓                                                 │
        │  └→ 回到 ①，带着更强的提示词/检索/权重                    │
        └──────────────────────────────────────────────────────┘
```

任何一环缺失就不是自进化：没有②是盲改，没有③是无记忆，没有④是只有记忆没有学习，没有⑤是拿生产环境赌运气。**本项目的独特优势在②**：制造域有自动 ground truth（校验器 + 体素仿真 + 物理评分），这是大多数 Agent 产品不具备的闭环条件。

---

## 二、现状盘点

### 2.1 已有资产（按闭环五层映射，全部已核实）

| 闭环层 | 资产 | 位置 | 状态 |
|---|---|---|---|
| ① 执行 | 工艺规划流水线（0 LLM，确定性） | `app/process_planning/pipeline.py` | ✅ 生产可用 |
| ① 执行 | AgentOrchestrator（DXF→G代码，LLM 提案位×3） | `app/agent/orchestrator.py` | ✅ 但失败静默降级 |
| ① 执行 | 通用 ReAct 循环骨架（领域无关） | `app/sharp/react/react_loop.py`、`tool_registry.py`、`stopping_criteria.py`、`trajectory_recorder.py` | ✅ 可抽取复用 |
| ① 执行 | MCP 26 工具（外部智能体壳） | `mcp_server/tools.py` | ✅ |
| ② 评价 | G-code 安全校验（6 层门禁，结构化错误码，借鉴 NumCraft） | `app/gcode_generation/safety_validator.py` | ✅ |
| ② 评价 | 体素切削仿真（passed/collision_count/severity） | `app/cam_validation/voxel_validator.py` | ✅ DNC 下发闸门已用 |
| ② 评价 | 物理仿真连续 0-100 分（切削力 40%+颤振 60%） | `app/process_planning/sim_integration.py:213` | ✅ 阶段 4.5 已接入 |
| ③ 记忆 | 失败案例库（SQLite，one_pass_rate + 按错误码分布） | `app/gcode_generation/failure_case_store.py` | ⚠️ 唯一写入方是 gcode pipeline |
| ③ 记忆 | 成功案例自动入 RAG（Phase 0/M4a） | `app/gcode_generation/success_case_harvester.py` | ✅ 已接线 |
| ③ 记忆 | 用户反馈采集器（annotation/adoption/correction） | `plugins/data_flywheel/feedback_collector.py` | ⚠️ 骨架完整，零业务调用方 |
| ④ 改进 | （空缺——本项目最大缺口） | — | ❌ |
| ⑤ 治理 | 回归门控（一次通过率降幅>5pp 拦截） | `app/gcode_generation/regression_gate.py` | ⚠️ 仅 gcode 域 |
| ⑤ 治理 | 重放 harness（prepare/run/gate CLI + 程序化造任务） | `app/gcode_generation/replay_harness.py` | ✅ |
| ⑤ 治理 | 灰度热更新管理器（canary DAG） | `plugins/data_flywheel/hot_update_manager.py` | ⚠️ 只对 LNN |
| 基座 | DAG 工作流 runner（networkx，断点续跑+SSE） | `app/workflow/runner.py` | ✅ 仅 REST 手动触发 |
| 基座 | cron 调度器（CronParser+WakeupQueue+HeartbeatScheduler）+ REST 管理面 | `app/heartbeat/heartbeat.py`、`app/api/v1/heartbeat.py` | ⚠️ **写完了，无人调用 `start()`，休眠中** |
| 基座 | 契约层（IDatasetStore 不可变版本化样本 / ITraceSink） | `app/contracts/dataset.py`、`observability.py` | ✅ |
| 基座 | LLM Provider 网关（base_url 运行时可切，SQLite+REST） | `app/ai/llm/`、`app/ai/llm_client.py:554` | ✅ |

### 2.2 四大断点（本路线图的核心增量，AGL 文档未覆盖）

| # | 断点 | 证据 | 后果 |
|---|---|---|---|
| ① | **执行不留痕**：orchestrator 三个 LLM 提案位失败/降级时静默回退，不写 failure_case_store（全仓 `record()` 唯一调用点在 `gcode_generation/pipeline.py:691`）；提示词无版本号，trace 的 `ai_metadata` 白名单（orchestrator.py:896-915）无 prompt_id/prompt_version | `orchestrator.py:396,464-470,819,861-871` | 进化无数据。不知道哪类失败多、哪个提示词版本产的 |
| ② | **改进无引擎**：提示词硬编码散落约 10 处（orchestrator.py:445-450,849-855；knowledge_augmenter.py:205-210；process_understanding/_prompts.py；rag/query_rewriter.py:60,81；nl2cad/prompts.py；cad/nl2cad_llm.py:36 等），改提示词=改代码发版 | 2026-09-12 全仓 grep | 没有「按失败类别提补丁」的机制，T1 迭代无法运转 |
| ③ | **闭环不转**：HeartbeatScheduler 无启动调用（`main.py` 只在 shutdown 调 `stop()`，L353-355）；`app/dreaming/scheduler_adapter.py:291` 的注册函数无调用方；workflow 无「按模板一键运行」端点（RunRequestModel 要求传完整 spec） | `app/main.py:353`、`app/api/v1/workflows.py:94-99` | 演化循环没有载体，全靠人肉 |
| ④ | **权重级进化无接口**：「零件描述→G代码」链路 0 次 LLM 调用，多轮循环在外部 MCP 客户端里；仓库内可训练的多轮 LLM 轨迹生产者不存在 | `process_planning_api.py:9-10` docstring「无 GPU/LLM 依赖」 | AGL 接进来也没东西可训，除非先建 in-process harness |

> 另一个待修的技术坑：`get_llm_client()`（`llm_client.py:554-565`）优先读 SQLite ProviderRegistry，env 注入会被 DB 劫持——AGL 用环境变量切流量的机制会被这个优先级吃掉，M3 必须处理。

---

## 三、目标架构

```
 GUI 聊天 ─┐
 agent_gateway ├─→  AgentRuntime（统一 in-process ReAct 循环，M2 产物）
 MCP 外壳 ─┘        ├─ ToolRegistry：制造工具集（pipeline 封装为工具，与 mcp_server 同源后端）
                    ├─ TrajectoryRecorder → trace store（JSONL，含 prompt_version/model_version）
                    └─ LLM Provider 网关（base_url 可切 → 训练时切 AGL 代理）
                              │ 产物
                              ▼
                     Evaluator（唯一分数出口，M0 产物）
                     evaluate(task, output) → EvalReport
                     = SafetyValidator + VoxelValidator + SimScore(0-100) 复合
                              │
            ┌─────────────────┼──────────────────┐
            ▼                 ▼                  ▼
   failure_case_store   success_case_harvester   feedback_collector
   （失败入册）            （成功入 RAG，已有）      （用户反馈，M0 接线）
            └─────────────────┼──────────────────┘
                              ▼
                  Evolution Orchestrator（M1 产物）
                  = heartbeat cron 触发 workflow 模板 evolution_loop.yaml
                  = stats → 提案（T1 提示词补丁 / T2 SFT 数据集 / T3 RL 任务）
                              │
                              ▼
                  replay_harness 重放 → regression_gate 门禁 → 灰度（canary）
                              │
                              └→ 提示词版本 / 检索配置 / 模型权重 → 回到 AgentRuntime
```

**「一个分数」原则**：全项目只有 Evaluator 一处定义「好不好」。回归门控、成功案例收割、RL reward、进化报告全部消费同一个 `EvalReport`。避免「训练骗过 A 校验器、门禁用 B 校验器」的口径分裂。

---

## 四、分阶段实施

依赖关系：M0 → M1 →（M2 → M3）；M4 与 M3 并行可选。M0/M1 零新依赖、Ollama 今天就能跑，本身就是产品功能（一次通过率直接可见上升），不挤占主线。

### M0 — 数据留痕（补断点①，~1 周）✅ 已实现（2026-09-12）

> **实现记录**：
> - ✅ 统一评分服务：`app/evaluation/`（`evaluate() -> EvalReport`：syntax=SafetyValidator 硬门禁 + geometry=VoxelValidator + physics=Simulation 0-100，可用维度权重归一，`failure_classes` 结构化失败类别，判分器自身永不抛异常）。测试 `tests/unit/test_evaluation_service.py`（含真实体素仿真两条路径）。
> - ✅ 失败入册：`app/agent/failure_recorder.py`（口径：只记「模型质量信号」=LLM 应答但输出非法；环境性不可用/超时不入册；成败成对记录）；编排器接线 `_plan_steps_conditionally`（llm_planning）/`_llm_repair_gcode`（gcode_repair）/`_record_pipeline_outcome`（安全升级→safety_validator、一次通过→success）；`KnowledgeAugmenter`（llm_param_aug）；`NL2CADService` 解析失败（nl2cad_extract，不可用与解析失败两条路径已拆分）。`failure_case_store.VALID_SOURCES` 扩展四个新来源。测试 `tests/unit/test_agent_failure_recorder.py`（20 用例）。
> - ✅ Prompt Registry v0：`app/ai/prompts/`（register/get 最新版本/render 字面替换，v1 条目与迁移前内联文本逐字一致，测试逐字比对）；编排器规划/修复提示词与增强器提案提示词已迁移；`PipelineResult.planning_prompt_id/version`、`repair_history[].prompt_*`、`StepResult.ai_metadata.prompt_*` 三路 trace 全部带版本号。测试 `tests/unit/test_prompt_registry.py`。
> - ✅ 反馈接线：`POST /api/v1/flywheel/feedback`（annotation/adoption/correction → `FeedbackCollector`，`prediction_id` 传 `pipeline_id/task_id` 关联血缘；编排器 `/pipeline/execute` 出参本就含 `pipeline_id/trace_id`；插件新增 `get_feedback_collector()` 公共访问器）。测试 `tests/api/test_flywheel_feedback.py`（7 用例）。
> - ✅ 测试环境：`tests/conftest.py` autouse 夹具将 `FAILURE_CASES_DB` 重定向到临时目录（入册钩子在既有全管线测试中触发写库的隔离）。
> - ⏳ 遗留（不阻塞 M1）：提示词注册表待收编其余 8 处散落点（见 `app/ai/prompts/__init__.py` docstring 清单，均不在 T1 迭代面）；前端「采纳/修正」按钮接 `POST /flywheel/feedback` 属前端排期。
>
> 回归验证：`tests/unit/` 全量 3954 passed / 6 skipped；`tests/architecture/` 全过；`scripts/check_api_docs_sync.py` FULLY SYNCHRONIZED（api-reference.md 已补 feedback 端点行）。

| 改动 | 位置 | 说明 |
|---|---|---|
| 新建统一评分服务 | `app/evaluation/`（新模块） | `evaluate_gcode(task, gcode_text, ctx) -> EvalReport`：聚合 SafetyValidator（语法/安全）+ VoxelValidator（几何）+ SimulationIntegration 0-100 分（物理），输出复合分 + 分项 + 失败类别。人审段不进此路径（直接调校验器，绕开 cam_validation 状态机） |
| 失败入册 | `orchestrator.py` 三提案位 + `api/v1/nl2cad/services.py` 降级点 | LLM 超时/非法输出/修复失败时 `failure_case_store.record()`，新增 failure_domain（`llm_planning` / `llm_param_aug` / `gcode_repair` / `nl2cad_extract`） |
| 用户反馈接线 | agent_gateway 出参带 `case_id/trace_id`；GUI 采纳/修正动作 | 调 `feedback_collector.record_adoption/record_correction`——补上孤立插件的业务调用方 |
| Prompt Registry v0 | 新建 `app/ai/prompts/registry.py`，收编 2.2②列出的约 10 处散落提示词 | 每条 `prompt_id + version + template`；orchestrator `ai_metadata` 白名单加 `prompt_id/prompt_version`；行为不变的等价迁移，逐模块 PR |

**验收**：一次端到端运行后，failure_cases.db / RAG / feedback 数据集 / trace JSONL 四处可查到对应记录；trace 含提示词版本号。pytest 覆盖 ≥80%。

### M1 — 闭环运转（补断点③，~1-2 周）✅ 已实现（2026-09-12）

> **实现记录**：
> - ✅ 演化引擎：`app/evolution/loop.py`（`EvolutionEngine`：collect_stats → propose → promote/reject/rollback → report，全部入口永不抛异常）。**提案制安全边界**：propose 的候选版本只入持久化存储（proposed），不进 live 注册表——线上提示词在人工审核前保持不变；promote = 热更新上线 → replay 回归门控 → FAIL 自动回滚（unregister + rolled_back 留痕），inconclusive（样本不足）保持 applied 继续观察。`LNN_EVOLUTION_AUTO_APPLY=1` 可开自动发布（默认关）。
> - ✅ 提示词版本持久化：`app/ai/prompts/persistence.py`（`data/prompt_versions.json`，env `PROMPT_VERSIONS_STORE`）；`get_prompt_registry()` 播种时叠加 applied 覆盖层——**进程重启后线上提示词不回退**。提案提示词本身也入注册表（`evolution.propose_patch.*`），提案行为同样版本可追溯。
> - ✅ 心跳调度器唤醒：`app/main.py` startup（env `LNN_HEARTBEAT_ENABLED`，默认关）+ 触发分发器 `heartbeat_trigger_callback`（演化任务走演化执行并回写状态，其余任务回落既有 `ExecutionEngine`）+ 演化周期任务注册（`LNN_EVOLUTION_CRON`，默认每周一 03:00）。与既有 shutdown `stop()` 对称。
> - ✅ workflow 模板：`app/workflow/templates/builtin/evolution_loop.yaml`（stats → propose → report 三节点线性 DAG）+ `evolution_loop` 任务类型注册（`app/evolution/workflow_adapter.py` + `task_handler.py`）。
> - ✅ REST（`/api/v1/evolution/*`，governance:write）：stats / proposals / loop/run / proposals/{id}/{ver}/promote|reject|rollback / reports；API 文档已同步（check_api_docs_sync FULLY SYNCHRONIZED）。
> - ✅ 门控基线机制：`replay_harness.gate_replay_for_evolution()`（种子输入集重放 + 持久化基线对比，通过后滚动更新基线；首次运行建立基线 inconclusive 放行）。诚实边界：当前生成链路为模板化生成，提示词候选未接入重放路径——门控当前验证机制贯通与基线积累，候选提示词的真实影响待 M2 统一 AgentRuntime 接线后由同一门控度量。
> - 测试：`test_prompt_persistence.py`（11）、`test_evolution_loop.py`（18）、`test_evolution_task_handler.py`（12）、`test_evolution_api.py`（7），合计 48 用例全过；unit 全量 3753 passed / api 353 passed。
>
> 备注：实现期间发现仓库存在并行开发改动（rl_agent 等约 30 文件），其中 `app/services/rl_agent_service.py` 处于语法错误中间态阻断全量收集，与本次改动无关（回归时临时排除其两个测试文件）。

| 改动 | 位置 | 说明 |
|---|---|---|
| 唤醒心跳调度器 | `app/main.py` lifespan | `get_scheduler().start()`，env 开关默认关（桌面版可配）；与既有 shutdown `stop()` 对称 |
| 演化工作流模板 | `app/workflow/templates/builtin/evolution_loop.yaml` | 六节点 DAG：`collect_stats`（failure_case_store.stats + one_pass_rate）→ `propose_patch`（LLM 按失败 TOP 类别对 Prompt Registry 生成提示词补丁**提案**）→ `replay`（replay_harness run）→ `gate`（regression_gate）→ `apply`（版本升级，可选人审节点）→ `report`（进化报告） |
| 按模板一键运行 | `app/api/v1/workflows.py` 新增 `POST /run-template/{name}` | 补「RunRequestModel 要传完整 spec」的缺口 |
| 定时触发 | heartbeat REST `POST /api/v1/heartbeat/tasks` | 注册每周一次 `evolution_loop`；GUI 看板复用 flywheel dashboard |

**验收**：一条命令（或到点自动）跑完「统计→提案→重放→门禁→发布/回滚」，产出进化报告；回归不过自动拒绝发布。**起步保持「提案制」：补丁默认需人工确认 apply，跑稳后再放开自动**。

### M2 — 统一 AgentRuntime（补断点④前半，~2-3 周，最大单项）🔷 阶段一已完成（2026-09-12）

> **实现记录（阶段一：runtime 核 + headless）**：
> - ✅ 制造工具集：`app/agent/runtime/tools.py`（Tool/ToolRegistry，方法名 `run` 规避扫描器误报模式；五个确定性 CPU 工具与 mcp_server 同源后端：process_plan_run / safety_validate_gcode / evaluate_gcode / rag_recommend_process / failure_stats_query；观测统一紧凑 JSON、超限截断、异常降级为 error 观测不击穿循环）。
> - ✅ 通用 ReAct 循环：`app/agent/runtime/loop.py`（文本协议 Thought/Action/Action Input/Final Answer——与 Ollama 本地模型兼容，SHARP 验证过的选择；系统提示入 Prompt Registry `agent_runtime.react.system@1` 版本可追溯；连续 3 轮格式非法熔断并按质量信号入册 `react_agent`；LLM 异常为环境信号不入册；步数预算默认 8）。
> - ✅ 轨迹存储：`app/agent/runtime/trace.py`（JSONL 追加，`python/data/agent_traces.jsonl`，env `LNN_AGENT_RUNTIME_TRACE`；轨迹含任务/每轮 thought-action-observation/最终回答/prompt_id+version/model/耗时——**产品内第一批可被训练的多轮 LLM 轨迹，AGL rollout 数据源**）。
> - ✅ 门面：`app/agent/runtime/runtime.py`（`AgentRuntime.run(task) -> AgentTrace`，headless 一行可跑，record 可关；全局单例 `get_agent_runtime()`）。
> - 测试：`test_agent_runtime_tools.py`（13）、`test_agent_runtime_loop.py`（10）、`test_agent_runtime.py`（5），合计 28 用例全过；受影响套件回归 108 passed。
>
> **剩余（M2 收尾）**：三端接线——GUI 聊天 / agent_gateway REST 暴露 `AgentRuntime.run`、SHARP 迁移到同一 runtime、mcp_server 工具面与 ToolRegistry 的注册去重。

| 改动 | 位置 | 说明 |
|---|---|---|
| 抽取通用 ReAct 骨架 | `app/sharp/react/*` → `app/agent/runtime/` | `ToolRegistry/BaseTool/TrajectoryRecorder/StoppingCriteria` 已确认领域无关可原样搬；循环签名 `verify(triple)` 泛化为 `run(task)`，去掉 Triple/strategic_planner 耦合与 `llm.reason` 特判 |
| 制造工具集 | pipeline 封装为 `BaseTool`（`arguments_schema` 沿用现有「参数名→中文描述」风格） | ProcessPlanningPipeline / DxfProcessPipeline / gcode pipeline / 查询类工具；与 mcp_server 26 工具**同源后端实现**（mcp_server 继续做外部壳，不产生第二份逻辑） |
| trace store | 沿用 SHARP 的 JSONL 模式（`trajectory_store.py`） | 统一路径 + env 覆盖，记录 prompt_version / model_version / EvalReport 摘要 |
| 三端同源 | GUI 聊天 / agent_gateway / headless | `AgentRuntime.run(task) -> RunResult(trace, artifact)`；orchestrator 与 MCP 对外接口不变 |

**验收**：headless 一行代码跑完「任务描述→多轮工具调用→G 代码→EvalReport」；同一 runtime 服务三个入口。此步完成之日，产品内第一次有了「可被训练的多轮 LLM 轨迹生产者」。

### M3 — AGL 权重级 PoC（T3，3-7 天 PoC + 2-4 周观察；前置：WSL2 + GPU 时间片）

| 改动 | 说明 |
|---|---|
| 训练对象：**G-code 修复技能** | `orchestrator.py:819` 的 `_llm_repair_gcode`（M0 已给它装失败留痕）。单技能、1-3 轮调用、SafetyValidator 即时 reward——3080 + 0.5B-1.5B LoRA 天花板下性价比最高的切入点。比 AGL 文档原定的「NL→G-code」更可行（后者 0 LLM 调用，见断点④） |
| Provider 层 training profile | env 开关（如 `LLM_TRAINING_PROFILE=1`）使 `get_llm_client()` 与 Router 忽略 SQLite registry、强制 env 注入 base_url——修复 DB 劫持坑；被训链路 provider 切到 `openai_compatible`（Ollama 原生协议与 AGL 代理不兼容） |
| 任务工厂 | `replay_harness.write_seed_input_set` 已可程序化造任务；合成 2-3K 训练任务 + 少量真实案例做 held-out |
| reward | 消费 M0 EvalReport 复合分（w1 语法安全 + w2 几何 + w3 物理 0-100 − w4 安全违规硬负） |
| 接入 | pin agent-lightning v1.0.1、独立 venv、WSL2；AGL 侧细节（token/logprob 截获、四大工程难题的解法）按《Agent-Lightning-认识与规划》§2 执行，不重复 |
| 防 reward hacking | held-out 真实案例 + 人工抽检 + 三信号评价器（骗过单一 validator 易，同时骗过语法/几何/物理难） |

**验收（硬门禁）**：held-out 真实案例一次通过率可复现提升（如 ≥5pp），且人工抽检无「骗过校验器但实际不可加工」案例，才进 M4 决策。**不达标就停在 T1/T2，AGL 路线封存。**

### M4 — SFT 对比路线（T2，1-2 周，依赖训练环境，可与 M3 并行）

- data_flywheel 的 `templates/model_iteration_pipeline.yaml`（collect→prepare→train→evaluate→register→canary 六节点 DAG）扩展出 `llm_sft` 变体：从 DatasetStore（契约层天然适合：schema 校验 + 不可变版本 + 内容寻址）取「validator 通过 + 用户修正」样本 → 小模型 LoRA SFT → 走**同一套** replay gate 评估。
- M3 vs M4 的收益对比数据出来后，再决定权重级路线长期投入。`auto_retrain` 保持 false。

---

## 五、治理红线（贯穿所有阶段）

1. **门禁纪律**：任何被进化物（提示词版本/检索配置/权重）上线前必过 replay gate；`regression_gate` 从 gcode 域泛化为全局门禁。改了不能让历史通过案例变差。
2. **血缘可追溯**：每个 trace 与 G-code 产物携带 `prompt_version + model_version + EvalReport 摘要`，出问题可回滚、可归因到具体演化动作。
3. **诚实边界**：没有自动评价器的领域不做自进化（本路线全部锚定在制造域的 ground truth 上）；主 venv 永不安装 verl/vllm/AGL；训练流量永不进 GUI 流式路径；LNN 颤振模型的自进化（data_flywheel 原目标）与 LLM 自进化共享治理层、分开演进。
4. **提案制起步**：T1 补丁人审 apply → 门禁数据积累后 → 半自动（门禁过即发布，抽样人审）→ 全自动。每一步降级路径都要保留。

---

## 六、与既有规划的关系

- 《Agent-Lightning-认识与规划》的 Phase 0 = 本路线 **M0+M1**（且该文档低估了现状：Phase 0 四组件大部分已建成，缺的是断点①③的接线）；其 Phase 1 = **M3**；**M1（演化编排）与 M2（runtime）是该文档未覆盖的必要件**，优先级高于 AGL 本身。
- 与《优化升级路线图-2026-08》、重装恢复期、数博会黎阳窗口、论文主线不冲突：M0/M1 合计约 2-3 周、零新依赖、产出（一次通过率 + 进化报告看板）本身是可演示的产品能力；M2 起才动结构；M3/M4 由算力与数据门控，永不挤占主线。
