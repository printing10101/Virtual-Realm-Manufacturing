# 灵境制造 — 上线就绪审计报告（2026-08-07 fresh 实测）

> 评估日期：2026-08-07 ｜ 分支：refactor/decouple-research-engineering ｜ 版本：2.7.0
> 方法：全部以 CI 等价命令 fresh 实测（非文档推断）。对照：`docs/上线就绪评分报告-20260804.md`（A-/86-88，多个关键数字被本次实测推翻）。

## 一、门禁矩阵：报告声称 vs 本次实测

| 门禁 | 报告声称(20260804) | 本次 fresh 实测 | 判定 |
|---|---|---|---|
| Python unit | 1927/1927 🟢 | **1357 passed / 0 failed**（CI 等价 `-m unit`）；1927 是不带 `-m` 的收集数，口径错误 | ⚠️ 测试本身过，数字不实 |
| 覆盖率（≥24%） | 24.45% 🟢 | **9.39%**（83327 语句盖 8977）→ `FAIL Required 24.0%`，exit 1 | 🔴 CI unit job 当前就是红的 |
| ruff | 0/0 🟢 | 0/0 | ✅ |
| vitest | 1799/1799 ×3 全绿 🟢 | **1862 测试，3 连跑中 SimulationViewer 超时失败 2 次**（flaky） | ⚠️ 偶发红 |
| eslint | 0/0 🟢 | **16 errors + 740 warnings**（`--max-warnings=0` 必挂） | 🔴 CI lint 红 |
| vue-tsc | 0 errors 🟢 | 0 errors | ✅ |
| 前端覆盖率 | 达标 🟢 | **从未真实跑通**：lockfile 无 `@vitest/coverage-v8`，`--coverage` 崩溃；vitest.config 内置 65% vs CI 检查 24% 自相矛盾 | 🔴 幻影门禁 |
| 后端启动 | — | **真实启动成功**：desktop_runtime(Py3.12.13)+start_server.py，冷启动 ~20s，555 路由在线，鉴权/限流正常 | ✅ 核心可用 |
| 顶层 tests/（MES/DNC） | — | **25 failed / 10 passed**（全部 401，测试未适配强制鉴权；不在 CI 收集范围 = 孤儿测试） | 🔴 被遗忘的测试 |
| version_sync | 2.7.0 一致 🟢 | 一致 | ✅ |
| api-docs 同步 | 🟢 | **假绿**：check_api_docs_sync.py 扫旧路径 `python/app/`（V2.7 后不存在）→ "0 路由 = FULLY SYNCHRONIZED" | 🔴 门禁失效 |
| CI 覆盖率检查步骤 | — | `if: matrix.python-version == '3.10'`，但 matrix 只有 3.11/3.12 → 永远不执行（手写门槛死代码；真正生效的是 pytest-cov fail_under） | 🔴 死代码 |

## 二、上线差距清单（按严重度）

### 🔴 P0 — 阻断真实上线
1. **CI 主流水线是红的**：python-unit-tests 覆盖率 9.39% < 24% fail_under（exit 1）；eslint 16 errors；前端 coverage 步骤必崩。当前任何 push 到 CI 都会失败。
2. **GitHub Releases 无任何安装包**（API 实测 NO RELEASES），README 却声称"前往 GitHub Releases 下载最新安装包"→ 用户按文档操作是断链。tag v1.7/1.8/1.10 存在但无 release 资产。
3. **eslint 16 errors 全是真实 Vue 反模式**：`vue/no-mutating-props`（EquipmentSettingsDialog 4 处、WorkflowSubmitDialog 3 处、WorkspaceTrainForm 9 处）——子组件直接改父组件 prop，会造成状态静默串扰。

### 🟠 P1 — 上线前必须修
4. **sidecar_main.py（开发模式入口）在有 torch 的环境启动必失败**：stub `__getattr__` 把 `__file__` 等 dunder 也返回成函数 → torch import 时 inspect 遍历 sys.modules 拿函数去 `.endswith` → 崩。打包运行时无 torch 不受影响，但开发机/CI 装 torch 后桌面 dev 模式无法启动。（已实测复现 2 次）
5. **顶层 MES/DNC 测试 25 个失败**：代码加了强制鉴权后测试未同步（全部 401）；且这些测试不在 CI 任何 job 里，属孤儿测试，失败无人知晓。
6. **前端 flaky**：SimulationViewer.test.ts 的 API 导出测试 10s 超时（3 连跑 2 次失败）——CI 上随机红，且掩盖真实回归。
7. **冷启动 ~20s vs Tauri 就绪超时 45s**：余量仅 25s，低配机/首次启动可能超时被杀；README 声称"3-8 秒"不实。
8. **api-docs 同步检查假绿**：脚本路径未随 V2.7 重构更新，永远报告"FULLY SYNCHRONIZED"。

### 🟡 P2 — 质量债
9. **前端覆盖率从未真实测过**：lockfile 缺 provider、门槛矛盾（65% vs 24%）。
10. **上线就绪评分报告（20260804）多个数字不实**：1927 口径错误、覆盖率 24.45% 未复现、eslint "0/0" 不实、vitest "×3 全绿" 不实、api-docs "🟢" 假绿——作为上线依据不可信，需按实测重写。
11. 集成测试本机无法复跑（Docker daemon 未运行）；单独跑 integration 卡在 PIL 图像处理超时，需定位。
12. pydantic `protected namespace model_*` 警告刷屏（3+ 处 Response 模型）、OPC UA 依赖缺失警告（可选功能）。

## 三、做得好的（值得保留）
- ✅ 后端真实可启动、可登录、555 路由在线，desktop_runtime 自包含（Py3.12.13 无 torch，符合解耦声明）
- ✅ 安全设计扎实：JWT fail-closed、占位符密钥黑名单、限流（实测触发 1007）、生产关闭 docs、CSP、初始密码随机化+警告
- ✅ 源码无硬编码密钥、.env 正确 gitignore、Git LFS 正常、version_sync 一致、ruff/vue-tsc 干净
- ✅ TODO 债务极少（后端 2 处、前端 8 处）

## 四、综合评级修正
**报告自评 A-（86-88）→ 实测 B-/C+（约 65-70）**。核心落差：CI 主流水线实际是红的 + 发布渠道断链 + 多个门禁假绿。后端核心能力（启动、路由、安全）是真的，但"全绿 + 可下载安装包"是幻影。

## 五、修复建议顺序
1. **A. CI 门禁修复**：unit job 删死代码检查（依赖 pytest-cov fail_under）；api-docs 脚本路径更新；前端 coverage 依赖补进 lockfile 或 CI 去掉 `--coverage`
2. **B. eslint 16 个 prop 变异真实修复**（3 个组件）
3. **C. sidecar stub `__getattr__` 白名单化**（放行 dunder）
4. **D. 顶层 MES/DNC 测试适配鉴权**（或收进 CI 收集）
5. **E. 前端 flaky 修复**（SimulationViewer 超时）
6. **F. 发布渠道**：打 tag + 建 Release 上传安装包（或 README 改为诚实的"源码构建"指引）

## 六、2026-08-07 修复执行记录（B/C + 预存 bug）
- ✅ **C 项**：`sidecar_main.py` stub `__getattr__` 白名单化（放行 dunder）——有 torch 环境启动失败已修复（`{"ping":true}` 实测）
- ✅ **B 项**：7 个前端文件 prop 变异修复（本地副本 + 双向 watch + `toRaw` 防 DataCloneError + `update:xxx` 回写）；`ExplainabilityDialogs.vue` Function 类型修正——eslint errors 16→0，vue-tsc 0，相关测试全过
- ✅ **预存 bug 1**：`fusion_trainer.py` mlflow 三件套无 None 判空（解耦后工程侧训练必崩）——加 no-op fallback，world_model 测试 4 failed→0
- ✅ **预存 bug 2**：`test_unified_state_assembler.py` async execute 未 await——加 `@pytest.mark.asyncio` + `async def`，159/159 全过
- 🔴 **环境重大发现**：本机"系统 Python 3.11"实为 **3.11.0rc2（2022 发布候选版）**，`import sentence_transformers` 直接 SIGSEGV（EXIT=139）——所有本地测试在此解释器上跑 C 扩展（transformers/tokenizers）相关测试都会崩。CI 用 actions/setup-python 装稳定版 3.11，不受影响。**建议**：安装官方稳定版 3.11.x 替换或升级 `AppData/Local/Programs/Python/Python311`，否则本地无法验证任何 sentence-transformers 相关代码路径。
## 七、2026-08-14 修复执行记录（D 项：MES/DNC 孤儿测试收编 + 覆盖率门禁实测）

### ✅ D 项：顶层 MES/DNC 孤儿测试收编（25 failed → 27 passed）

**背景**：tests/test_dnc_api.py、tests/test_mes_api.py 位于仓库根 tests/，不在 pytest.ini
testpaths（engineering/python/tests）覆盖范围，CI 从不收集（孤儿测试）；且代码加强制鉴权后
未适配，本地跑全部 401。

**处置**：
1. **迁移**：git mv 至 engineering/python/tests/api/，纳入 CI full suite（pytest tests/）收集。
2. **鉴权适配**：新位置由 engineering/python/tests/conftest.py 预置
   LNN_PERMISSION_ENFORCED=false（测试降级环境，与既有 API 测试一致）。
3. **MES 测试修复**：补 mocker 缺失 fixture 参数（NameError）、datetime.now() → UTC（P2-7 约定）、
   底部 import 上提到模块顶层；业务用例统一走 app.dependency_overrides[get_mes_client] mock 注入。
4. **DNC 测试修复**：原用例真实连接 OPC UA/MTConnect（5 次重试可挂起数十秒），改为 autouse fixture
   mock dnc_manager 网络方法（AsyncMock/PropertyMock），仅验证 API 契约。
5. **响应断言对齐**：当前统一信封格式为成功 {code:0, data:...}、业务错误 HTTP 200 + 非零 code、
   异常 {code, message}（5xx 消息脱敏为「系统内部错误，请联系管理员」）。
6. **CI 依赖安全**：改用 unittest.mock + monkeypatch，不依赖 pytest-mock（CI pip install 未包含）。

**验证**：pytest engineering/python/tests/api/test_dnc_api.py test_mes_api.py → **27 passed**。
api 目录全量回归（两个文件置于末位收集）→ 全部通过。

**覆盖率门禁实测（CI 等价命令）**：
python -m pytest tests/unit/ -m "not skip_ci" --cov=app --cov-config=../../.coveragerc ...
→ 2966 passed, 0 failed（162.7s）
→ statements=83627 covered=31117 → 覆盖率 33.67% ≥ 24% 阈值 ✅
  （此前审计 9.39% 为错误口径「-m unit」测得；CI 实际命令收集 2966 个测试，门禁为绿）

**前端覆盖率幻影门禁核查**：@vitest/coverage-v8 已在 package.json / pnpm-lock.yaml /
node_modules 就绪，不再是幻影门禁。

**本地环境备注（非代码缺陷）**：本机（Windows + Python 3.11.0rc2）在 pytest tests/api/ 全目录
默认字母序收集时，test_dnc_api 的模块导入（dnc_manager → asyncua）会偶发污染后续文件收集的
TxF stat()（WinError 6714，Windows 专属）。CI 跑 ubuntu 不受影响；本机将新文件置于末位收集或
分文件运行均可正常通过。

